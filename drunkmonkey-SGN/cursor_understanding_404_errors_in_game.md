# Understanding 404 errors in game assets
_Exported on 11/15/2025 at 14:46:39 GMT+1 from Cursor (2.0.75)_

---

**User**

Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/music/game_music.mp3:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/music/menu_music.mp3:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/music/victory_music.mp3:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/sfx/sfx_jump.wav:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/sfx/sfx_throw.wav:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/sfx/sfx_hit.wav:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/sfx/sfx_enemy_death.wav:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/sfx/sfx_player_hurt.wav:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/sfx/sfx_game_over.wav:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/sfx/sfx_victory.wav:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

assets/audio/sfx/sfx_boss_roar.wav:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

favicon.ico:1  Failed to load resource: the server responded with a status of 404 (Not Found)Understand this error

PreloadScene.js:107 Uncaught TypeError: this.textures.rename is not a function

    at PreloadScene.generateMissingAssets (PreloadScene.js:107:31)

    at PreloadScene.create (PreloadScene.js:60:14)

    at initialize.create (phaser.min.js:1:954364)

    at initialize.loadComplete (phaser.min.js:1:953753)

    at a.emit (phaser.min.js:1:2080)

    at initialize.loadComplete (phaser.min.js:1:531612)

    at initialize.fileProcessComplete (phaser.min.js:1:531312)

    at t.onProcessComplete (phaser.min.js:1:525102)

    at data.onload (phaser.min.js:1:556046)

---

**Cursor**

Revisando el código para entender los errores. Examinando los archivos relevantes.


Leyendo `PreloadScene.js` para entender el problema:


Problemas identificados:
1. `this.textures.rename()` no existe en Phaser 3 (línea 107 de `PreloadScene.js`).
2. Archivos de audio faltantes: las carpetas existen pero están vacías, generando 404.

Corrigiendo ambos:


Corrigiendo ambos problemas:


```javascript
class PreloadScene extends Phaser.Scene {
    constructor() {
        super({ key: 'PreloadScene' });
    }
    
    preload() {
        // Mostrar barra de carga
        const width = this.cameras.main.width;
        const height = this.cameras.main.height;
        
        const progressBar = this.add.graphics();
        const progressBox = this.add.graphics();
        progressBox.fillStyle(0x222222, 0.8);
        progressBox.fillRect(width / 2 - 200, height / 2 - 30, 400, 50);
        
        const loadingText = this.make.text({
            x: width / 2,
            y: height / 2 - 50,
            text: 'Cargando...',
            style: {
                font: '20px Arial',
                fill: '#ffffff'
            }
        });
        loadingText.setOrigin(0.5, 0.5);
        
        const percentText = this.make.text({
            x: width / 2,
            y: height / 2 - 5,
            text: '0%',
            style: {
                font: '18px Arial',
                fill: '#ffffff'
            }
        });
        percentText.setOrigin(0.5, 0.5);
        
        // Actualizar barra de progreso
        this.load.on('progress', (value) => {
            progressBar.clear();
            progressBar.fillStyle(0xffffff, 1);
            progressBar.fillRect(width / 2 - 190, height / 2 - 20, 380 * value, 30);
            percentText.setText(parseInt(value * 100) + '%');
        });
        
        this.load.on('complete', () => {
            progressBar.destroy();
            progressBox.destroy();
            loadingText.destroy();
            percentText.destroy();
        });
        
        // Cargar assets usando AssetLoader
        const assetLoader = new AssetLoader(this);
        assetLoader.preload();
    }
    
    create() {
        // Generar assets programáticamente si no se cargaron
        this.generateMissingAssets();
        
        // Ir al menú
        this.scene.start('MenuScene');
    }
    
    generateMissingAssets() {
        // Generar sprites si no existen
        if (!this.textures.exists('player')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generatePlayer();
        }
        if (!this.textures.exists('monkey_basic')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generateMonkeyBasic();
        }
        if (!this.textures.exists('monkey_fast')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generateMonkeyFast();
        }
        if (!this.textures.exists('monkey_strong')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generateMonkeyStrong();
        }
        if (!this.textures.exists('projectile')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generateProjectile();
        }
        
        // Generar gorilas si no existen
        for (let i = 1; i <= 4; i++) {
            if (!this.textures.exists(`gorilla_${i}`)) {
                const spriteGen = new SpriteGenerator(this);
                spriteGen.generateGorilla(i);
            }
        }
        
        // Generar fondos si no existen
        if (!this.textures.exists('menu_bg')) {
            const bgGen = new BackgroundGenerator(this);
            bgGen.generateMenuBackground();
        }
        for (let i = 1; i <= 4; i++) {
            // Para nivel 4, verificar también el nombre alternativo con typo
            let bgKey = `jungle_bg_${i}`;
            if (i === 4 && this.textures.exists('jungle_bg_4_alt') && !this.textures.exists('jungle_bg_4')) {
                // Copiar la textura con typo al nombre correcto (Phaser 3 no tiene rename)
                const sourceTexture = this.textures.get('jungle_bg_4_alt');
                const canvas = sourceTexture.getSourceImage();
                this.textures.addCanvas('jungle_bg_4', canvas);
            }
            
            if (!this.textures.exists(bgKey)) {
                const bgGen = new BackgroundGenerator(this);
                bgGen.generateJungleBackground(i);
            }
        }
    }
}


```

```javascript
/**
 * Utilidad para cargar assets externos si están disponibles
 * Este archivo puede ser usado para cargar sprites, audio y fondos personalizados
 */

class AssetLoader {
    constructor(scene) {
        this.scene = scene;
        this.assetsLoaded = false;
    }
    
    /**
     * Precarga todos los assets disponibles
     */
    preload() {
        // Intentar cargar sprites personalizados
        this.loadSprites();
        
        // Intentar cargar fondos personalizados
        this.loadBackgrounds();
        
        // Intentar cargar audio
        this.loadAudio();
    }
    
    /**
     * Carga sprites personalizados si están disponibles
     */
    loadSprites() {
        const sprites = [
            { key: 'player', path: 'assets/images/sprites/player.png' },
            { key: 'monkey_basic', path: 'assets/images/sprites/monkey_basic.png' },
            { key: 'monkey_fast', path: 'assets/images/sprites/monkey_fast.png' },
            { key: 'monkey_strong', path: 'assets/images/sprites/monkey_strong.png' },
            { key: 'projectile', path: 'assets/images/sprites/projectile.png' }
        ];
        
        // Cargar gorilas para diferentes niveles
        for (let i = 1; i <= 4; i++) {
            sprites.push({
                key: `gorilla_${i}`,
                path: `assets/images/sprites/gorilla_${i}.png`
            });
        }
        
        // Cargar sprites con manejo de errores
        sprites.forEach(sprite => {
            this.scene.load.image(sprite.key, sprite.path);
        });
        
        // Manejar errores de carga
        this.scene.load.on('fileerror', (file) => {
            console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
        });
    }
    
    /**
     * Carga fondos personalizados si están disponibles
     */
    loadBackgrounds() {
        const backgrounds = [
            { key: 'menu_bg', path: 'assets/images/backgrounds/menu_bg.png' },
            { key: 'jungle_bg_1', path: 'assets/images/backgrounds/jungle_bg_1.png' },
            { key: 'jungle_bg_2', path: 'assets/images/backgrounds/jungle_bg_2.png' },
            { key: 'jungle_bg_3', path: 'assets/images/backgrounds/jungle_bg_3.png' }
        ];
        
        // Nivel 4 - intentar ambos nombres (por si hay typo)
        this.scene.load.image('jungle_bg_4', 'assets/images/backgrounds/jungle_bg_4.png');
        this.scene.load.image('jungle_bg_4_alt', 'assets/images/backgrounds/jungel_bg_4.png');
        
        // Cargar fondos
        backgrounds.forEach(bg => {
            this.scene.load.image(bg.key, bg.path);
        });
    }
    
    /**
     * Carga archivos de audio si están disponibles
     */
    loadAudio() {
        // Música
        const music = [
            { key: 'menu_music', path: 'assets/audio/music/menu_music.mp3' },
            { key: 'game_music', path: 'assets/audio/music/game_music.mp3' },
            { key: 'victory_music', path: 'assets/audio/music/victory_music.mp3' }
        ];
        
        // Efectos de sonido
        const sfx = [
            { key: 'sfx_jump', path: 'assets/audio/sfx/sfx_jump.wav' },
            { key: 'sfx_throw', path: 'assets/audio/sfx/sfx_throw.wav' },
            { key: 'sfx_hit', path: 'assets/audio/sfx/sfx_hit.wav' },
            { key: 'sfx_enemy_death', path: 'assets/audio/sfx/sfx_enemy_death.wav' },
            { key: 'sfx_player_hurt', path: 'assets/audio/sfx/sfx_player_hurt.wav' },
            { key: 'sfx_game_over', path: 'assets/audio/sfx/sfx_game_over.wav' },
            { key: 'sfx_victory', path: 'assets/audio/sfx/sfx_victory.wav' },
            { key: 'sfx_boss_roar', path: 'assets/audio/sfx/sfx_boss_roar.wav' }
        ];
        
        // Manejar errores de carga de audio silenciosamente
        this.scene.load.on('fileerror', (file) => {
            // Solo mostrar error si no es un archivo de audio (los 404 de audio son esperados)
            if (!file.type || file.type !== 'audio') {
                console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
            }
        });
        
        // Cargar audio (Phaser maneja automáticamente si no existe)
        // Los 404 son normales cuando los archivos no existen
        [...music, ...sfx].forEach(audio => {
            this.scene.load.audio(audio.key, audio.path);
        });
    }
    
    /**
     * Verifica si un asset está disponible
     */
    hasAsset(key) {
        return this.scene.textures.exists(key) || this.scene.cache.audio.exists(key);
    }
    
    /**
     * Obtiene un sprite, usando el personalizado si está disponible, o generando uno programáticamente
     */
    getSprite(key, generatorFunction) {
        if (this.hasAsset(key)) {
            return this.scene.add.image(0, 0, key);
        } else {
            return generatorFunction();
        }
    }
}


```

Buscando dónde se usa `jungle_bg_4` para verificar la compatibilidad:


Buscando dónde se usa `jungle_bg_4` en el código del juego:


Corrigiendo la copia de la textura: `getSourceImage()` puede devolver una imagen HTML, no un canvas. Usando un método más robusto:


```javascript
class PreloadScene extends Phaser.Scene {
    constructor() {
        super({ key: 'PreloadScene' });
    }
    
    preload() {
        // Mostrar barra de carga
        const width = this.cameras.main.width;
        const height = this.cameras.main.height;
        
        const progressBar = this.add.graphics();
        const progressBox = this.add.graphics();
        progressBox.fillStyle(0x222222, 0.8);
        progressBox.fillRect(width / 2 - 200, height / 2 - 30, 400, 50);
        
        const loadingText = this.make.text({
            x: width / 2,
            y: height / 2 - 50,
            text: 'Cargando...',
            style: {
                font: '20px Arial',
                fill: '#ffffff'
            }
        });
        loadingText.setOrigin(0.5, 0.5);
        
        const percentText = this.make.text({
            x: width / 2,
            y: height / 2 - 5,
            text: '0%',
            style: {
                font: '18px Arial',
                fill: '#ffffff'
            }
        });
        percentText.setOrigin(0.5, 0.5);
        
        // Actualizar barra de progreso
        this.load.on('progress', (value) => {
            progressBar.clear();
            progressBar.fillStyle(0xffffff, 1);
            progressBar.fillRect(width / 2 - 190, height / 2 - 20, 380 * value, 30);
            percentText.setText(parseInt(value * 100) + '%');
        });
        
        this.load.on('complete', () => {
            progressBar.destroy();
            progressBox.destroy();
            loadingText.destroy();
            percentText.destroy();
        });
        
        // Cargar assets usando AssetLoader
        const assetLoader = new AssetLoader(this);
        assetLoader.preload();
    }
    
    create() {
        // Generar assets programáticamente si no se cargaron
        this.generateMissingAssets();
        
        // Ir al menú
        this.scene.start('MenuScene');
    }
    
    generateMissingAssets() {
        // Generar sprites si no existen
        if (!this.textures.exists('player')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generatePlayer();
        }
        if (!this.textures.exists('monkey_basic')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generateMonkeyBasic();
        }
        if (!this.textures.exists('monkey_fast')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generateMonkeyFast();
        }
        if (!this.textures.exists('monkey_strong')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generateMonkeyStrong();
        }
        if (!this.textures.exists('projectile')) {
            const spriteGen = new SpriteGenerator(this);
            spriteGen.generateProjectile();
        }
        
        // Generar gorilas si no existen
        for (let i = 1; i <= 4; i++) {
            if (!this.textures.exists(`gorilla_${i}`)) {
                const spriteGen = new SpriteGenerator(this);
                spriteGen.generateGorilla(i);
            }
        }
        
        // Generar fondos si no existen
        if (!this.textures.exists('menu_bg')) {
            const bgGen = new BackgroundGenerator(this);
            bgGen.generateMenuBackground();
        }
        for (let i = 1; i <= 4; i++) {
            // Para nivel 4, verificar también el nombre alternativo con typo
            let bgKey = `jungle_bg_${i}`;
            if (i === 4 && this.textures.exists('jungle_bg_4_alt') && !this.textures.exists('jungle_bg_4')) {
                // Copiar la textura con typo al nombre correcto (Phaser 3 no tiene rename)
                const sourceTexture = this.textures.get('jungle_bg_4_alt');
                const sourceImage = sourceTexture.getSourceImage();
                
                // Crear un canvas temporal para copiar la imagen
                const canvas = document.createElement('canvas');
                canvas.width = sourceImage.width || sourceTexture.width;
                canvas.height = sourceImage.height || sourceTexture.height;
                const ctx = canvas.getContext('2d');
                ctx.drawImage(sourceImage, 0, 0);
                
                // Agregar la textura copiada con el nombre correcto
                this.textures.addCanvas('jungle_bg_4', canvas);
            }
            
            if (!this.textures.exists(bgKey)) {
                const bgGen = new BackgroundGenerator(this);
                bgGen.generateJungleBackground(i);
            }
        }
    }
}


```

Revisando el manejo de errores de audio. El evento `fileerror` podría estar registrado múltiples veces. Mejorándolo:


Centralizando el manejo de errores en un solo lugar para evitar registros duplicados:


```javascript
/**
 * Utilidad para cargar assets externos si están disponibles
 * Este archivo puede ser usado para cargar sprites, audio y fondos personalizados
 */

class AssetLoader {
    constructor(scene) {
        this.scene = scene;
        this.assetsLoaded = false;
    }
    
    /**
     * Precarga todos los assets disponibles
     */
    preload() {
        // Configurar manejo centralizado de errores (solo una vez)
        this.scene.load.once('fileerror', (file) => {
            // Los 404 de audio son esperados cuando los archivos no existen
            // Solo mostrar errores para imágenes y otros assets críticos
            const isAudio = file.type === 'audio' || 
                           file.src.includes('/audio/') ||
                           file.key.startsWith('sfx_') ||
                           file.key.includes('_music');
            
            if (!isAudio) {
                console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
            }
        });
        
        // Intentar cargar sprites personalizados
        this.loadSprites();
        
        // Intentar cargar fondos personalizados
        this.loadBackgrounds();
        
        // Intentar cargar audio
        this.loadAudio();
    }
    
    /**
     * Carga sprites personalizados si están disponibles
     */
    loadSprites() {
        const sprites = [
            { key: 'player', path: 'assets/images/sprites/player.png' },
            { key: 'monkey_basic', path: 'assets/images/sprites/monkey_basic.png' },
            { key: 'monkey_fast', path: 'assets/images/sprites/monkey_fast.png' },
            { key: 'monkey_strong', path: 'assets/images/sprites/monkey_strong.png' },
            { key: 'projectile', path: 'assets/images/sprites/projectile.png' }
        ];
        
        // Cargar gorilas para diferentes niveles
        for (let i = 1; i <= 4; i++) {
            sprites.push({
                key: `gorilla_${i}`,
                path: `assets/images/sprites/gorilla_${i}.png`
            });
        }
        
        // Cargar sprites con manejo de errores
        sprites.forEach(sprite => {
            this.scene.load.image(sprite.key, sprite.path);
        });
        
        // Manejar errores de carga
        this.scene.load.on('fileerror', (file) => {
            console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
        });
    }
    
    /**
     * Carga fondos personalizados si están disponibles
     */
    loadBackgrounds() {
        const backgrounds = [
            { key: 'menu_bg', path: 'assets/images/backgrounds/menu_bg.png' },
            { key: 'jungle_bg_1', path: 'assets/images/backgrounds/jungle_bg_1.png' },
            { key: 'jungle_bg_2', path: 'assets/images/backgrounds/jungle_bg_2.png' },
            { key: 'jungle_bg_3', path: 'assets/images/backgrounds/jungle_bg_3.png' }
        ];
        
        // Nivel 4 - intentar ambos nombres (por si hay typo)
        this.scene.load.image('jungle_bg_4', 'assets/images/backgrounds/jungle_bg_4.png');
        this.scene.load.image('jungle_bg_4_alt', 'assets/images/backgrounds/jungel_bg_4.png');
        
        // Cargar fondos
        backgrounds.forEach(bg => {
            this.scene.load.image(bg.key, bg.path);
        });
    }
    
    /**
     * Carga archivos de audio si están disponibles
     */
    loadAudio() {
        // Música
        const music = [
            { key: 'menu_music', path: 'assets/audio/music/menu_music.mp3' },
            { key: 'game_music', path: 'assets/audio/music/game_music.mp3' },
            { key: 'victory_music', path: 'assets/audio/music/victory_music.mp3' }
        ];
        
        // Efectos de sonido
        const sfx = [
            { key: 'sfx_jump', path: 'assets/audio/sfx/sfx_jump.wav' },
            { key: 'sfx_throw', path: 'assets/audio/sfx/sfx_throw.wav' },
            { key: 'sfx_hit', path: 'assets/audio/sfx/sfx_hit.wav' },
            { key: 'sfx_enemy_death', path: 'assets/audio/sfx/sfx_enemy_death.wav' },
            { key: 'sfx_player_hurt', path: 'assets/audio/sfx/sfx_player_hurt.wav' },
            { key: 'sfx_game_over', path: 'assets/audio/sfx/sfx_game_over.wav' },
            { key: 'sfx_victory', path: 'assets/audio/sfx/sfx_victory.wav' },
            { key: 'sfx_boss_roar', path: 'assets/audio/sfx/sfx_boss_roar.wav' }
        ];
        
        // Manejar errores de carga de audio silenciosamente
        this.scene.load.on('fileerror', (file) => {
            // Solo mostrar error si no es un archivo de audio (los 404 de audio son esperados)
            if (!file.type || file.type !== 'audio') {
                console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
            }
        });
        
        // Cargar audio (Phaser maneja automáticamente si no existe)
        // Los 404 son normales cuando los archivos no existen
        [...music, ...sfx].forEach(audio => {
            this.scene.load.audio(audio.key, audio.path);
        });
    }
    
    /**
     * Verifica si un asset está disponible
     */
    hasAsset(key) {
        return this.scene.textures.exists(key) || this.scene.cache.audio.exists(key);
    }
    
    /**
     * Obtiene un sprite, usando el personalizado si está disponible, o generando uno programáticamente
     */
    getSprite(key, generatorFunction) {
        if (this.hasAsset(key)) {
            return this.scene.add.image(0, 0, key);
        } else {
            return generatorFunction();
        }
    }
}


```

```javascript
/**
 * Utilidad para cargar assets externos si están disponibles
 * Este archivo puede ser usado para cargar sprites, audio y fondos personalizados
 */

class AssetLoader {
    constructor(scene) {
        this.scene = scene;
        this.assetsLoaded = false;
    }
    
    /**
     * Precarga todos los assets disponibles
     */
    preload() {
        // Configurar manejo centralizado de errores (solo una vez)
        this.scene.load.once('fileerror', (file) => {
            // Los 404 de audio son esperados cuando los archivos no existen
            // Solo mostrar errores para imágenes y otros assets críticos
            const isAudio = file.type === 'audio' || 
                           file.src.includes('/audio/') ||
                           file.key.startsWith('sfx_') ||
                           file.key.includes('_music');
            
            if (!isAudio) {
                console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
            }
        });
        
        // Intentar cargar sprites personalizados
        this.loadSprites();
        
        // Intentar cargar fondos personalizados
        this.loadBackgrounds();
        
        // Intentar cargar audio
        this.loadAudio();
    }
    
    /**
     * Carga sprites personalizados si están disponibles
     */
    loadSprites() {
        const sprites = [
            { key: 'player', path: 'assets/images/sprites/player.png' },
            { key: 'monkey_basic', path: 'assets/images/sprites/monkey_basic.png' },
            { key: 'monkey_fast', path: 'assets/images/sprites/monkey_fast.png' },
            { key: 'monkey_strong', path: 'assets/images/sprites/monkey_strong.png' },
            { key: 'projectile', path: 'assets/images/sprites/projectile.png' }
        ];
        
        // Cargar gorilas para diferentes niveles
        for (let i = 1; i <= 4; i++) {
            sprites.push({
                key: `gorilla_${i}`,
                path: `assets/images/sprites/gorilla_${i}.png`
            });
        }
        
        // Cargar sprites (el manejo de errores está en preload())
        sprites.forEach(sprite => {
            this.scene.load.image(sprite.key, sprite.path);
        });
    }
    
    /**
     * Carga fondos personalizados si están disponibles
     */
    loadBackgrounds() {
        const backgrounds = [
            { key: 'menu_bg', path: 'assets/images/backgrounds/menu_bg.png' },
            { key: 'jungle_bg_1', path: 'assets/images/backgrounds/jungle_bg_1.png' },
            { key: 'jungle_bg_2', path: 'assets/images/backgrounds/jungle_bg_2.png' },
            { key: 'jungle_bg_3', path: 'assets/images/backgrounds/jungle_bg_3.png' }
        ];
        
        // Nivel 4 - intentar ambos nombres (por si hay typo)
        this.scene.load.image('jungle_bg_4', 'assets/images/backgrounds/jungle_bg_4.png');
        this.scene.load.image('jungle_bg_4_alt', 'assets/images/backgrounds/jungel_bg_4.png');
        
        // Cargar fondos
        backgrounds.forEach(bg => {
            this.scene.load.image(bg.key, bg.path);
        });
    }
    
    /**
     * Carga archivos de audio si están disponibles
     */
    loadAudio() {
        // Música
        const music = [
            { key: 'menu_music', path: 'assets/audio/music/menu_music.mp3' },
            { key: 'game_music', path: 'assets/audio/music/game_music.mp3' },
            { key: 'victory_music', path: 'assets/audio/music/victory_music.mp3' }
        ];
        
        // Efectos de sonido
        const sfx = [
            { key: 'sfx_jump', path: 'assets/audio/sfx/sfx_jump.wav' },
            { key: 'sfx_throw', path: 'assets/audio/sfx/sfx_throw.wav' },
            { key: 'sfx_hit', path: 'assets/audio/sfx/sfx_hit.wav' },
            { key: 'sfx_enemy_death', path: 'assets/audio/sfx/sfx_enemy_death.wav' },
            { key: 'sfx_player_hurt', path: 'assets/audio/sfx/sfx_player_hurt.wav' },
            { key: 'sfx_game_over', path: 'assets/audio/sfx/sfx_game_over.wav' },
            { key: 'sfx_victory', path: 'assets/audio/sfx/sfx_victory.wav' },
            { key: 'sfx_boss_roar', path: 'assets/audio/sfx/sfx_boss_roar.wav' }
        ];
        
        // Manejar errores de carga de audio silenciosamente
        this.scene.load.on('fileerror', (file) => {
            // Solo mostrar error si no es un archivo de audio (los 404 de audio son esperados)
            if (!file.type || file.type !== 'audio') {
                console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
            }
        });
        
        // Cargar audio (Phaser maneja automáticamente si no existe)
        // Los 404 son normales cuando los archivos no existen
        [...music, ...sfx].forEach(audio => {
            this.scene.load.audio(audio.key, audio.path);
        });
    }
    
    /**
     * Verifica si un asset está disponible
     */
    hasAsset(key) {
        return this.scene.textures.exists(key) || this.scene.cache.audio.exists(key);
    }
    
    /**
     * Obtiene un sprite, usando el personalizado si está disponible, o generando uno programáticamente
     */
    getSprite(key, generatorFunction) {
        if (this.hasAsset(key)) {
            return this.scene.add.image(0, 0, key);
        } else {
            return generatorFunction();
        }
    }
}


```

```javascript
/**
 * Utilidad para cargar assets externos si están disponibles
 * Este archivo puede ser usado para cargar sprites, audio y fondos personalizados
 */

class AssetLoader {
    constructor(scene) {
        this.scene = scene;
        this.assetsLoaded = false;
    }
    
    /**
     * Precarga todos los assets disponibles
     */
    preload() {
        // Configurar manejo centralizado de errores (solo una vez)
        this.scene.load.once('fileerror', (file) => {
            // Los 404 de audio son esperados cuando los archivos no existen
            // Solo mostrar errores para imágenes y otros assets críticos
            const isAudio = file.type === 'audio' || 
                           file.src.includes('/audio/') ||
                           file.key.startsWith('sfx_') ||
                           file.key.includes('_music');
            
            if (!isAudio) {
                console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
            }
        });
        
        // Intentar cargar sprites personalizados
        this.loadSprites();
        
        // Intentar cargar fondos personalizados
        this.loadBackgrounds();
        
        // Intentar cargar audio
        this.loadAudio();
    }
    
    /**
     * Carga sprites personalizados si están disponibles
     */
    loadSprites() {
        const sprites = [
            { key: 'player', path: 'assets/images/sprites/player.png' },
            { key: 'monkey_basic', path: 'assets/images/sprites/monkey_basic.png' },
            { key: 'monkey_fast', path: 'assets/images/sprites/monkey_fast.png' },
            { key: 'monkey_strong', path: 'assets/images/sprites/monkey_strong.png' },
            { key: 'projectile', path: 'assets/images/sprites/projectile.png' }
        ];
        
        // Cargar gorilas para diferentes niveles
        for (let i = 1; i <= 4; i++) {
            sprites.push({
                key: `gorilla_${i}`,
                path: `assets/images/sprites/gorilla_${i}.png`
            });
        }
        
        // Cargar sprites (el manejo de errores está en preload())
        sprites.forEach(sprite => {
            this.scene.load.image(sprite.key, sprite.path);
        });
    }
    
    /**
     * Carga fondos personalizados si están disponibles
     */
    loadBackgrounds() {
        const backgrounds = [
            { key: 'menu_bg', path: 'assets/images/backgrounds/menu_bg.png' },
            { key: 'jungle_bg_1', path: 'assets/images/backgrounds/jungle_bg_1.png' },
            { key: 'jungle_bg_2', path: 'assets/images/backgrounds/jungle_bg_2.png' },
            { key: 'jungle_bg_3', path: 'assets/images/backgrounds/jungle_bg_3.png' }
        ];
        
        // Nivel 4 - intentar ambos nombres (por si hay typo)
        this.scene.load.image('jungle_bg_4', 'assets/images/backgrounds/jungle_bg_4.png');
        this.scene.load.image('jungle_bg_4_alt', 'assets/images/backgrounds/jungel_bg_4.png');
        
        // Cargar fondos
        backgrounds.forEach(bg => {
            this.scene.load.image(bg.key, bg.path);
        });
    }
    
    /**
     * Carga archivos de audio si están disponibles
     */
    loadAudio() {
        // Música
        const music = [
            { key: 'menu_music', path: 'assets/audio/music/menu_music.mp3' },
            { key: 'game_music', path: 'assets/audio/music/game_music.mp3' },
            { key: 'victory_music', path: 'assets/audio/music/victory_music.mp3' }
        ];
        
        // Efectos de sonido
        const sfx = [
            { key: 'sfx_jump', path: 'assets/audio/sfx/sfx_jump.wav' },
            { key: 'sfx_throw', path: 'assets/audio/sfx/sfx_throw.wav' },
            { key: 'sfx_hit', path: 'assets/audio/sfx/sfx_hit.wav' },
            { key: 'sfx_enemy_death', path: 'assets/audio/sfx/sfx_enemy_death.wav' },
            { key: 'sfx_player_hurt', path: 'assets/audio/sfx/sfx_player_hurt.wav' },
            { key: 'sfx_game_over', path: 'assets/audio/sfx/sfx_game_over.wav' },
            { key: 'sfx_victory', path: 'assets/audio/sfx/sfx_victory.wav' },
            { key: 'sfx_boss_roar', path: 'assets/audio/sfx/sfx_boss_roar.wav' }
        ];
        
        // Cargar audio (Phaser maneja automáticamente si no existe)
        // Los 404 son normales cuando los archivos no existen
        // El manejo de errores está centralizado en preload()
        [...music, ...sfx].forEach(audio => {
            this.scene.load.audio(audio.key, audio.path);
        });
    }
    
    /**
     * Verifica si un asset está disponible
     */
    hasAsset(key) {
        return this.scene.textures.exists(key) || this.scene.cache.audio.exists(key);
    }
    
    /**
     * Obtiene un sprite, usando el personalizado si está disponible, o generando uno programáticamente
     */
    getSprite(key, generatorFunction) {
        if (this.hasAsset(key)) {
            return this.scene.add.image(0, 0, key);
        } else {
            return generatorFunction();
        }
    }
}


```

Corrigiendo el manejo de errores: `once()` solo maneja el primer error. Cambiando a `on()` para manejar todos los errores:


```javascript
/**
 * Utilidad para cargar assets externos si están disponibles
 * Este archivo puede ser usado para cargar sprites, audio y fondos personalizados
 */

class AssetLoader {
    constructor(scene) {
        this.scene = scene;
        this.assetsLoaded = false;
    }
    
    /**
     * Precarga todos los assets disponibles
     */
    preload() {
        // Configurar manejo centralizado de errores
        // Verificar si ya está registrado para evitar duplicados
        if (!this._errorHandlerRegistered) {
            this.scene.load.on('fileerror', (file) => {
                // Los 404 de audio son esperados cuando los archivos no existen
                // Solo mostrar errores para imágenes y otros assets críticos
                const isAudio = file.type === 'audio' || 
                               file.src.includes('/audio/') ||
                               file.key.startsWith('sfx_') ||
                               file.key.includes('_music');
                
                if (!isAudio) {
                    console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
                }
            });
            this._errorHandlerRegistered = true;
        }
        
        // Intentar cargar sprites personalizados
        this.loadSprites();
        
        // Intentar cargar fondos personalizados
        this.loadBackgrounds();
        
        // Intentar cargar audio
        this.loadAudio();
    }
    
    /**
     * Carga sprites personalizados si están disponibles
     */
    loadSprites() {
        const sprites = [
            { key: 'player', path: 'assets/images/sprites/player.png' },
            { key: 'monkey_basic', path: 'assets/images/sprites/monkey_basic.png' },
            { key: 'monkey_fast', path: 'assets/images/sprites/monkey_fast.png' },
            { key: 'monkey_strong', path: 'assets/images/sprites/monkey_strong.png' },
            { key: 'projectile', path: 'assets/images/sprites/projectile.png' }
        ];
        
        // Cargar gorilas para diferentes niveles
        for (let i = 1; i <= 4; i++) {
            sprites.push({
                key: `gorilla_${i}`,
                path: `assets/images/sprites/gorilla_${i}.png`
            });
        }
        
        // Cargar sprites (el manejo de errores está en preload())
        sprites.forEach(sprite => {
            this.scene.load.image(sprite.key, sprite.path);
        });
    }
    
    /**
     * Carga fondos personalizados si están disponibles
     */
    loadBackgrounds() {
        const backgrounds = [
            { key: 'menu_bg', path: 'assets/images/backgrounds/menu_bg.png' },
            { key: 'jungle_bg_1', path: 'assets/images/backgrounds/jungle_bg_1.png' },
            { key: 'jungle_bg_2', path: 'assets/images/backgrounds/jungle_bg_2.png' },
            { key: 'jungle_bg_3', path: 'assets/images/backgrounds/jungle_bg_3.png' }
        ];
        
        // Nivel 4 - intentar ambos nombres (por si hay typo)
        this.scene.load.image('jungle_bg_4', 'assets/images/backgrounds/jungle_bg_4.png');
        this.scene.load.image('jungle_bg_4_alt', 'assets/images/backgrounds/jungel_bg_4.png');
        
        // Cargar fondos
        backgrounds.forEach(bg => {
            this.scene.load.image(bg.key, bg.path);
        });
    }
    
    /**
     * Carga archivos de audio si están disponibles
     */
    loadAudio() {
        // Música
        const music = [
            { key: 'menu_music', path: 'assets/audio/music/menu_music.mp3' },
            { key: 'game_music', path: 'assets/audio/music/game_music.mp3' },
            { key: 'victory_music', path: 'assets/audio/music/victory_music.mp3' }
        ];
        
        // Efectos de sonido
        const sfx = [
            { key: 'sfx_jump', path: 'assets/audio/sfx/sfx_jump.wav' },
            { key: 'sfx_throw', path: 'assets/audio/sfx/sfx_throw.wav' },
            { key: 'sfx_hit', path: 'assets/audio/sfx/sfx_hit.wav' },
            { key: 'sfx_enemy_death', path: 'assets/audio/sfx/sfx_enemy_death.wav' },
            { key: 'sfx_player_hurt', path: 'assets/audio/sfx/sfx_player_hurt.wav' },
            { key: 'sfx_game_over', path: 'assets/audio/sfx/sfx_game_over.wav' },
            { key: 'sfx_victory', path: 'assets/audio/sfx/sfx_victory.wav' },
            { key: 'sfx_boss_roar', path: 'assets/audio/sfx/sfx_boss_roar.wav' }
        ];
        
        // Cargar audio (Phaser maneja automáticamente si no existe)
        // Los 404 son normales cuando los archivos no existen
        // El manejo de errores está centralizado en preload()
        [...music, ...sfx].forEach(audio => {
            this.scene.load.audio(audio.key, audio.path);
        });
    }
    
    /**
     * Verifica si un asset está disponible
     */
    hasAsset(key) {
        return this.scene.textures.exists(key) || this.scene.cache.audio.exists(key);
    }
    
    /**
     * Obtiene un sprite, usando el personalizado si está disponible, o generando uno programáticamente
     */
    getSprite(key, generatorFunction) {
        if (this.hasAsset(key)) {
            return this.scene.add.image(0, 0, key);
        } else {
            return generatorFunction();
        }
    }
}


```

## Resumen de correcciones

### 1. Error `this.textures.rename is not a function`
- Problema: Phaser 3 no tiene `this.textures.rename()`.
- Solución: Se copia la textura usando un canvas temporal. Si existe `jungle_bg_4_alt` (por el typo en el nombre del archivo), se copia a `jungle_bg_4` para mantener la compatibilidad.

```105:119:ghosts-goblins-SGN/js/scenes/PreloadScene.js
            if (i === 4 && this.textures.exists('jungle_bg_4_alt') && !this.textures.exists('jungle_bg_4')) {
                // Copiar la textura con typo al nombre correcto (Phaser 3 no tiene rename)
                const sourceTexture = this.textures.get('jungle_bg_4_alt');
                const sourceImage = sourceTexture.getSourceImage();
                
                // Crear un canvas temporal para copiar la imagen
                const canvas = document.createElement('canvas');
                canvas.width = sourceImage.width || sourceTexture.width;
                canvas.height = sourceImage.height || sourceTexture.height;
                const ctx = canvas.getContext('2d');
                ctx.drawImage(sourceImage, 0, 0);
                
                // Agregar la textura copiada con el nombre correcto
                this.textures.addCanvas('jungle_bg_4', canvas);
            }
```

### 2. Errores 404 de audio
- Problema: Los archivos de audio no existen, generando muchos 404 en la consola.
- Solución: Manejo centralizado de errores que silencia los 404 de audio (esperados cuando los archivos no existen) y solo muestra errores de imágenes y otros assets críticos.

```16:32:ghosts-goblins-SGN/assets/js/asset-loader.js
        // Configurar manejo centralizado de errores
        // Verificar si ya está registrado para evitar duplicados
        if (!this._errorHandlerRegistered) {
            this.scene.load.on('fileerror', (file) => {
                // Los 404 de audio son esperados cuando los archivos no existen
                // Solo mostrar errores para imágenes y otros assets críticos
                const isAudio = file.type === 'audio' || 
                               file.src.includes('/audio/') ||
                               file.key.startsWith('sfx_') ||
                               file.key.includes('_music');
                
                if (!isAudio) {
                    console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
                }
            });
            this._errorHandlerRegistered = true;
        }
```

### Nota sobre los 404
Los 404 seguirán apareciendo en la pestaña Network de las herramientas de desarrollo del navegador (comportamiento normal). El código ya no muestra mensajes adicionales en la consola de JavaScript para archivos de audio faltantes.

El juego debería funcionar correctamente. Los assets se generan programáticamente si no existen, y el error de `rename()` está resuelto.

```javascript
            if (i === 4 && this.textures.exists('jungle_bg_4_alt') && !this.textures.exists('jungle_bg_4')) {
                // Copiar la textura con typo al nombre correcto (Phaser 3 no tiene rename)
                const sourceTexture = this.textures.get('jungle_bg_4_alt');
                const sourceImage = sourceTexture.getSourceImage();
                
                // Crear un canvas temporal para copiar la imagen
                const canvas = document.createElement('canvas');
                canvas.width = sourceImage.width || sourceTexture.width;
                canvas.height = sourceImage.height || sourceTexture.height;
                const ctx = canvas.getContext('2d');
                ctx.drawImage(sourceImage, 0, 0);
                
                // Agregar la textura copiada con el nombre correcto
                this.textures.addCanvas('jungle_bg_4', canvas);
            }
```

```javascript
        // Configurar manejo centralizado de errores
        // Verificar si ya está registrado para evitar duplicados
        if (!this._errorHandlerRegistered) {
            this.scene.load.on('fileerror', (file) => {
                // Los 404 de audio son esperados cuando los archivos no existen
                // Solo mostrar errores para imágenes y otros assets críticos
                const isAudio = file.type === 'audio' || 
                               file.src.includes('/audio/') ||
                               file.key.startsWith('sfx_') ||
                               file.key.includes('_music');
                
                if (!isAudio) {
                    console.log(`No se pudo cargar: ${file.key} desde ${file.src}`);
                }
            });
            this._errorHandlerRegistered = true;
        }
```

---

**User**

Monkey.js:44 Uncaught ReferenceError: size is not defined

    at new Monkey (Monkey.js:44:27)

    at GameScene.createEnemies (GameScene.js:119:28)

    at GameScene.create (GameScene.js:41:14)

    at initialize.create (phaser.min.js:1:954364)

    at initialize.bootScene (phaser.min.js:1:953634)

    at initialize.start (phaser.min.js:1:957497)

    at initialize.processQueue (phaser.min.js:1:952320)

    at initialize.update (phaser.min.js:1:953849)

    at initialize.step (phaser.min.js:1:76119)

    at initialize.step (phaser.min.js:1:80333)

---

**Cursor**

Revisando `Monkey.js` para identificar el error:


La variable `size` se define dentro del `else` (línea 11) y se usa fuera (línea 44). Si el sprite existe, `size` no se define. Moviendo la definición de `size` fuera del bloque para que esté disponible en ambos casos:


```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        this.body.setCollideWorldBounds(true);
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

Corregido el error. La variable `size` se definía dentro del bloque `else` (solo cuando se generaba el sprite programáticamente) y se usaba fuera en la línea 44 (configuración de física), por lo que no estaba disponible cuando el sprite ya existía.

## Corrección aplicada

Moví la definición de `size` antes del bloque `if/else` para que esté disponible en ambos casos:

```6:7:ghosts-goblins-SGN/js/entities/Monkey.js
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
```

Ahora `size` está definida antes de usarse en la configuración de física (línea 46), tanto si el sprite existe como si se genera programáticamente. El error debería estar resuelto.

```javascript
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
```

---

**User**

Necesito que cambiemos los siguientes aspectos del juego:

- Cuando llego al final del ancho del tamaño original del fondo del escenario, el protagonista no avanza, debería seguir avanzando hasta encontrar al jefe del nivel

- Los proyectiles salen y solo bajan hacia abajo, deberían ser lanzados y tener física de avance y gravedad.

- Los monos cuando caen a la plataforma inferior mueren, cuando no debería ser así.

- El botón espacio salta y lanza proyectiles, cuando solo debería lanzar proyectiles

---

**Cursor**

Revisando los archivos relevantes para entender la implementación actual:


Revisando el código de Monkey para entender por qué mueren al caer:


Creando la lista de tareas e implementando los cambios:


Aplicando los cambios:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        this.boss = new Gorilla(this, GAME_CONFIG.WIDTH * 2.5, GAME_CONFIG.HEIGHT - 200, this.level);
        this.gorillas.add(this.boss);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 200);
        
        // Cámara
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player);
        this.cameras.main.setDeadzone(100, 100);
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Permitir movimiento horizontal más allá del ancho original, pero mantener límites verticales
        this.body.setCollideWorldBounds(true);
        this.body.setCollideWorldBounds(true, true, false, true); // Solo colisionar arriba y abajo
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto
        if ((cursors.up.isDown || cursors.w.isDown || cursors.space.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Permitir movimiento horizontal más allá del ancho original, pero mantener límites verticales
        this.body.setCollideWorldBounds(true);
        this.body.setCollideWorldBounds(true, true, false, true); // Solo colisionar arriba y abajo
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Projectile extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, direction) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('projectile')) {
            super(scene, x, y, 'projectile');
        } else {
            // Crear sprite simple programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PROJECTILE);
            graphics.fillCircle(0, 0, 8);
            const texture = graphics.generateTexture('projectile', 16, 16);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.direction = direction; // 1 para derecha, -1 para izquierda
        this.speed = GAME_CONFIG.PROJECTILE_SPEED;
        
        // Configurar física
        this.body.setSize(12, 12);
        this.body.setCollideWorldBounds(false); // No colisionar con bordes, destruir manualmente
        this.body.setGravityY(GAME_CONFIG.GRAVITY); // Agregar gravedad para arco parabólico
        
        // Velocidad inicial con componente horizontal y vertical (arco parabólico)
        const horizontalSpeed = this.speed * this.direction;
        const verticalSpeed = -200; // Velocidad vertical inicial hacia arriba
        this.setVelocity(horizontalSpeed, verticalSpeed);
        
        // Guardar referencia a la escena
        this.scene = scene;
    }
    
    update() {
        // El proyectil se mueve automáticamente
        // Se destruye al colisionar con plataformas o enemigos
        
        // Destruir si sale de los límites del mundo
        if (this.x < -50 || this.x > GAME_CONFIG.WIDTH * 3 + 50 || 
            this.y < -50 || this.y > GAME_CONFIG.HEIGHT + 50) {
            this.destroy();
        }
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Permitir movimiento horizontal libre, pero mantener límites verticales amplios
        this.body.setCollideWorldBounds(true, true, false, true); // Solo colisionar arriba y abajo
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes horizontales (si existen)
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

Corrigiendo el uso de `setCollideWorldBounds`; la API de Phaser 3 no acepta múltiples parámetros booleanos. Ajustando también el Gorilla:


```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Permitir movimiento horizontal más allá del ancho original, pero mantener límites verticales
        // setCollideWorldBounds con objeto para controlar qué bordes colisionan
        this.body.setCollideWorldBounds(true, true, false, true); // Solo colisionar arriba y abajo
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Permitir movimiento horizontal libre, pero mantener límites verticales amplios
        // Solo colisionar arriba y abajo, no en los lados (para que no mueran al caer)
        this.body.setCollideWorldBounds(true, true, false, true);
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes horizontales (si existen)
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Permitir movimiento horizontal libre, pero mantener límites verticales
        this.body.setCollideWorldBounds(true, true, false, true);
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Movimiento hacia el jugador
        if (player && player.isAlive) {
            if (player.x > this.x + 50) {
                this.direction = 1;
            } else if (player.x < this.x - 50) {
                this.direction = -1;
            }
        }
        
        // Aplicar velocidad con patrón más complejo
        const baseSpeed = this.speed * this.direction;
        // Movimiento con patrón sinusoidal para hacerlo más impredecible
        const patternOffset = Math.sin(time * 0.001) * 20;
        this.body.setVelocityX(baseSpeed + patternOffset);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // El gorila puede lanzar proyectiles o cargar
        // Por simplicidad, aquí solo cambia de dirección rápidamente
        // En una versión más avanzada, podría lanzar proyectiles
        this.direction *= -1;
        this.body.setVelocityX(this.speed * this.direction * 1.5); // Carga rápida
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Verificando la API de Phaser 3 para `setCollideWorldBounds`:


Corrigiendo el uso de la API de Phaser 3. `setCollideWorldBounds` solo acepta un booleano. Ajustando el código:


```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Permitir movimiento horizontal más allá del ancho original
        // Desactivar colisión con bordes horizontales, mantener verticales
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true;
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Permitir movimiento horizontal libre, pero mantener límites verticales amplios
        // Desactivar colisión con bordes horizontales para que no mueran al caer
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true;
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes horizontales (si existen)
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Permitir movimiento horizontal libre, pero mantener límites verticales
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true;
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Movimiento hacia el jugador
        if (player && player.isAlive) {
            if (player.x > this.x + 50) {
                this.direction = 1;
            } else if (player.x < this.x - 50) {
                this.direction = -1;
            }
        }
        
        // Aplicar velocidad con patrón más complejo
        const baseSpeed = this.speed * this.direction;
        // Movimiento con patrón sinusoidal para hacerlo más impredecible
        const patternOffset = Math.sin(time * 0.001) * 20;
        this.body.setVelocityX(baseSpeed + patternOffset);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // El gorila puede lanzar proyectiles o cargar
        // Por simplicidad, aquí solo cambia de dirección rápidamente
        // En una versión más avanzada, podría lanzar proyectiles
        this.direction *= -1;
        this.body.setVelocityX(this.speed * this.direction * 1.5); // Carga rápida
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

## Cambios implementados

### 1. El protagonista puede avanzar más allá del ancho original del fondo
- Configurados los límites del mundo físico a `GAME_CONFIG.WIDTH * 3` (línea 62 de `GameScene.js`)
- Desactivadas las colisiones con los bordes horizontales del mundo para el jugador
- El jugador puede moverse horizontalmente hasta encontrar al jefe

### 2. Los proyectiles tienen física de arco parabólico
- Agregada gravedad a los proyectiles (`setGravityY`)
- Velocidad inicial con componente vertical (`-200`) para crear un arco parabólico
- Los proyectiles ahora se lanzan con trayectoria curva en lugar de solo caer

### 3. Los monos no mueren al caer a la plataforma inferior
- Desactivadas las colisiones con los bordes horizontales del mundo para los monos
- Ajustados los límites del mundo para permitir más espacio vertical (`HEIGHT + 200`)
- Los monos pueden caer y seguir vivos, colisionando solo con las plataformas

### 4. El botón espacio solo lanza proyectiles (no salta)
- Removido `cursors.space.isDown` de la condición de salto en `Player.js`
- Ahora solo `W` o `↑` saltan
- `Espacio` y `J` solo lanzan proyectiles

Cambios aplicados. El juego debería comportarse según lo solicitado.

---

**User**

- El limite inferior del suelo, debe ser transitable, ahora si llego abajo, o los monos llegan abajo desaparecen. Restablece el espacio vertical del mundo, pero no hagas que al caer al límite inferior mueran los personajes

- El proyectil tiene que moverse también en el eje de las X debería tener las mismas reglas físicas que el protagonista.



---

**Cursor**

Revisando el código para aplicar los cambios:


Aplicando los cambios:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        this.boss = new Gorilla(this, GAME_CONFIG.WIDTH * 2.5, GAME_CONFIG.HEIGHT - 200, this.level);
        this.gorillas.add(this.boss);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Restablecer espacio vertical normal, pero permitir pasar a través del límite inferior
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        
        // Cámara
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player);
        this.cameras.main.setDeadzone(100, 100);
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Permitir movimiento horizontal más allá del ancho original
        // Desactivar colisión con bordes horizontales y límite inferior (transitable)
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = false; // Límite inferior transitable
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Permitir movimiento horizontal libre y límite inferior transitable
        // Desactivar colisión con bordes horizontales y límite inferior
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = false; // Límite inferior transitable
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes horizontales (si existen)
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Permitir movimiento horizontal libre y límite inferior transitable
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = false; // Límite inferior transitable
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Movimiento hacia el jugador
        if (player && player.isAlive) {
            if (player.x > this.x + 50) {
                this.direction = 1;
            } else if (player.x < this.x - 50) {
                this.direction = -1;
            }
        }
        
        // Aplicar velocidad con patrón más complejo
        const baseSpeed = this.speed * this.direction;
        // Movimiento con patrón sinusoidal para hacerlo más impredecible
        const patternOffset = Math.sin(time * 0.001) * 20;
        this.body.setVelocityX(baseSpeed + patternOffset);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // El gorila puede lanzar proyectiles o cargar
        // Por simplicidad, aquí solo cambia de dirección rápidamente
        // En una versión más avanzada, podría lanzar proyectiles
        this.direction *= -1;
        this.body.setVelocityX(this.speed * this.direction * 1.5); // Carga rápida
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Projectile extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, direction) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('projectile')) {
            super(scene, x, y, 'projectile');
        } else {
            // Crear sprite simple programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PROJECTILE);
            graphics.fillCircle(0, 0, 8);
            const texture = graphics.generateTexture('projectile', 16, 16);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.direction = direction; // 1 para derecha, -1 para izquierda
        this.speed = GAME_CONFIG.PROJECTILE_SPEED;
        
        // Configurar física
        this.body.setSize(12, 12);
        this.body.setCollideWorldBounds(false); // No colisionar con bordes, destruir manualmente
        this.body.setGravityY(GAME_CONFIG.GRAVITY); // Misma gravedad que el protagonista
        
        // Velocidad inicial con componente horizontal y vertical (arco parabólico)
        // Mantener velocidad horizontal constante como el protagonista
        this.horizontalSpeed = this.speed * this.direction;
        const verticalSpeed = -200; // Velocidad vertical inicial hacia arriba
        this.setVelocity(this.horizontalSpeed, verticalSpeed);
        
        // Guardar referencia a la escena
        this.scene = scene;
    }
    
    update() {
        // El proyectil se mueve automáticamente
        // Se destruye al colisionar con plataformas o enemigos
        
        // Destruir si sale de los límites del mundo
        if (this.x < -50 || this.x > GAME_CONFIG.WIDTH * 3 + 50 || 
            this.y < -50 || this.y > GAME_CONFIG.HEIGHT + 50) {
            this.destroy();
        }
    }
}


```

```javascript
class Projectile extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, direction) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('projectile')) {
            super(scene, x, y, 'projectile');
        } else {
            // Crear sprite simple programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PROJECTILE);
            graphics.fillCircle(0, 0, 8);
            const texture = graphics.generateTexture('projectile', 16, 16);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        this.direction = direction; // 1 para derecha, -1 para izquierda
        this.speed = GAME_CONFIG.PROJECTILE_SPEED;
        
        // Configurar física
        this.body.setSize(12, 12);
        this.body.setCollideWorldBounds(false); // No colisionar con bordes, destruir manualmente
        this.body.setGravityY(GAME_CONFIG.GRAVITY); // Misma gravedad que el protagonista
        
        // Velocidad inicial con componente horizontal y vertical (arco parabólico)
        // Mantener velocidad horizontal constante como el protagonista
        this.horizontalSpeed = this.speed * this.direction;
        const verticalSpeed = -200; // Velocidad vertical inicial hacia arriba
        this.setVelocity(this.horizontalSpeed, verticalSpeed);
        
        // Guardar referencia a la escena
        this.scene = scene;
    }
    
    update() {
        // Mantener velocidad horizontal constante (mismas reglas físicas que el protagonista)
        // La gravedad ya está aplicada automáticamente
        this.body.setVelocityX(this.horizontalSpeed);
        
        // Destruir si sale de los límites del mundo
        if (this.x < -50 || this.x > GAME_CONFIG.WIDTH * 3 + 50 || 
            this.y < -50 || this.y > GAME_CONFIG.HEIGHT + 50) {
            this.destroy();
        }
    }
}


```

## Cambios implementados

### 1. Límite inferior transitable
- Restablecido el espacio vertical del mundo a `GAME_CONFIG.HEIGHT` (sin el `+ 200` extra)
- Desactivada la colisión con el límite inferior (`checkCollision.down = false`) para:
  - Jugador (`Player.js`)
  - Monos (`Monkey.js`)
  - Gorilas (`Gorilla.js`)
- Los personajes pueden pasar por el límite inferior sin desaparecer

### 2. Proyectil con movimiento horizontal constante
- Agregada la propiedad `horizontalSpeed` para mantener la velocidad horizontal constante
- En `update()`, se mantiene la velocidad X constante con `setVelocityX(this.horizontalSpeed)`
- El proyectil mantiene su velocidad horizontal mientras la gravedad afecta la vertical, igual que el protagonista

Los cambios están aplicados. El límite inferior es transitable y los proyectiles mantienen su velocidad horizontal constante mientras siguen la gravedad.

---

**User**

los personajes desaparecen por el límite inferior

---

**Cursor**

Revisando el código para evitar que los personajes desaparezcan al pasar el límite inferior. Probablemente Phaser los destruye al salir de los límites del mundo. Ajustando la configuración:


Ampliando los límites del mundo verticalmente para evitar que Phaser destruya los objetos al salir. Ajustando la configuración:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        this.boss = new Gorilla(this, GAME_CONFIG.WIDTH * 2.5, GAME_CONFIG.HEIGHT - 200, this.level);
        this.gorillas.add(this.boss);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior para que los personajes no desaparezcan al caer
        // El límite inferior es transitable, pero necesitamos espacio extra para que no se destruyan
        this.physics.world.setBounds(0, -100, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 500);
        
        // Cámara
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player);
        this.cameras.main.setDeadzone(100, 100);
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Verificando si hay lógica que destruya objetos fuera de los límites. Asegurando que los personajes no se destruyan automáticamente:


Asegurando que los objetos no se desactiven al salir de los límites. Ajustando la configuración del mundo físico:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        this.boss = new Gorilla(this, GAME_CONFIG.WIDTH * 2.5, GAME_CONFIG.HEIGHT - 200, this.level);
        this.gorillas.add(this.boss);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // El límite inferior es transitable, pero necesitamos espacio extra para que no se destruyan
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 1000);
        
        // Desactivar la desactivación automática de objetos fuera de los límites
        this.physics.world.disableBody(this.player.body, false);
        
        // Cámara
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player);
        this.cameras.main.setDeadzone(100, 100);
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Corrigiendo el enfoque: ampliando los límites y asegurando que los objetos permanezcan activos:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        this.boss = new Gorilla(this, GAME_CONFIG.WIDTH * 2.5, GAME_CONFIG.HEIGHT - 200, this.level);
        this.gorillas.add(this.boss);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player);
        this.cameras.main.setDeadzone(100, 100);
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el jugador no se desactive al salir de los límites
        this.setActive(true);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Permitir movimiento horizontal más allá del ancho original
        // Desactivar colisión con bordes horizontales y límite inferior (transitable)
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = false; // Límite inferior transitable
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el mono no se desactive al salir de los límites
        this.setActive(true);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Permitir movimiento horizontal libre y límite inferior transitable
        // Desactivar colisión con bordes horizontales y límite inferior
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = false; // Límite inferior transitable
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes horizontales (si existen)
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Permitir movimiento horizontal libre y límite inferior transitable
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = false; // Límite inferior transitable
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Movimiento hacia el jugador
        if (player && player.isAlive) {
            if (player.x > this.x + 50) {
                this.direction = 1;
            } else if (player.x < this.x - 50) {
                this.direction = -1;
            }
        }
        
        // Aplicar velocidad con patrón más complejo
        const baseSpeed = this.speed * this.direction;
        // Movimiento con patrón sinusoidal para hacerlo más impredecible
        const patternOffset = Math.sin(time * 0.001) * 20;
        this.body.setVelocityX(baseSpeed + patternOffset);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // El gorila puede lanzar proyectiles o cargar
        // Por simplicidad, aquí solo cambia de dirección rápidamente
        // En una versión más avanzada, podría lanzar proyectiles
        this.direction *= -1;
        this.body.setVelocityX(this.speed * this.direction * 1.5); // Carga rápida
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

## Cambios implementados

### 1. Límites del mundo ampliados verticalmente
- Ampliado el límite inferior del mundo físico a `GAME_CONFIG.HEIGHT + 2000` píxeles
- Esto da espacio suficiente para que los personajes caigan sin salir de los límites del mundo

### 2. Objetos configurados para permanecer activos
- Agregado `setActive(true)` en:
  - **Player** (`Player.js`)
  - **Monkey** (`Monkey.js`)
  - **Gorilla** (`Gorilla.js`)
- Esto evita que Phaser los desactive automáticamente al salir de los límites visibles

### 3. Límite inferior transitable
- La colisión con el límite inferior sigue desactivada (`checkCollision.down = false`)
- Los personajes pueden pasar a través del límite inferior sin desaparecer

Los personajes ya no deberían desaparecer al caer por el límite inferior. El mundo físico tiene espacio suficiente abajo y los objetos permanecen activos incluso fuera de los límites visibles de la cámara.

---

**User**

sigue pasando lo mismo, desaparecen por abajo, activa la colisión abajo, y probemos si con esto se soluciona

---

**Cursor**

Activando la colisión con el límite inferior para todos los personajes:


```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el jugador no se desactive al salir de los límites
        this.setActive(true);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Permitir movimiento horizontal más allá del ancho original
        // Desactivar colisión con bordes horizontales, activar límite inferior
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Activar colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el mono no se desactive al salir de los límites
        this.setActive(true);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Permitir movimiento horizontal libre, activar límite inferior
        // Desactivar colisión con bordes horizontales, activar límite inferior
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Activar colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes horizontales (si existen)
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Permitir movimiento horizontal libre, activar límite inferior
        this.body.setCollideWorldBounds(false);
        this.body.checkCollision.left = false;
        this.body.checkCollision.right = false;
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Activar colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Movimiento hacia el jugador
        if (player && player.isAlive) {
            if (player.x > this.x + 50) {
                this.direction = 1;
            } else if (player.x < this.x - 50) {
                this.direction = -1;
            }
        }
        
        // Aplicar velocidad con patrón más complejo
        const baseSpeed = this.speed * this.direction;
        // Movimiento con patrón sinusoidal para hacerlo más impredecible
        const patternOffset = Math.sin(time * 0.001) * 20;
        this.body.setVelocityX(baseSpeed + patternOffset);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // El gorila puede lanzar proyectiles o cargar
        // Por simplicidad, aquí solo cambia de dirección rápidamente
        // En una versión más avanzada, podría lanzar proyectiles
        this.direction *= -1;
        this.body.setVelocityX(this.speed * this.direction * 1.5); // Carga rápida
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Activada la colisión con el límite inferior para todos los personajes. Cambios aplicados:

## Cambios implementados

### Colisión con límite inferior activada
- **Player** (`Player.js`): `checkCollision.down = true`
- **Monkey** (`Monkey.js`): `checkCollision.down = true`
- **Gorilla** (`Gorilla.js`): `checkCollision.down = true`

Al llegar al límite inferior, los personajes colisionarán y se detendrán en lugar de desaparecer. El límite inferior del mundo está en `GAME_CONFIG.HEIGHT + 2000`, así que hay espacio suficiente.

Prueba el juego y confirma si esto resuelve el problema. Si siguen desapareciendo, puede haber otra lógica que los destruya; en ese caso, la revisamos.

---

**User**

La parte inferior del juego debe ser también una plataforma, de forma que los personajes no caigan y desaparezcan

---

**Cursor**

Agregando una plataforma en la parte inferior del mundo para que los personajes no caigan:


Agregando una plataforma en la parte inferior del mundo:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        this.boss = new Gorilla(this, GAME_CONFIG.WIDTH * 2.5, GAME_CONFIG.HEIGHT - 200, this.level);
        this.gorillas.add(this.boss);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player);
        this.cameras.main.setDeadzone(100, 100);
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Agregada una plataforma en la parte inferior del mundo. Cambios:

## Cambio implementado

### Plataforma inferior del mundo (suelo)
- Creada una plataforma que cubre todo el ancho del mundo (`GAME_CONFIG.WIDTH * 3`)
- Posicionada en el límite inferior visible (`GAME_CONFIG.HEIGHT - 15`)
- Grosor de 30 píxeles para colisiones
- Agregada al grupo de plataformas, por lo que colisiona automáticamente con:
  - El jugador
  - Los monos
  - Los gorilas

Los personajes ya no deberían caer y desaparecer; se detendrán sobre esta plataforma inferior. La plataforma usa el mismo color que las demás (`COLORS.PLATFORM`), así que se verá como parte del suelo del juego.

---

**User**

Cuando llego al final de la escena el protagonista desaparece. Y no veo al jefe por ningun lado

---

**Cursor**

Revisando dónde se crea el jefe y por qué el protagonista desaparece al final:


Revisando los límites del mundo y la posición del jefe:


Ajustando los límites de la cámara y verificando la posición del jefe:


Ajustando la cámara y asegurando que el jugador y el jefe permanezcan visibles:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        this.boss = new Gorilla(this, GAME_CONFIG.WIDTH * 2.5, GAME_CONFIG.HEIGHT - 200, this.level);
        this.gorillas.add(this.boss);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        // Posicionar el jefe en una posición visible cerca del final pero accesible
        const bossX = GAME_CONFIG.WIDTH * 2.4 + (GAME_CONFIG.WIDTH * 0.4) / 2; // Centro de la última plataforma
        const bossY = GAME_CONFIG.HEIGHT - 200; // Sobre la plataforma del jefe
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible
        this.boss.setActive(true);
        this.boss.setVisible(true);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        // Posicionar el jefe en una posición visible cerca del final pero accesible
        const bossX = GAME_CONFIG.WIDTH * 2.4 + (GAME_CONFIG.WIDTH * 0.4) / 2; // Centro de la última plataforma
        const bossY = GAME_CONFIG.HEIGHT - 200; // Sobre la plataforma del jefe
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible
        this.boss.setActive(true);
        this.boss.setVisible(true);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

## Cambios implementados

### 1. Cámara ajustada para seguir al jugador
- Eliminado el deadzone (0, 0) para que la cámara siga al jugador siempre
- Configurado `startFollow` con parámetros para seguimiento suave y centrado
- La cámara sigue al jugador hasta el final del mundo

### 2. Posición del jefe ajustada
- Reposicionado en el centro de la última plataforma
- Calculado como: `GAME_CONFIG.WIDTH * 2.4 + (GAME_CONFIG.WIDTH * 0.4) / 2`
- Asegurado que esté activo y visible al crearlo

### 3. Jugador permanece visible
- Agregada verificación en `update()` para mantener al jugador activo y visible
- Si se desactiva o se oculta, se reactiva automáticamente

El jugador debería permanecer visible al llegar al final y el jefe debería estar visible en la última plataforma. La cámara seguirá al jugador hasta el final del nivel.

---

**User**

Activa la colisión en la derecha y en la izquierda al llegar al final e inicio de la escena.

---

**Cursor**

Activando las colisiones en los bordes izquierdo y derecho del mundo:


Activando las colisiones en los bordes izquierdo y derecho:


```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el jugador no se desactive al salir de los límites
        this.setActive(true);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el mono no se desactive al salir de los límites
        this.setActive(true);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes horizontales (si existen)
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Movimiento hacia el jugador
        if (player && player.isAlive) {
            if (player.x > this.x + 50) {
                this.direction = 1;
            } else if (player.x < this.x - 50) {
                this.direction = -1;
            }
        }
        
        // Aplicar velocidad con patrón más complejo
        const baseSpeed = this.speed * this.direction;
        // Movimiento con patrón sinusoidal para hacerlo más impredecible
        const patternOffset = Math.sin(time * 0.001) * 20;
        this.body.setVelocityX(baseSpeed + patternOffset);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // El gorila puede lanzar proyectiles o cargar
        // Por simplicidad, aquí solo cambia de dirección rápidamente
        // En una versión más avanzada, podría lanzar proyectiles
        this.direction *= -1;
        this.body.setVelocityX(this.speed * this.direction * 1.5); // Carga rápida
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Activadas las colisiones en los bordes izquierdo y derecho del mundo. Cambios aplicados:

## Cambios implementados

### Colisiones con bordes horizontales activadas
- **Player** (`Player.js`):
  - `setCollideWorldBounds(true)`
  - `checkCollision.left = true`
  - `checkCollision.right = true`
- **Monkey** (`Monkey.js`):
  - `setCollideWorldBounds(true)`
  - `checkCollision.left = true`
  - `checkCollision.right = true`
- **Gorilla** (`Gorilla.js`):
  - `setCollideWorldBounds(true)`
  - `checkCollision.left = true`
  - `checkCollision.right = true`

El jugador y los enemigos colisionan con los bordes izquierdo y derecho del mundo. El jugador no podrá salir por los extremos de la escena y se detendrá al llegar a los límites.

---

**User**

Sigo sin ver al jefe

---

**Cursor**

Revisando la posición y visibilidad del jefe:


Revisando el orden de creación y ajustando la posición del jefe para que quede sobre la plataforma:


Ajustando la posición del jefe para que quede sobre la plataforma y agregando un indicador visual para verificar su ubicación:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // Posicionar el jefe en el centro de esa plataforma, justo encima
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        const bossY = bossPlatformY - 50; // Justo encima de la plataforma (la plataforma tiene 30px de grosor)
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Debug: mostrar posición del jefe en consola
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Agregando una verificación en el update para mantener al jefe visible y un indicador visual:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // Posicionar el jefe en el centro de esa plataforma, justo encima
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        const bossY = bossPlatformY - 50; // Justo encima de la plataforma (la plataforma tiene 30px de grosor)
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Debug: mostrar posición del jefe en consola
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && !this.boss.active) {
            this.boss.setActive(true);
        }
        if (this.boss && !this.boss.visible) {
            this.boss.setVisible(true);
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Ajustes aplicados. Resumen:

## Cambios implementados

### 1. Posición del jefe corregida
- Calculada la posición basándose en la última plataforma
- Posición X: centro de la plataforma del jefe (3120 píxeles)
- Posición Y: justo encima de la plataforma (650 - 50 = 600 píxeles)
- El jefe queda sobre la plataforma en lugar de flotar

### 2. Visibilidad del jefe asegurada
- `setActive(true)` y `setVisible(true)` al crearlo
- `setDepth(10)` para que esté por encima de otros elementos
- Verificación en `update()` para mantenerlo activo y visible

### 3. Debug agregado
- Console.log con la posición del jefe para verificar su ubicación

El jefe debería estar visible en la última plataforma. Si no aparece:
1. Abre la consola del navegador (F12) y revisa el mensaje de debug con la posición
2. Avanza hasta el final del nivel; el jefe está en x=3120 (cerca del final)
3. Verifica que el jefe no esté cayendo; debería colisionar con la plataforma

Si sigue sin aparecer, comparte lo que muestra la consola.

---

**User**

Sigo sin ver al jefe, pero aparece el siguiente log en la consola:

Jefe creado en posición: x=3120, y=600, nivel=1

---

**Cursor**

El jefe se crea en x=3120, pero puede estar fuera de la vista inicial. Verificando que esté visible cuando el jugador llegue al final y que no esté cayendo:


Ajustando la posición del jefe para que quede sobre la plataforma y agregando más debug para verificar su estado:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene aproximadamente 80-100px de altura, posicionarlo justo sobre la plataforma
        const bossY = bossPlatformY - 60; // Justo encima de la plataforma (considerando altura del gorila)
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener la velocidad inicial del jefe para que no se mueva hasta que el jugador se acerque
        this.boss.body.setVelocityX(0);
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && !this.boss.active) {
            this.boss.setActive(true);
        }
        if (this.boss && !this.boss.visible) {
            this.boss.setVisible(true);
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene aproximadamente 80-100px de altura, posicionarlo justo sobre la plataforma
        const bossY = bossPlatformY - 60; // Justo encima de la plataforma (considerando altura del gorila)
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener la velocidad inicial del jefe para que no se mueva hasta que el jugador se acerque
        this.boss.body.setVelocityX(0);
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Colisiones
        this.setupCollisions();
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Agregado más debug y ajustes. Al ejecutar el juego, la consola mostrará:

1. Al crear el jefe: posición, estado (activo/visible/alive) y tamaño
2. Cada 5 segundos: posición actual y estado
3. Si se desactiva o se oculta: mensajes cuando se reactiva

Cambios:
- Posición Y ajustada: `bossY = bossPlatformY - 60` (justo sobre la plataforma)
- Velocidad inicial detenida: el jefe no se mueve hasta que el jugador se acerque
- Debug periódico: cada 5 segundos muestra la posición y estado

Revisa la consola y comparte:
1. Los mensajes al crear el jefe (posición, activo, visible, tamaño)
2. Los mensajes periódicos cada 5 segundos (posición actual)
3. Si aparece "Jefe reactivado" o "Jefe hecho visible"

Con esto podremos ver si el jefe se está creando correctamente, si está cayendo, o si hay otro problema.

---

**User**

Jefe creado en posición: x=3120, y=590, nivel=1

GameScene.js:65 Jefe activo: true, visible: true, alive: true

GameScene.js:66 Tamaño del jefe: width=80, height=80

GameScene.js:279 Jefe - Posición: x=3504, y=735, activo: true, visible: true

GameScene.js:279 Jefe - Posición: x=4254, y=5881, activo: true, visible: true

GameScene.js:279 Jefe - Posición: x=5004, y=25806, activo: true, visible: true

GameScene.js:279 Jefe - Posición: x=5754, y=60731, activo: true, visible: true

GameScene.js:279 Jefe - Posición: x=6504, y=108818, activo: true, visible: true

GameScene.js:279 Jefe - Posición: x=7254, y=158818, activo: true, visible: true

GameScene.js:279 Jefe - Posición: x=8004, y=208818, activo: true, visible: true

---

**Cursor**

El jefe está cayendo sin parar: la Y aumenta continuamente (590 → 208818), lo que indica que no colisiona con la plataforma. También se mueve en X. Corrigiendo la posición inicial y asegurando la colisión con la plataforma:


El jefe está cayendo porque no colisiona con la plataforma. Ajustando la posición para que quede sobre la plataforma y asegurando la colisión:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Aplicados cambios para evitar que el jefe caiga:

## Cambios implementados

### 1. Colisiones configuradas antes de crear el jefe
- `setupCollisions()` se llama antes de crear el jefe para que las colisiones estén activas

### 2. Posición ajustada
- Posición Y: `bossPlatformY - 40` (más cerca de la plataforma)
- Velocidades X e Y detenidas al crear el jefe

### 3. Reposicionamiento automático
- `delayedCall` después de 100ms para reposicionar el jefe
- Verificación en `update()`: si `y > GAME_CONFIG.HEIGHT + 100`, se reposiciona automáticamente sobre la plataforma

### 4. Debug mejorado
- Muestra si el jefe está en el suelo (`onFloor()`)
- Detecta y corrige caídas automáticamente

El jefe debería quedarse sobre la plataforma. Si sigue cayendo, el sistema lo reposiciona automáticamente. Prueba el juego y revisa la consola; deberías ver que la posición Y se mantiene alrededor de 610 (o se corrige automáticamente si cae).

---

**User**

El jefe no tiene un movimiento coherente, debería ser similar al de los monos, pero más complejo para que sea más dificil de matar. 



Jefe creado en posición: x=3120, y=610, nivel=1

GameScene.js:81 Jefe activo: true, visible: true, alive: true

GameScene.js:82 Tamaño del jefe: width=80, height=80

GameScene.js:75 Jefe reposicionado: x=3120, y=610

GameScene.js:306 Jefe - Posición: x=3428, y=735, activo: true, visible: true, en suelo: true

GameScene.js:80 Jefe creado en posición: x=3120, y=610, nivel=1

GameScene.js:81 Jefe activo: true, visible: true, alive: true

GameScene.js:82 Tamaño del jefe: width=80, height=80

GameScene.js:75 Jefe reposicionado: x=3120, y=610

GameScene.js:306 Jefe - Posición: x=3140, y=618, activo: true, visible: true, en suelo: false

GameScene.js:301 Jefe estaba cayendo, reposicionado a: x=3120, y=610

GameScene.js:306 Jefe - Posición: x=3265, y=735, activo: true, visible: true, en suelo: true

GameScene.js:301 Jefe estaba cayendo, reposicionado a: x=3120, y=610

GameScene.js:306 Jefe - Posición: x=3390, y=735, activo: true, visible: true, en suelo: true

GameScene.js:301 Jefe estaba cayendo, reposicionado a: x=3120, y=610

GameScene.js:306 Jefe - Posición: x=3515, y=735, activo: true, visible: true, en suelo: true

GameScene.js:301 Jefe estaba cayendo, reposicionado a: x=3120, y=610

GameScene.js:306 Jefe - Posición: x=3640, y=736, activo: true, visible: true, en suelo: false

GameScene.js:301 Jefe estaba cayendo, reposicionado a: x=3120, y=610

---

**Cursor**

Revisando el movimiento del jefe y ajustándolo para que sea similar al de los monos, pero más complejo y desafiante:


Ajustando el movimiento del jefe para que sea similar al de los monos, pero más complejo y desafiante:


```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Movimiento hacia el jugador
        if (player && player.isAlive) {
            if (player.x > this.x + 50) {
                this.direction = 1;
            } else if (player.x < this.x - 50) {
                this.direction = -1;
            }
        }
        
        // Aplicar velocidad con patrón más complejo
        const baseSpeed = this.speed * this.direction;
        // Movimiento con patrón sinusoidal para hacerlo más impredecible
        const patternOffset = Math.sin(time * 0.001) * 20;
        this.body.setVelocityX(baseSpeed + patternOffset);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // El gorila puede lanzar proyectiles o cargar
        // Por simplicidad, aquí solo cambia de dirección rápidamente
        // En una versión más avanzada, podría lanzar proyectiles
        this.direction *= -1;
        this.body.setVelocityX(this.speed * this.direction * 1.5); // Carga rápida
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                }
                
                // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, moverse aleatoriamente o patrullar
                if (time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Cambiar dirección ocasionalmente cuando el jugador está lejos
                    if (Math.random() < 0.3) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        const baseSpeed = this.speed * this.direction;
        // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
        const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
        this.body.setVelocityX(baseSpeed + speedVariation);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // El gorila puede lanzar proyectiles o cargar
        // Por simplicidad, aquí solo cambia de dirección rápidamente
        // En una versión más avanzada, podría lanzar proyectiles
        this.direction *= -1;
        this.body.setVelocityX(this.speed * this.direction * 1.5); // Carga rápida
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                }
                
                // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, moverse aleatoriamente o patrullar
                if (time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Cambiar dirección ocasionalmente cuando el jugador está lejos
                    if (Math.random() < 0.3) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        const baseSpeed = this.speed * this.direction;
        // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
        const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
        this.body.setVelocityX(baseSpeed + speedVariation);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        this.boss.direction = 0; // Sin dirección inicial hasta detectar jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Corrigiendo el problema de dirección inicial:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                }
                
                // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, moverse aleatoriamente o patrullar
                if (time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Cambiar dirección ocasionalmente cuando el jugador está lejos
                    if (Math.random() < 0.3) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Asegurando que el jefe establezca una dirección válida cuando detecte al jugador:


```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = -1; // Empieza moviéndose a la izquierda
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, moverse aleatoriamente o patrullar
                if (time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Cambiar dirección ocasionalmente cuando el jugador está lejos
                    if (Math.random() < 0.3) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Inicializando la dirección a 0 en el constructor:


```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, moverse aleatoriamente o patrullar
                if (time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Cambiar dirección ocasionalmente cuando el jugador está lejos
                    if (Math.random() < 0.3) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Ajustando el comportamiento cuando el jugador está lejos:


```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

## Cambios implementados

### 1. Comportamiento similar a los monos
- Detección del jugador: rango de 400px (más amplio que los monos)
- Movimiento hacia el jugador cuando está dentro del rango
- Cambio de dirección con cooldown (500ms) para evitar cambios bruscos

### 2. Comportamiento más complejo
- Pausas estratégicas: ocasionalmente se detiene (1% de probabilidad) durante 300-500ms
- Variación de velocidad: velocidad base con variación sinusoidal (±20%)
- Patrullaje cuando el jugador está lejos: puede cambiar de dirección o detenerse
- Ataques mejorados: carga hacia el jugador a 1.3x la velocidad normal

### 3. Estado inicial
- El jefe comienza quieto (direction = 0)
- Solo se mueve cuando detecta al jugador dentro del rango
- Evita movimientos erráticos al inicio

### 4. Movimiento más estratégico
- No cambia de dirección constantemente
- Usa cooldowns para decisiones más predecibles
- Velocidad variable para dificultar el apuntado

El jefe debería moverse de forma más coherente y desafiante, similar a los monos pero con comportamientos adicionales que lo hacen más difícil de derrotar.

---

**User**

Modificaciones a realizar:

1. El juego tiene que tener solo 4 niveles

2. Si te matan y tienes más vidas, tienes que empezar el nivel desde el principio

3. Los enemigos cuando colisionan de forma lateral o detectan un obstaculo, pueden saltar o cambiar de dirección para salvarlo

---

**Cursor**

Revisando el código para implementar los cambios:


Implementando los cambios:


Implementando los tres cambios:


```javascript
class VictoryScene extends Phaser.Scene {
    constructor() {
        super({ key: 'VictoryScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.timeBonus = data.timeBonus || 0;
    }
    
    create() {
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x001a00)
            .setOrigin(0, 0);
        
        // Título Victoria
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, '¡NIVEL COMPLETADO!', {
            fontSize: '56px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información del nivel
        const infoStyle = {
            fontSize: '28px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 280, `Nivel ${this.level}`, {
            ...infoStyle,
            fontSize: '36px',
            fill: '#ffff00'
        }).setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 340, `Puntuación: ${this.score}`, infoStyle)
            .setOrigin(0.5);
        
        if (this.timeBonus > 0) {
            this.add.text(GAME_CONFIG.WIDTH / 2, 380, `Bonus de Tiempo: +${this.timeBonus}`, {
                ...infoStyle,
                fill: '#00ff00'
            }).setOrigin(0.5);
        }
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 420, `Vidas Restantes: ${this.lives}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Siguiente Nivel (solo si no es el nivel 4)
        if (this.level < 4) {
            const nextButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 350, 80, 0x228b22)
                .setInteractive({ useHandCursor: true })
                .on('pointerdown', () => {
                    this.scene.start('GameScene', {
                        level: this.level + 1,
                        score: this.score,
                        lives: this.lives
                    });
                })
                .on('pointerover', () => {
                    nextButton.setFillStyle(0x32cd32);
                })
                .on('pointerout', () => {
                    nextButton.setFillStyle(0x228b22);
                });
            
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'SIGUIENTE NIVEL', {
                fontSize: '36px',
                fontFamily: 'Arial',
                fill: '#ffffff',
                stroke: '#000000',
                strokeThickness: 2
            }).setOrigin(0.5);
        } else {
            // Si es el nivel 4, mostrar mensaje de victoria final
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, '¡JUEGO COMPLETADO!', {
                fontSize: '40px',
                fontFamily: 'Arial',
                fill: '#ffd700',
                stroke: '#000000',
                strokeThickness: 3
            }).setOrigin(0.5);
        }
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 350, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Efecto de celebración
        this.createCelebrationEffect();
    }
    
    createCelebrationEffect() {
        // Partículas de celebración
        for (let i = 0; i < 20; i++) {
            const particle = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                5,
                0xffd700
            );
            
            this.tweens.add({
                targets: particle,
                y: particle.y - 200,
                alpha: 0,
                duration: 2000 + Math.random() * 1000,
                delay: Math.random() * 500,
                repeat: -1
            });
        }
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Si el jugador tiene vidas restantes, reiniciar el nivel desde el principio
        if (this.player.lives > 0) {
            // Reiniciar el nivel actual con las vidas restantes
            this.scene.start('GameScene', {
                level: this.level,
                score: this.score,
                lives: this.player.lives
            });
        } else {
            // Si no hay vidas, ir a Game Over
            this.scene.start('GameOverScene', {
                score: this.score,
                level: this.level
            });
        }
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el mono no se desactive al salir de los límites
        this.setActive(true);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Capacidad de salto y detección de obstáculos
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 200; // Verificar obstáculos cada 200ms
        this.jumpCooldown = 0;
        this.minJumpCooldown = 1000; // Mínimo 1 segundo entre saltos
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección al tocar bordes horizontales (si existen)
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el mono no se desactive al salir de los límites
        this.setActive(true);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Capacidad de salto y detección de obstáculos
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 200; // Verificar obstáculos cada 200ms
        this.jumpCooldown = 0;
        this.minJumpCooldown = 1000; // Mínimo 1 segundo entre saltos
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección o saltar al tocar bordes horizontales
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (70% cambiar dirección, 30% saltar)
                if (this.canJump && !this.isJumping && Math.random() < 0.3 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                }
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player) {
        if (!this.isAlive) return;
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el mono no se desactive al salir de los límites
        this.setActive(true);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Capacidad de salto y detección de obstáculos
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 200; // Verificar obstáculos cada 200ms
        this.jumpCooldown = 0;
        this.minJumpCooldown = 1000; // Mínimo 1 segundo entre saltos
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección o saltar al tocar bordes horizontales
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (70% cambiar dirección, 30% saltar)
                if (this.canJump && !this.isJumping && Math.random() < 0.3 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                }
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar obstáculos delante (solo si ha pasado el cooldown)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    checkObstacles(time) {
        // Raycast hacia adelante para detectar obstáculos
        const rayLength = 50; // Distancia de detección
        const rayX = this.x + (this.direction * (this.width / 2 + rayLength));
        const rayY = this.y;
        
        // Verificar si hay una plataforma delante (usando el mundo físico)
        // Si detectamos que vamos a chocar, decidir acción
        if (this.body.blocked.left || this.body.blocked.right) {
            // Ya estamos bloqueados, decidir acción
            if (this.canJump && !this.isJumping && Math.random() < 0.4 && this.jumpCooldown <= 0) {
                // 40% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 60% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.7); // Salto un poco más bajo que el jugador
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

Actualizando la llamada a `monkey.update()` para pasar el parámetro `time`:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                if (!player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Si el jugador tiene vidas restantes, reiniciar el nivel desde el principio
        if (this.player.lives > 0) {
            // Reiniciar el nivel actual con las vidas restantes
            this.scene.start('GameScene', {
                level: this.level,
                score: this.score,
                lives: this.player.lives
            });
        } else {
            // Si no hay vidas, ir a Game Over
            this.scene.start('GameOverScene', {
                score: this.score,
                level: this.level
            });
        }
    }
}


```

Mejorando la detección de obstáculos para que los monos detecten plataformas delante y reaccionen mejor:


```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el mono no se desactive al salir de los límites
        this.setActive(true);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Capacidad de salto y detección de obstáculos
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 200; // Verificar obstáculos cada 200ms
        this.jumpCooldown = 0;
        this.minJumpCooldown = 1000; // Mínimo 1 segundo entre saltos
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección o saltar al tocar bordes horizontales
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (70% cambiar dirección, 30% saltar)
                if (this.canJump && !this.isJumping && Math.random() < 0.3 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                }
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar obstáculos delante (solo si ha pasado el cooldown)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    checkObstacles(time) {
        // Verificar si estamos bloqueados por un obstáculo lateral
        if (this.body.blocked.left || this.body.blocked.right) {
            // Estamos bloqueados, decidir acción
            if (this.canJump && !this.isJumping && Math.random() < 0.4 && this.jumpCooldown <= 0) {
                // 40% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 60% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        }
        
        // También verificar si hay un borde delante (prevenir caídas)
        // Usar raycast para detectar si hay suelo delante
        const rayLength = 60;
        const rayStartX = this.x + (this.direction * (this.width / 2));
        const rayStartY = this.y + (this.height / 2);
        const rayEndX = rayStartX + (this.direction * rayLength);
        const rayEndY = rayStartY + 40; // Verificar un poco más abajo
        
        // Si estamos en el suelo y no hay suelo delante, considerar cambiar de dirección
        if (this.body.onFloor() && this.body.velocity.x !== 0) {
            // Usar el mundo físico para verificar si hay colisión delante
            // Si no hay plataforma delante y estamos cerca del borde, cambiar dirección
            const worldBounds = this.scene.physics.world.bounds;
            const isNearEdge = (this.direction > 0 && this.x > worldBounds.width - 100) ||
                              (this.direction < 0 && this.x < 100);
            
            if (isNearEdge && Math.random() < 0.3) {
                // 30% de probabilidad de cambiar dirección cerca de los bordes
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.7); // Salto un poco más bajo que el jugador
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

Mejorando la detección de obstáculos usando colisiones con plataformas:


```javascript
class Monkey extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, type = ENEMY_TYPES.MONKEY_BASIC) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `monkey_${type}`;
        
        // Definir el tamaño según el tipo (necesario para la física)
        const size = type === ENEMY_TYPES.MONKEY_STRONG ? 50 : 40;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del mono según el tipo programáticamente
            const graphics = scene.add.graphics();
            const color = type === ENEMY_TYPES.MONKEY_STRONG ? 0x654321 : COLORS.MONKEY;
            
            graphics.fillStyle(color);
            graphics.fillCircle(size / 2, size / 2, size / 2);
            // Ojos
            graphics.fillStyle(0xffffff);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 6);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 6);
            graphics.fillStyle(0x000000);
            graphics.fillCircle(size / 2 - 8, size / 2 - 5, 3);
            graphics.fillCircle(size / 2 + 8, size / 2 - 5, 3);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 8, 12, 8);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el mono no se desactive al salir de los límites
        this.setActive(true);
        
        this.type = type;
        this.isAlive = true;
        this.direction = Math.random() > 0.5 ? 1 : -1; // Dirección aleatoria inicial
        this.detectionRange = 200; // Rango de detección del jugador
        this.resistance = this.getResistance();
        this.speed = this.getSpeed();
        
        // Capacidad de salto y detección de obstáculos
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 200; // Verificar obstáculos cada 200ms
        this.jumpCooldown = 0;
        this.minJumpCooldown = 1000; // Mínimo 1 segundo entre saltos
        
        // Configurar física
        this.body.setSize(size - 5, size - 5);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Cambiar dirección o saltar al tocar bordes horizontales
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (70% cambiar dirección, 30% saltar)
                if (this.canJump && !this.isJumping && Math.random() < 0.3 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                }
            }
        });
    }
    
    getResistance() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return RESISTANCE.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return RESISTANCE.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return RESISTANCE.MONKEY_STRONG;
            default:
                return 1;
        }
    }
    
    getSpeed() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return ENEMY_SPEEDS.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return ENEMY_SPEEDS.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return ENEMY_SPEEDS.MONKEY_STRONG;
            default:
                return 80;
        }
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar obstáculos delante (solo si ha pasado el cooldown)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Detectar jugador cercano
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x) {
                    this.direction = 1;
                } else {
                    this.direction = -1;
                }
            }
        }
        
        // Aplicar velocidad
        this.body.setVelocityX(this.speed * this.direction);
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
    }
    
    checkObstacles(time) {
        // Verificar si estamos bloqueados por un obstáculo lateral (colisión activa)
        if (this.body.blocked.left || this.body.blocked.right) {
            // Estamos bloqueados, decidir acción
            if (this.canJump && !this.isJumping && Math.random() < 0.4 && this.jumpCooldown <= 0) {
                // 40% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 60% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
            return; // Ya manejamos el obstáculo
        }
        
        // Verificar si hay plataformas delante usando overlap temporal
        // Crear un pequeño rectángulo de detección delante del mono
        const detectionDistance = 40;
        const detectionX = this.x + (this.direction * (this.width / 2 + detectionDistance));
        const detectionY = this.y;
        const detectionWidth = 20;
        const detectionHeight = this.height;
        
        // Verificar colisión con plataformas usando el sistema de física
        // Si hay una plataforma delante a la misma altura o más alta, considerar saltar
        if (this.body.onFloor() && this.body.velocity.x !== 0) {
            // Verificar si estamos cerca de los bordes del mundo
            const worldBounds = this.scene.physics.world.bounds;
            const isNearWorldEdge = (this.direction > 0 && this.x > worldBounds.width - 150) ||
                                   (this.direction < 0 && this.x < 150);
            
            if (isNearWorldEdge && Math.random() < 0.3) {
                // 30% de probabilidad de cambiar dirección cerca de los bordes del mundo
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.7); // Salto un poco más bajo que el jugador
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Enemigo derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(100, () => {
            this.destroy();
        });
    }
    
    getScore() {
        switch (this.type) {
            case ENEMY_TYPES.MONKEY_BASIC:
                return SCORES.MONKEY_BASIC;
            case ENEMY_TYPES.MONKEY_FAST:
                return SCORES.MONKEY_FAST;
            case ENEMY_TYPES.MONKEY_STRONG:
                return SCORES.MONKEY_STRONG;
            default:
                return 100;
        }
    }
}


```

Implementados los tres cambios. Resumen:

## Cambios implementados

### 1. Juego limitado a 4 niveles
- Modificado `VictoryScene.js` para que el botón "SIGUIENTE NIVEL" solo aparezca si `level < 4`
- Al completar el nivel 4, muestra "¡JUEGO COMPLETADO!" en lugar del botón

### 2. Reiniciar nivel al morir con vidas restantes
- Modificado `GameScene.gameOver()` para verificar si el jugador tiene vidas
- Si hay vidas: reinicia el nivel actual desde el principio manteniendo la puntuación y las vidas restantes
- Si no hay vidas: va a la escena de Game Over

### 3. Enemigos pueden saltar o cambiar de dirección al detectar obstáculos
- Agregadas propiedades en `Monkey.js`:
  - `canJump`, `isJumping`: control de salto
  - `jumpCooldown`: cooldown entre saltos (1 segundo mínimo)
  - `lastObstacleCheck`: control de frecuencia de verificación
- Método `checkObstacles()`:
  - Detecta cuando están bloqueados lateralmente (`body.blocked.left/right`)
  - 40% de probabilidad de saltar sobre el obstáculo
  - 60% de probabilidad de cambiar de dirección
  - Detecta bordes del mundo y cambia de dirección para evitar caídas
- Método `jump()`:
  - Permite a los monos saltar (70% de la altura del salto del jugador)
  - Respeta el cooldown para evitar saltos constantes
- Actualizado `update()`:
  - Ahora recibe el parámetro `time` para controlar cooldowns
  - Verifica si está en el suelo para permitir saltos
  - Llama a `checkObstacles()` periódicamente

Los monos ahora pueden saltar sobre obstáculos o cambiar de dirección cuando detectan colisiones laterales, haciendo el juego más dinámico y desafiante.

---

**User**

Los jefes se quedan enganchados en elementos colisionados y no se mueven lo que hace que sea extremadamente facil matarlos

---

**Cursor**

Revisando el código del jefe para corregir el bloqueo y mejorar su movimiento:


Agregando detección de obstáculos y capacidad de salto al jefe, y mejorando la lógica para evitar que se quede atascado:


```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar si está atascado (bloqueado y sin movimiento)
        if (this.body.blocked.left || this.body.blocked.right) {
            this.stuckTimer += time;
            
            // Si está atascado más del umbral, forzar acción inmediata
            if (this.stuckTimer > this.stuckThreshold) {
                // Forzar cambio de dirección o salto sin cooldown
                if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
                    this.jump();
                    this.stuckTimer = 0;
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido para escapar
                    this.stuckTimer = 0;
                    this.lastDirectionChange = time; // Resetear cooldown de cambio de dirección
                }
            }
        } else {
            // Si no está bloqueado, resetear contador
            this.stuckTimer = 0;
        }
        
        // Verificar obstáculos delante (más frecuentemente que los monos)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && 
                    time - this.lastDirectionChange > this.directionChangeCooldown) {
                    // Cambiar dirección solo si ha pasado suficiente tiempo (movimiento más estratégico)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar si está atascado (bloqueado y sin movimiento)
        if (this.body.blocked.left || this.body.blocked.right) {
            this.stuckTimer += time;
            
            // Si está atascado más del umbral, forzar acción inmediata
            if (this.stuckTimer > this.stuckThreshold) {
                // Forzar cambio de dirección o salto sin cooldown
                if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
                    this.jump();
                    this.stuckTimer = 0;
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido para escapar
                    this.stuckTimer = 0;
                    this.lastDirectionChange = time; // Resetear cooldown de cambio de dirección
                }
            }
        } else {
            // Si no está bloqueado, resetear contador
            this.stuckTimer = 0;
        }
        
        // Verificar obstáculos delante (más frecuentemente que los monos)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                // Si está atascado, ignorar el cooldown
                const canChangeDirection = (time - this.lastDirectionChange > this.directionChangeCooldown) || 
                                         (this.stuckTimer > this.stuckThreshold / 2);
                
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && canChangeDirection) {
                    // Cambiar dirección (movimiento más estratégico, pero permitir cambio si está atascado)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                    this.stuckTimer = 0; // Resetear contador al cambiar dirección
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        this.directionChangeCooldown = 500; // Cambiar dirección cada 500ms como máximo
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar si está atascado (bloqueado y sin movimiento)
        if (this.body.blocked.left || this.body.blocked.right) {
            this.stuckTimer += time;
            
            // Si está atascado más del umbral, forzar acción inmediata
            if (this.stuckTimer > this.stuckThreshold) {
                // Forzar cambio de dirección o salto sin cooldown
                if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
                    this.jump();
                    this.stuckTimer = 0;
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido para escapar
                    this.stuckTimer = 0;
                    this.lastDirectionChange = time; // Resetear cooldown de cambio de dirección
                }
            }
        } else {
            // Si no está bloqueado, resetear contador
            this.stuckTimer = 0;
        }
        
        // Verificar obstáculos delante (más frecuentemente que los monos)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                // Si está atascado, ignorar el cooldown
                const canChangeDirection = (time - this.lastDirectionChange > this.directionChangeCooldown) || 
                                         (this.stuckTimer > this.stuckThreshold / 2);
                
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && canChangeDirection) {
                    // Cambiar dirección (movimiento más estratégico, pero permitir cambio si está atascado)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                    this.stuckTimer = 0; // Resetear contador al cambiar dirección
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    checkObstacles(time) {
        // Verificar si estamos bloqueados por un obstáculo lateral
        if (this.body.blocked.left || this.body.blocked.right) {
            // Estamos bloqueados, decidir acción inmediata
            if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                // 50% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 50% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido
                this.stuckTimer = 0;
                this.lastDirectionChange = time; // Resetear cooldown
            }
            return; // Ya manejamos el obstáculo
        }
        
        // Verificar si estamos cerca de los bordes del mundo
        if (this.body.onFloor() && this.body.velocity.x !== 0) {
            const worldBounds = this.scene.physics.world.bounds;
            const isNearWorldEdge = (this.direction > 0 && this.x > worldBounds.width - 150) ||
                                   (this.direction < 0 && this.x < 150);
            
            if (isNearWorldEdge && Math.random() < 0.2) {
                // 20% de probabilidad de cambiar dirección cerca de los bordes
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
                this.lastDirectionChange = time;
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.8); // Salto del jefe (80% del salto del jugador)
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
            this.stuckTimer = 0; // Resetear contador al saltar
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Mejoras implementadas para evitar que el jefe se quede atascado. Resumen:

## Cambios implementados

### 1. Detección de obstáculos
- Agregado `checkObstacles()` similar a los monos, pero más frecuente (cada 150ms)
- Detecta cuando está bloqueado lateralmente
- 50% de probabilidad de saltar sobre el obstáculo
- 50% de probabilidad de cambiar de dirección con velocidad aumentada (1.2x)

### 2. Sistema anti-atascado
- `stuckTimer`: contador del tiempo bloqueado
- `stuckThreshold`: 500ms; si supera este tiempo, fuerza una acción
- Si está atascado más de 500ms:
  - Intenta saltar (si puede)
  - O cambia de dirección inmediatamente sin esperar cooldown
  - Aumenta la velocidad (1.2x) para escapar más rápido

### 3. Capacidad de salto
- Agregado método `jump()` al jefe
- Salto al 80% de la altura del jugador
- Cooldown de 0.8 segundos entre saltos
- Puede saltar sobre obstáculos cuando está bloqueado

### 4. Mejoras en el cambio de dirección
- Si está atascado, ignora el cooldown de cambio de dirección
- Cambia de dirección inmediatamente si `stuckTimer > stuckThreshold / 2`
- Resetea el contador de atascado al cambiar de dirección o saltar

### 5. Detección en bordes del mundo
- Detecta cuando está cerca de los bordes del mundo
- 20% de probabilidad de cambiar de dirección para evitar quedarse atascado en los bordes

El jefe debería moverse mejor y no quedarse atascado. Si detecta un bloqueo, saltará o cambiará de dirección rápidamente, y si está atascado más de 500ms, forzará una acción inmediata para escapar.

---

**User**

Cuando me quitan una vida, sale directamente la pantalla de gameover

---

**Cursor**

Revisando la lógica de vidas y muerte del jugador:


Revisando la lógica: `gameOver()` se llama cuando `isAlive` es false, pero debería verificarse solo cuando no quedan vidas. Corrigiéndolo:


Corrigiendo la lógica: `gameOver()` se llama cuando `isAlive` es false, pero debe verificarse solo cuando no quedan vidas. Ajustando la verificación:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Solo llamar a gameOver si el jugador realmente murió (sin vidas)
                if (player.lives <= 0 && !player.isAlive) {
                    this.gameOver();
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable) {
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Solo llamar a gameOver si el jugador realmente murió (sin vidas)
                if (player.lives <= 0 && !player.isAlive) {
                    this.gameOver();
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Si el jugador tiene vidas restantes, reiniciar el nivel desde el principio
        if (this.player.lives > 0) {
            // Reiniciar el nivel actual con las vidas restantes
            this.scene.start('GameScene', {
                level: this.level,
                score: this.score,
                lives: this.player.lives
            });
        } else {
            // Si no hay vidas, ir a Game Over
            this.scene.start('GameOverScene', {
                score: this.score,
                level: this.level
            });
        }
    }
}


```

Revisando la inicialización de vidas y ajustando la lógica para que solo se llame a `gameOver()` cuando realmente no queden vidas:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable) {
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Solo llamar a gameOver si el jugador realmente murió (sin vidas restantes)
                // Verificar que las vidas llegaron a 0 y el jugador está muerto
                if (player.lives <= 0 && !player.isAlive) {
                    // Esperar un momento antes de llamar a gameOver para que se actualice el estado
                    this.time.delayedCall(100, () => {
                        this.gameOver();
                    });
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable) {
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Solo llamar a gameOver si el jugador realmente murió (sin vidas restantes)
                // Verificar que las vidas llegaron a 0 y el jugador está muerto
                if (player.lives <= 0 && !player.isAlive) {
                    // Esperar un momento antes de llamar a gameOver para que se actualice el estado
                    this.time.delayedCall(100, () => {
                        this.gameOver();
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Si el jugador tiene vidas restantes, reiniciar el nivel desde el principio
        if (this.player.lives > 0) {
            // Reiniciar el nivel actual con las vidas restantes
            this.scene.start('GameScene', {
                level: this.level,
                score: this.score,
                lives: this.player.lives
            });
        } else {
            // Si no hay vidas, ir a Game Over
            this.scene.start('GameOverScene', {
                score: this.score,
                level: this.level
            });
        }
    }
}


```

Ajustando la lógica: verificando las vidas después de recibir daño, antes de que `die()` se ejecute:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable) {
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), llamar a gameOver
                // gameOver() verificará si hay vidas restantes para reiniciar o mostrar Game Over
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        this.gameOver();
                    });
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable) {
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), llamar a gameOver
                // gameOver() verificará si hay vidas restantes para reiniciar o mostrar Game Over
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        this.gameOver();
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Si el jugador tiene vidas restantes, reiniciar el nivel desde el principio
        if (this.player.lives > 0) {
            // Reiniciar el nivel actual con las vidas restantes
            this.scene.start('GameScene', {
                level: this.level,
                score: this.score,
                lives: this.player.lives
            });
        } else {
            // Si no hay vidas, ir a Game Over
            this.scene.start('GameOverScene', {
                score: this.score,
                level: this.level
            });
        }
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable) {
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), llamar a gameOver
                // gameOver() verificará si hay vidas restantes para reiniciar o mostrar Game Over
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        this.gameOver();
                    });
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable) {
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), llamar a gameOver
                // gameOver() verificará si hay vidas restantes para reiniciar o mostrar Game Over
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        this.gameOver();
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Verificar las vidas del jugador
        // Si el jugador tiene vidas restantes (antes de morir), reiniciar el nivel
        // Nota: player.lives puede ser 0 aquí porque takeDamage() ya lo redujo
        // Pero si había vidas antes de morir, deberíamos reiniciar
        // Usamos this.lives que se actualiza antes de que el jugador muera completamente
        
        // Si hay vidas restantes (mayor a 0), reiniciar el nivel desde el principio
        if (this.lives > 0) {
            // Reiniciar el nivel actual con las vidas restantes
            this.scene.start('GameScene', {
                level: this.level,
                score: this.score,
                lives: this.lives
            });
        } else {
            // Si no hay vidas, ir a Game Over
            this.scene.start('GameOverScene', {
                score: this.score,
                level: this.level
            });
        }
    }
}


```

Corrigiendo: guardando las vidas antes de recibir daño para que `gameOver()` use el valor correcto:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), llamar a gameOver
                // Pasar las vidas restantes (livesBefore - 1) para verificar si hay vidas
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        // Usar livesBefore - 1 para saber las vidas que quedaban antes de morir
                        const remainingLives = livesBefore - 1;
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), llamar a gameOver
                // Pasar las vidas restantes (livesBefore - 1) para verificar si hay vidas
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        // Usar livesBefore - 1 para saber las vidas que quedaban antes de morir
                        const remainingLives = livesBefore - 1;
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Verificar las vidas del jugador
        // Si el jugador tiene vidas restantes (antes de morir), reiniciar el nivel
        // Nota: player.lives puede ser 0 aquí porque takeDamage() ya lo redujo
        // Pero si había vidas antes de morir, deberíamos reiniciar
        // Usamos this.lives que se actualiza antes de que el jugador muera completamente
        
        // Si hay vidas restantes (mayor a 0), reiniciar el nivel desde el principio
        if (this.lives > 0) {
            // Reiniciar el nivel actual con las vidas restantes
            this.scene.start('GameScene', {
                level: this.level,
                score: this.score,
                lives: this.lives
            });
        } else {
            // Si no hay vidas, ir a Game Over
            this.scene.start('GameOverScene', {
                score: this.score,
                level: this.level
            });
        }
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), llamar a gameOver
                // Pasar las vidas restantes (livesBefore - 1) para verificar si hay vidas
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        // Usar livesBefore - 1 para saber las vidas que quedaban antes de morir
                        const remainingLives = livesBefore - 1;
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), llamar a gameOver
                // Pasar las vidas restantes (livesBefore - 1) para verificar si hay vidas
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        // Usar livesBefore - 1 para saber las vidas que quedaban antes de morir
                        const remainingLives = livesBefore - 1;
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Corrigiendo el cálculo de vidas restantes:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), verificar si hay vidas restantes
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        // Las vidas restantes son las que tenía antes de recibir el daño menos 1
                        // Si tenía 1 vida y recibe daño: remainingLives = 0 (game over)
                        // Si tenía 2+ vidas y recibe daño: remainingLives > 0 (reiniciar nivel)
                        const remainingLives = livesBefore - 1;
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                // Si el jugador murió (lives <= 0), verificar si hay vidas restantes
                if (player.lives <= 0) {
                    this.time.delayedCall(500, () => {
                        // Las vidas restantes son las que tenía antes de recibir el daño menos 1
                        // Si tenía 1 vida y recibe daño: remainingLives = 0 (game over)
                        // Si tenía 2+ vidas y recibe daño: remainingLives > 0 (reiniciar nivel)
                        const remainingLives = livesBefore - 1;
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Corregida la lógica de vidas. Resumen:

## Corrección implementada

### Problema
Al recibir daño, se llamaba a `gameOver()` inmediatamente, incluso si quedaban vidas.

### Solución
1. Guardar vidas antes del daño: se guarda `livesBefore` antes de llamar a `takeDamage()`.
2. Verificar vidas restantes: si el jugador muere (`lives <= 0`), se calculan las vidas restantes como `livesBefore - 1`.
3. Decisión:
   - Si `remainingLives > 0`: reinicia el nivel con las vidas restantes.
   - Si `remainingLives <= 0`: va a la pantalla de Game Over.

### Ejemplo
- Jugador con 3 vidas recibe daño: `livesBefore = 3`, `player.lives = 2` → no se llama a `gameOver()` (sigue vivo).
- Jugador con 2 vidas recibe daño: `livesBefore = 2`, `player.lives = 1` → no se llama a `gameOver()` (sigue vivo).
- Jugador con 1 vida recibe daño: `livesBefore = 1`, `player.lives = 0`, `remainingLives = 0` → va a Game Over.
- Jugador con 2 vidas recibe daño y muere: `livesBefore = 2`, `player.lives = 0`, `remainingLives = 1` → reinicia el nivel.

Ahora solo se muestra Game Over cuando realmente no quedan vidas. Si quedan vidas, el nivel se reinicia automáticamente.

---

**User**

cuando me tocan los enemigos, deberían quitarme una vida y comenzar desde el principio del nivel, ahora cuando me tocan, me quitan todas las vidas y sale el game over

---

**Cursor**

Revisando cómo se manejan las vidas al recibir daño. Comprobando `Player.js` para entender `takeDamage()`:


Revisando cómo se inicializan las vidas en `GameScene`:


Ajustando la lógica: al recibir daño, siempre se reinicia el nivel (si quedan vidas). También añadiendo protección contra colisiones múltiples:


Ajustando la lógica: al recibir daño, siempre se reinicia el nivel (si quedan vidas), no solo cuando muere. Añadiendo protección contra colisiones múltiples:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                
                // Prevenir múltiples colisiones
                this.isRestarting = true;
                
                // Quitar UNA vida
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                
                // SIEMPRE reiniciar el nivel cuando recibe daño (si tiene vidas restantes)
                this.time.delayedCall(500, () => {
                    // Las vidas restantes son las que tenía antes de recibir el daño menos 1
                    const remainingLives = livesBefore - 1;
                    
                    if (remainingLives > 0) {
                        // Reiniciar nivel con las vidas restantes
                        this.scene.start('GameScene', {
                            level: this.level,
                            score: this.score,
                            lives: remainingLives
                        });
                    } else {
                        // No hay vidas, ir a Game Over
                        this.scene.start('GameOverScene', {
                            score: this.score,
                            level: this.level
                        });
                    }
                });
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                
                // Prevenir múltiples colisiones
                this.isRestarting = true;
                
                // Quitar UNA vida
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                
                // SIEMPRE reiniciar el nivel cuando recibe daño (si tiene vidas restantes)
                this.time.delayedCall(500, () => {
                    // Las vidas restantes son las que tenía antes de recibir el daño menos 1
                    const remainingLives = livesBefore - 1;
                    
                    if (remainingLives > 0) {
                        // Reiniciar nivel con las vidas restantes
                        this.scene.start('GameScene', {
                            level: this.level,
                            score: this.score,
                            lives: remainingLives
                        });
                    } else {
                        // No hay vidas, ir a Game Over
                        this.scene.start('GameOverScene', {
                            score: this.score,
                            level: this.level
                        });
                    }
                });
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Corregida la lógica de daño y reinicio. Cambios:

## Correcciones implementadas

### Problema
Al recibir daño, se quitaban todas las vidas y se mostraba Game Over, en lugar de quitar una vida y reiniciar el nivel.

### Solución
1. Siempre reiniciar al recibir daño: ahora se reinicia el nivel siempre que el jugador reciba daño (si tiene vidas restantes), no solo cuando muere.
2. Protección contra colisiones múltiples: se añadió el flag `isRestarting` para evitar que múltiples colisiones se procesen simultáneamente.
3. Cálculo correcto de vidas: se guardan las vidas antes del daño (`livesBefore`) y se calculan las restantes como `livesBefore - 1`.

### Comportamiento actual
- Jugador con 3 vidas recibe daño:
  - Se quita 1 vida → quedan 2
  - Se reinicia el nivel con 2 vidas
- Jugador con 2 vidas recibe daño:
  - Se quita 1 vida → queda 1
  - Se reinicia el nivel con 1 vida
- Jugador con 1 vida recibe daño:
  - Se quita 1 vida → quedan 0
  - Se muestra Game Over

### Código clave
```212:248:ghosts-goblins-SGN/js/scenes/GameScene.js
// Flag para prevenir múltiples reinicios simultáneos
this.isRestarting = false;

// Jugador con enemigos
this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
    if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
        // Guardar vidas ANTES de recibir daño
        const livesBefore = player.lives;
        
        // Prevenir múltiples colisiones
        this.isRestarting = true;
        
        // Quitar UNA vida
        player.takeDamage();
        this.lives = player.lives;
        this.updateHUD();
        
        // SIEMPRE reiniciar el nivel cuando recibe daño (si tiene vidas restantes)
        this.time.delayedCall(500, () => {
            // Las vidas restantes son las que tenía antes de recibir el daño menos 1
            const remainingLives = livesBefore - 1;
            
            if (remainingLives > 0) {
                // Reiniciar nivel con las vidas restantes
                this.scene.start('GameScene', {
                    level: this.level,
                    score: this.score,
                    lives: remainingLives
                });
            } else {
                // No hay vidas, ir a Game Over
                this.scene.start('GameOverScene', {
                    score: this.score,
                    level: this.level
                });
            }
        });
    }
});
```

Al recibir daño, se quita una vida y se reinicia el nivel desde el principio (si quedan vidas). El flag `isRestarting` evita que múltiples colisiones se procesen al mismo tiempo.

```javascript
// Flag para prevenir múltiples reinicios simultáneos
this.isRestarting = false;

// Jugador con enemigos
this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
    if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
        // Guardar vidas ANTES de recibir daño
        const livesBefore = player.lives;
        
        // Prevenir múltiples colisiones
        this.isRestarting = true;
        
        // Quitar UNA vida
        player.takeDamage();
        this.lives = player.lives;
        this.updateHUD();
        
        // SIEMPRE reiniciar el nivel cuando recibe daño (si tiene vidas restantes)
        this.time.delayedCall(500, () => {
            // Las vidas restantes son las que tenía antes de recibir el daño menos 1
            const remainingLives = livesBefore - 1;
            
            if (remainingLives > 0) {
                // Reiniciar nivel con las vidas restantes
                this.scene.start('GameScene', {
                    level: this.level,
                    score: this.score,
                    lives: remainingLives
                });
            } else {
                // No hay vidas, ir a Game Over
                this.scene.start('GameOverScene', {
                    score: this.score,
                    level: this.level
                });
            }
        });
    }
});
```

---

**User**

Ahora es muy facil que te maten, necesitaría que cada vida tuviera un porcentaje y que con cada toque de los enemigos te quitara un 10% salvo el jefe que te quita un 25%. Una vez que te quitan el 100% de una vida, entonces el protagonista tiene que empezar el nivel

---

**Cursor**

Implementando un sistema de salud por porcentaje: cada vida tiene 100% de salud; los enemigos quitan 10% por toque y el jefe 25%. El nivel se reinicia solo cuando una vida llega a 0%.


Implementando el sistema de salud por porcentaje:


```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el jugador no se desactive al salir de los límites
        this.setActive(true);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.currentHealth = 100; // Porcentaje de salud de la vida actual (0-100)
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage() {
        if (this.invulnerable || !this.isAlive) return;
        
        this.lives--;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano)
        if (this.hasArmor) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        if (this.lives <= 0) {
            this.die();
        }
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el jugador no se desactive al salir de los límites
        this.setActive(true);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.currentHealth = 100; // Porcentaje de salud de la vida actual (0-100)
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage(damagePercent = 10) {
        if (this.invulnerable || !this.isAlive) return false;
        
        // Aplicar daño al porcentaje de salud actual
        this.currentHealth -= damagePercent;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano) cuando la salud baja del 50%
        if (this.hasArmor && this.currentHealth <= 50) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        // Si la salud llega a 0 o menos, perder una vida
        if (this.currentHealth <= 0) {
            this.lives--;
            this.currentHealth = 100; // Resetear salud para la siguiente vida
            
            if (this.lives <= 0) {
                this.die();
                return true; // Indica que se perdió una vida y no hay más vidas
            }
            return true; // Indica que se perdió una vida pero aún hay más
        }
        
        return false; // No se perdió una vida completa
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el jugador no se desactive al salir de los límites
        this.setActive(true);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.currentHealth = 100; // Porcentaje de salud de la vida actual (0-100)
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage(damagePercent = 10) {
        if (this.invulnerable || !this.isAlive) return false;
        
        // Aplicar daño al porcentaje de salud actual
        this.currentHealth -= damagePercent;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano) cuando la salud baja del 50%
        if (this.hasArmor && this.currentHealth <= 50) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        // Si la salud llega a 0 o menos, perder una vida
        if (this.currentHealth <= 0) {
            this.lives--;
            this.currentHealth = 100; // Resetear salud para la siguiente vida
            
            if (this.lives <= 0) {
                this.die();
                return true; // Indica que se perdió una vida y no hay más vidas
            }
            return true; // Indica que se perdió una vida pero aún hay más
        }
        
        return false; // No se perdió una vida completa
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.currentHealth = 100; // Resetear salud al 100%
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

Actualizando `GameScene` para usar el nuevo sistema de salud:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                
                // Prevenir múltiples colisiones
                this.isRestarting = true;
                
                // Quitar UNA vida
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                
                // SIEMPRE reiniciar el nivel cuando recibe daño (si tiene vidas restantes)
                this.time.delayedCall(500, () => {
                    // Las vidas restantes son las que tenía antes de recibir el daño menos 1
                    const remainingLives = livesBefore - 1;
                    
                    if (remainingLives > 0) {
                        // Reiniciar nivel con las vidas restantes
                        this.scene.start('GameScene', {
                            level: this.level,
                            score: this.score,
                            lives: remainingLives
                        });
                    } else {
                        // No hay vidas, ir a Game Over
                        this.scene.start('GameOverScene', {
                            score: this.score,
                            level: this.level
                        });
                    }
                });
            }
        });
        
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Guardar vidas ANTES de recibir daño
                const livesBefore = player.lives;
                
                // Prevenir múltiples colisiones
                this.isRestarting = true;
                
                // Quitar UNA vida
                player.takeDamage();
                this.lives = player.lives;
                this.updateHUD();
                
                // SIEMPRE reiniciar el nivel cuando recibe daño (si tiene vidas restantes)
                this.time.delayedCall(500, () => {
                    // Las vidas restantes son las que tenía antes de recibir el daño menos 1
                    const remainingLives = livesBefore - 1;
                    
                    if (remainingLives > 0) {
                        // Reiniciar nivel con las vidas restantes
                        this.scene.start('GameScene', {
                            level: this.level,
                            score: this.score,
                            lives: remainingLives
                        });
                    } else {
                        // No hay vidas, ir a Game Over
                        this.scene.start('GameOverScene', {
                            score: this.score,
                            level: this.level
                        });
                    }
                });
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD
        const hudBg = this.add.rectangle(10, 10, 300, 100, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        this.levelText = this.add.text(20, 80, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Asegurando que el HUD se actualice correctamente. Revisando si hay que llamar a `updateHUD()` en otros lugares:


Asegurando que el HUD se inicialice correctamente al crear la escena:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 300; // 300ms entre proyectiles
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Sistema de salud por porcentaje implementado. Resumen de cambios:

## Sistema de salud por porcentaje implementado

### Cambios principales

1. Sistema de salud en `Player.js`:
   - Nueva propiedad `currentHealth` (0-100%) para cada vida
   - `takeDamage(damagePercent)` acepta un porcentaje de daño
   - Solo se pierde una vida cuando `currentHealth` llega a 0%
   - Retorna `true` si se perdió una vida completa, `false` si solo se redujo la salud

2. Daño diferenciado en `GameScene.js`:
   - Monos: 10% por toque
   - Jefe (gorila): 25% por toque
   - El nivel se reinicia solo cuando se pierde una vida completa (100%)

3. HUD mejorado:
   - Barra de salud visual que muestra el porcentaje actual
   - Color dinámico: verde (>60%), amarillo (30-60%), rojo (<30%)
   - Texto con el porcentaje exacto

### Comportamiento

- Jugador con 100% de salud recibe toque de mono:
  - Salud: 100% → 90% (no se reinicia el nivel)
- Jugador con 90% recibe toque de mono:
  - Salud: 90% → 80% (no se reinicia)
- Jugador con 20% recibe toque de jefe:
  - Salud: 20% → 0% (se pierde una vida, se reinicia el nivel)
- Jugador con 50% recibe toque de jefe:
  - Salud: 50% → 25% (no se reinicia)

### Ejemplos

- 10 toques de monos = 1 vida perdida
- 4 toques de jefe = 1 vida perdida
- 2 toques de jefe + 5 toques de mono = 1 vida perdida

El juego es más justo: puedes recibir varios toques antes de perder una vida, y el jefe es más peligroso (25% vs 10%).

---

**User**

Sigue siendo muy dificil matar al jefe ahora. Los projectiles tienen que permitir disparar más rápido si se deja pulsado el botón de disparo

---

**Cursor**

Revisando el sistema de disparo para permitir disparo continuo mientras se mantiene pulsado el botón:


Reduciendo el cooldown de disparo para permitir disparo continuo más rápido:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles
        if ((this.cursors.j.isDown || this.cursors.space.isDown) && 
            time - this.lastProjectileTime > this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Verificando que el sistema de disparo continuo funcione correctamente. Revisando cómo se pasa el tiempo:


Mejorando el sistema de disparo para que funcione mejor con disparo continuo. Ajustando la lógica:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

Sistema de disparo mejorado. Cambios:

## Sistema de disparo rápido continuo implementado

### Cambios realizados

1. Cooldown reducido:
   - Antes: 300ms entre disparos (~3.3 proyectiles/segundo)
   - Ahora: 100ms entre disparos (~10 proyectiles/segundo)

2. Lógica de disparo:
   - Disparo continuo mientras se mantiene pulsado el botón (Espacio o J)
   - El cooldown se respeta automáticamente

### Resultado

- Disparo continuo: mantén pulsado Espacio o J para disparar continuamente
- Velocidad: ~10 proyectiles por segundo (antes ~3.3)
- Facilita eliminar al jefe: más proyectiles en menos tiempo

### Uso

- Mantén pulsado Espacio o J para disparar continuamente
- Suelta el botón para dejar de disparar
- El sistema respeta el cooldown de 100ms entre disparos

Con esto, debería ser más fácil eliminar al jefe. Si quieres ajustar la velocidad, puedo cambiar el cooldown.

---

**User**

Sigue siendo muy dificil matar al jefe, la fijación hacia el protagonista en el nivel 3 es muy agresiva

---

**Cursor**

Revisando el comportamiento del jefe para reducir su agresividad, especialmente en el nivel 3, y facilitar su eliminación:


Revisando la resistencia del jefe y el daño de los proyectiles:


Revisando las constantes y ajustando la agresividad del jefe:


Ajustando la resistencia, velocidad y comportamiento del jefe para reducir su agresividad:


```javascript
// Constantes del juego
const GAME_CONFIG = {
    WIDTH: 1200,
    HEIGHT: 800,
    GRAVITY: 600,
    PLAYER_SPEED: 250,
    JUMP_VELOCITY: -500,
    PROJECTILE_SPEED: 400,
    PLAYER_LIVES: 3
};

// Puntuaciones
const SCORES = {
    MONKEY_BASIC: 100,
    MONKEY_FAST: 150,
    MONKEY_STRONG: 200,
    GORILLA_BASE: 500,
    LEVEL_COMPLETE: 1000,
    TIME_BONUS_MULTIPLIER: 10
};

// Tipos de enemigos
const ENEMY_TYPES = {
    MONKEY_BASIC: 'basic',
    MONKEY_FAST: 'fast',
    MONKEY_STRONG: 'strong'
};

// Daño y resistencia
const DAMAGE = {
    PROJECTILE: 1,
    ENEMY_TOUCH: 1
};

const RESISTANCE = {
    MONKEY_BASIC: 1,
    MONKEY_FAST: 1,
    MONKEY_STRONG: 2,
    GORILLA_LEVEL_1: 5,
    GORILLA_LEVEL_2: 7,
    GORILLA_LEVEL_3: 8  // Reducido de 12 a 8 para hacerlo más fácil de matar
};

// Velocidades de enemigos
const ENEMY_SPEEDS = {
    MONKEY_BASIC: 80,
    MONKEY_FAST: 150,
    MONKEY_STRONG: 100,
    GORILLA_LEVEL_1: 100,
    GORILLA_LEVEL_2: 150,
    GORILLA_LEVEL_3: 200
};

// Colores
const COLORS = {
    PLAYER: 0xffd700, // Amarillo (plátano)
    MONKEY: 0x8b4513, // Marrón
    GORILLA: 0x2c1810, // Marrón oscuro
    PROJECTILE: 0xffff00, // Amarillo brillante
    PLATFORM: 0x228b22, // Verde jungla
    BACKGROUND: 0x1a4d2e // Verde oscuro
};


```

```javascript
// Constantes del juego
const GAME_CONFIG = {
    WIDTH: 1200,
    HEIGHT: 800,
    GRAVITY: 600,
    PLAYER_SPEED: 250,
    JUMP_VELOCITY: -500,
    PROJECTILE_SPEED: 400,
    PLAYER_LIVES: 3
};

// Puntuaciones
const SCORES = {
    MONKEY_BASIC: 100,
    MONKEY_FAST: 150,
    MONKEY_STRONG: 200,
    GORILLA_BASE: 500,
    LEVEL_COMPLETE: 1000,
    TIME_BONUS_MULTIPLIER: 10
};

// Tipos de enemigos
const ENEMY_TYPES = {
    MONKEY_BASIC: 'basic',
    MONKEY_FAST: 'fast',
    MONKEY_STRONG: 'strong'
};

// Daño y resistencia
const DAMAGE = {
    PROJECTILE: 1,
    ENEMY_TOUCH: 1
};

const RESISTANCE = {
    MONKEY_BASIC: 1,
    MONKEY_FAST: 1,
    MONKEY_STRONG: 2,
    GORILLA_LEVEL_1: 5,
    GORILLA_LEVEL_2: 7,
    GORILLA_LEVEL_3: 8  // Reducido de 12 a 8 para hacerlo más fácil de matar
};

// Velocidades de enemigos
const ENEMY_SPEEDS = {
    MONKEY_BASIC: 80,
    MONKEY_FAST: 150,
    MONKEY_STRONG: 100,
    GORILLA_LEVEL_1: 100,
    GORILLA_LEVEL_2: 130,
    GORILLA_LEVEL_3: 130  // Reducido de 200 a 130 para hacerlo menos agresivo
};

// Colores
const COLORS = {
    PLAYER: 0xffd700, // Amarillo (plátano)
    MONKEY: 0x8b4513, // Marrón
    GORILLA: 0x2c1810, // Marrón oscuro
    PROJECTILE: 0xffff00, // Amarillo brillante
    PLATFORM: 0x228b22, // Verde jungla
    BACKGROUND: 0x1a4d2e // Verde oscuro
};


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        // Cooldown más largo para reducir agresividad, especialmente en nivel 3
        this.directionChangeCooldown = level === 3 ? 1500 : 800; // Nivel 3: 1.5s, otros: 0.8s
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar si está atascado (bloqueado y sin movimiento)
        if (this.body.blocked.left || this.body.blocked.right) {
            this.stuckTimer += time;
            
            // Si está atascado más del umbral, forzar acción inmediata
            if (this.stuckTimer > this.stuckThreshold) {
                // Forzar cambio de dirección o salto sin cooldown
                if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
                    this.jump();
                    this.stuckTimer = 0;
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido para escapar
                    this.stuckTimer = 0;
                    this.lastDirectionChange = time; // Resetear cooldown de cambio de dirección
                }
            }
        } else {
            // Si no está bloqueado, resetear contador
            this.stuckTimer = 0;
        }
        
        // Verificar obstáculos delante (más frecuentemente que los monos)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // Moverse hacia el jugador
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                // Si está atascado, ignorar el cooldown
                const canChangeDirection = (time - this.lastDirectionChange > this.directionChangeCooldown) || 
                                         (this.stuckTimer > this.stuckThreshold / 2);
                
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && canChangeDirection) {
                    // Cambiar dirección (movimiento más estratégico, pero permitir cambio si está atascado)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                    this.stuckTimer = 0; // Resetear contador al cambiar dirección
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                if (Math.random() < 0.01 && !this.pauseMovement) { // 1% de probabilidad cada frame
                    this.pauseMovement = true;
                    this.pauseTimer = time + 300 + Math.random() * 200; // Pausa de 300-500ms
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    checkObstacles(time) {
        // Verificar si estamos bloqueados por un obstáculo lateral
        if (this.body.blocked.left || this.body.blocked.right) {
            // Estamos bloqueados, decidir acción inmediata
            if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                // 50% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 50% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido
                this.stuckTimer = 0;
                this.lastDirectionChange = time; // Resetear cooldown
            }
            return; // Ya manejamos el obstáculo
        }
        
        // Verificar si estamos cerca de los bordes del mundo
        if (this.body.onFloor() && this.body.velocity.x !== 0) {
            const worldBounds = this.scene.physics.world.bounds;
            const isNearWorldEdge = (this.direction > 0 && this.x > worldBounds.width - 150) ||
                                   (this.direction < 0 && this.x < 150);
            
            if (isNearWorldEdge && Math.random() < 0.2) {
                // 20% de probabilidad de cambiar dirección cerca de los bordes
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
                this.lastDirectionChange = time;
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.8); // Salto del jefe (80% del salto del jugador)
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
            this.stuckTimer = 0; // Resetear contador al saltar
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        // Cooldown más largo para reducir agresividad, especialmente en nivel 3
        this.directionChangeCooldown = level === 3 ? 1500 : 800; // Nivel 3: 1.5s, otros: 0.8s
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar si está atascado (bloqueado y sin movimiento)
        if (this.body.blocked.left || this.body.blocked.right) {
            this.stuckTimer += time;
            
            // Si está atascado más del umbral, forzar acción inmediata
            if (this.stuckTimer > this.stuckThreshold) {
                // Forzar cambio de dirección o salto sin cooldown
                if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
                    this.jump();
                    this.stuckTimer = 0;
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido para escapar
                    this.stuckTimer = 0;
                    this.lastDirectionChange = time; // Resetear cooldown de cambio de dirección
                }
            }
        } else {
            // Si no está bloqueado, resetear contador
            this.stuckTimer = 0;
        }
        
        // Verificar obstáculos delante (más frecuentemente que los monos)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // En nivel 3, hacer el movimiento menos directo y agresivo
                const isLevel3 = this.level === 3;
                const aggressionFactor = isLevel3 ? 0.6 : 1.0; // Reducir agresividad en nivel 3
                
                // Moverse hacia el jugador, pero con menos precisión en nivel 3
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // En nivel 3, ocasionalmente ignorar el jugador para hacer movimiento más errático
                if (isLevel3 && Math.random() < 0.15) {
                    // 15% de probabilidad de moverse en dirección aleatoria en lugar de hacia el jugador
                    targetDirection = Math.random() < 0.5 ? -1 : 1;
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                // Si está atascado, ignorar el cooldown
                const canChangeDirection = (time - this.lastDirectionChange > this.directionChangeCooldown) || 
                                         (this.stuckTimer > this.stuckThreshold / 2);
                
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && canChangeDirection) {
                    // Cambiar dirección (movimiento más estratégico, pero permitir cambio si está atascado)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                    this.stuckTimer = 0; // Resetear contador al cambiar dirección
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                // En nivel 3, pausar más frecuentemente
                const pauseChance = isLevel3 ? 0.02 : 0.01;
                if (Math.random() < pauseChance && !this.pauseMovement) {
                    this.pauseMovement = true;
                    const pauseDuration = isLevel3 ? 500 + Math.random() * 300 : 300 + Math.random() * 200;
                    this.pauseTimer = time + pauseDuration; // Pausa más larga en nivel 3
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(baseSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    checkObstacles(time) {
        // Verificar si estamos bloqueados por un obstáculo lateral
        if (this.body.blocked.left || this.body.blocked.right) {
            // Estamos bloqueados, decidir acción inmediata
            if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                // 50% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 50% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido
                this.stuckTimer = 0;
                this.lastDirectionChange = time; // Resetear cooldown
            }
            return; // Ya manejamos el obstáculo
        }
        
        // Verificar si estamos cerca de los bordes del mundo
        if (this.body.onFloor() && this.body.velocity.x !== 0) {
            const worldBounds = this.scene.physics.world.bounds;
            const isNearWorldEdge = (this.direction > 0 && this.x > worldBounds.width - 150) ||
                                   (this.direction < 0 && this.x < 150);
            
            if (isNearWorldEdge && Math.random() < 0.2) {
                // 20% de probabilidad de cambiar dirección cerca de los bordes
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
                this.lastDirectionChange = time;
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.8); // Salto del jefe (80% del salto del jugador)
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
            this.stuckTimer = 0; // Resetear contador al saltar
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        // Cooldown más largo para reducir agresividad, especialmente en nivel 3
        this.directionChangeCooldown = level === 3 ? 1500 : 800; // Nivel 3: 1.5s, otros: 0.8s
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar si está atascado (bloqueado y sin movimiento)
        if (this.body.blocked.left || this.body.blocked.right) {
            this.stuckTimer += time;
            
            // Si está atascado más del umbral, forzar acción inmediata
            if (this.stuckTimer > this.stuckThreshold) {
                // Forzar cambio de dirección o salto sin cooldown
                if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
                    this.jump();
                    this.stuckTimer = 0;
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido para escapar
                    this.stuckTimer = 0;
                    this.lastDirectionChange = time; // Resetear cooldown de cambio de dirección
                }
            }
        } else {
            // Si no está bloqueado, resetear contador
            this.stuckTimer = 0;
        }
        
        // Verificar obstáculos delante (más frecuentemente que los monos)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // En nivel 3, hacer el movimiento menos directo y agresivo
                const isLevel3 = this.level === 3;
                const aggressionFactor = isLevel3 ? 0.6 : 1.0; // Reducir agresividad en nivel 3
                
                // Moverse hacia el jugador, pero con menos precisión en nivel 3
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // En nivel 3, ocasionalmente ignorar el jugador para hacer movimiento más errático
                if (isLevel3 && Math.random() < 0.15) {
                    // 15% de probabilidad de moverse en dirección aleatoria en lugar de hacia el jugador
                    targetDirection = Math.random() < 0.5 ? -1 : 1;
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                // Si está atascado, ignorar el cooldown
                const canChangeDirection = (time - this.lastDirectionChange > this.directionChangeCooldown) || 
                                         (this.stuckTimer > this.stuckThreshold / 2);
                
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && canChangeDirection) {
                    // Cambiar dirección (movimiento más estratégico, pero permitir cambio si está atascado)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                    this.stuckTimer = 0; // Resetear contador al cambiar dirección
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                // En nivel 3, pausar más frecuentemente
                const pauseChance = isLevel3 ? 0.02 : 0.01;
                if (Math.random() < pauseChance && !this.pauseMovement) {
                    this.pauseMovement = true;
                    const pauseDuration = isLevel3 ? 500 + Math.random() * 300 : 300 + Math.random() * 200;
                    this.pauseTimer = time + pauseDuration; // Pausa más larga en nivel 3
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // En nivel 3, reducir la velocidad efectiva para hacerlo menos agresivo
            const effectiveSpeed = this.level === 3 ? baseSpeed * 0.85 : baseSpeed;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(effectiveSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    checkObstacles(time) {
        // Verificar si estamos bloqueados por un obstáculo lateral
        if (this.body.blocked.left || this.body.blocked.right) {
            // Estamos bloqueados, decidir acción inmediata
            if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                // 50% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 50% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido
                this.stuckTimer = 0;
                this.lastDirectionChange = time; // Resetear cooldown
            }
            return; // Ya manejamos el obstáculo
        }
        
        // Verificar si estamos cerca de los bordes del mundo
        if (this.body.onFloor() && this.body.velocity.x !== 0) {
            const worldBounds = this.scene.physics.world.bounds;
            const isNearWorldEdge = (this.direction > 0 && this.x > worldBounds.width - 150) ||
                                   (this.direction < 0 && this.x < 150);
            
            if (isNearWorldEdge && Math.random() < 0.2) {
                // 20% de probabilidad de cambiar dirección cerca de los bordes
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
                this.lastDirectionChange = time;
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.8); // Salto del jefe (80% del salto del jugador)
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
            this.stuckTimer = 0; // Resetear contador al saltar
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Reducción de agresividad del jefe, especialmente en el nivel 3. Cambios:

## Reducción de agresividad del jefe

### Cambios realizados

1. Resistencia reducida:
   - Nivel 3: de 12 a 8 proyectiles (33% más fácil de matar)
   - Nivel 2: de 8 a 7 proyectiles

2. Velocidad reducida:
   - Nivel 3: de 200 a 130 (35% más lento)
   - Nivel 2: de 150 a 130

3. Cooldown de cambio de dirección aumentado:
   - Nivel 3: de 500ms a 1500ms (3x más lento para cambiar dirección)
   - Otros niveles: 800ms

4. Movimiento menos directo en nivel 3:
   - 15% de probabilidad de moverse aleatoriamente en lugar de perseguir directamente
   - Velocidad efectiva reducida al 85% en nivel 3
   - Pausas más frecuentes y largas (500-800ms vs 300-500ms)

5. Comportamiento más errático:
   - Pausas más frecuentes (2% vs 1% de probabilidad)
   - Pausas más largas en nivel 3

### Resultado

- Más fácil de matar: 8 proyectiles en lugar de 12 en nivel 3
- Menos agresivo: velocidad reducida y cambios de dirección más lentos
- Movimiento más predecible: pausas y movimiento errático que dan más oportunidades de disparar

El jefe debería ser más manejable, especialmente en el nivel 3, con más oportunidades para atacarlo mientras se mueve de forma menos agresiva.

---

**User**

No se oyen los sonidos

---

**Cursor**

Revisando cómo se cargan y reproducen los sonidos:


Buscando dónde se reproducen los sonidos en el juego:


Los sonidos se cargan, pero no se reproducen. Agregando la lógica para reproducirlos en los eventos correspondientes:


Agregando funciones para reproducir sonidos en los eventos correspondientes:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            if (monkey.takeDamage()) {
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     */
    playSound(key, volume = 0.5) {
        try {
            if (this.cache.audio.exists(key)) {
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5); // Sonido de muerte de enemigo
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            if (gorilla.takeDamage()) {
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     */
    playSound(key, volume = 0.5) {
        try {
            if (this.cache.audio.exists(key)) {
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5); // Sonido de muerte de enemigo
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6); // Sonido de muerte de jefe
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     */
    playSound(key, volume = 0.5) {
        try {
            if (this.cache.audio.exists(key)) {
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5); // Sonido de muerte de enemigo
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6); // Sonido de muerte de jefe
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5); // Sonido de daño al jugador
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     */
    playSound(key, volume = 0.5) {
        try {
            if (this.cache.audio.exists(key)) {
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5); // Sonido de muerte de enemigo
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6); // Sonido de muerte de jefe
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5); // Sonido de daño al jugador
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6); // Sonido de daño al jugador (más fuerte para jefe)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     */
    playSound(key, volume = 0.5) {
        try {
            if (this.cache.audio.exists(key)) {
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5); // Sonido de muerte de enemigo
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6); // Sonido de muerte de jefe
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5); // Sonido de daño al jugador
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6); // Sonido de daño al jugador (más fuerte para jefe)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     */
    playSound(key, volume = 0.5) {
        try {
            if (this.cache.audio.exists(key)) {
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                this.playSound('sfx_throw', 0.3); // Sonido de lanzar proyectil (volumen bajo para disparo rápido)
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el jugador no se desactive al salir de los límites
        this.setActive(true);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.currentHealth = 100; // Porcentaje de salud de la vida actual (0-100)
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
            // Reproducir sonido de salto
            try {
                if (this.scene.cache.audio.exists('sfx_jump')) {
                    this.scene.sound.play('sfx_jump', { volume: 0.4 });
                }
            } catch (e) {
                // Silenciar errores si el sonido no existe
            }
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage(damagePercent = 10) {
        if (this.invulnerable || !this.isAlive) return false;
        
        // Aplicar daño al porcentaje de salud actual
        this.currentHealth -= damagePercent;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano) cuando la salud baja del 50%
        if (this.hasArmor && this.currentHealth <= 50) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        // Si la salud llega a 0 o menos, perder una vida
        if (this.currentHealth <= 0) {
            this.lives--;
            this.currentHealth = 100; // Resetear salud para la siguiente vida
            
            if (this.lives <= 0) {
                this.die();
                return true; // Indica que se perdió una vida y no hay más vidas
            }
            return true; // Indica que se perdió una vida pero aún hay más
        }
        
        return false; // No se perdió una vida completa
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.currentHealth = 100; // Resetear salud al 100%
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

Agregando sonidos en otras escenas y el rugido del jefe:


Agregando sonidos en las escenas y el rugido del jefe:


```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        // Cooldown más largo para reducir agresividad, especialmente en nivel 3
        this.directionChangeCooldown = level === 3 ? 1500 : 800; // Nivel 3: 1.5s, otros: 0.8s
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar si está atascado (bloqueado y sin movimiento)
        if (this.body.blocked.left || this.body.blocked.right) {
            this.stuckTimer += time;
            
            // Si está atascado más del umbral, forzar acción inmediata
            if (this.stuckTimer > this.stuckThreshold) {
                // Forzar cambio de dirección o salto sin cooldown
                if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
                    this.jump();
                    this.stuckTimer = 0;
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido para escapar
                    this.stuckTimer = 0;
                    this.lastDirectionChange = time; // Resetear cooldown de cambio de dirección
                }
            }
        } else {
            // Si no está bloqueado, resetear contador
            this.stuckTimer = 0;
        }
        
        // Verificar obstáculos delante (más frecuentemente que los monos)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // En nivel 3, hacer el movimiento menos directo y agresivo
                const isLevel3 = this.level === 3;
                const aggressionFactor = isLevel3 ? 0.6 : 1.0; // Reducir agresividad en nivel 3
                
                // Moverse hacia el jugador, pero con menos precisión en nivel 3
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // En nivel 3, ocasionalmente ignorar el jugador para hacer movimiento más errático
                if (isLevel3 && Math.random() < 0.15) {
                    // 15% de probabilidad de moverse en dirección aleatoria en lugar de hacia el jugador
                    targetDirection = Math.random() < 0.5 ? -1 : 1;
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                // Si está atascado, ignorar el cooldown
                const canChangeDirection = (time - this.lastDirectionChange > this.directionChangeCooldown) || 
                                         (this.stuckTimer > this.stuckThreshold / 2);
                
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && canChangeDirection) {
                    // Cambiar dirección (movimiento más estratégico, pero permitir cambio si está atascado)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                    this.stuckTimer = 0; // Resetear contador al cambiar dirección
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                // En nivel 3, pausar más frecuentemente
                const pauseChance = isLevel3 ? 0.02 : 0.01;
                if (Math.random() < pauseChance && !this.pauseMovement) {
                    this.pauseMovement = true;
                    const pauseDuration = isLevel3 ? 500 + Math.random() * 300 : 300 + Math.random() * 200;
                    this.pauseTimer = time + pauseDuration; // Pausa más larga en nivel 3
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // En nivel 3, reducir la velocidad efectiva para hacerlo menos agresivo
            const effectiveSpeed = this.level === 3 ? baseSpeed * 0.85 : baseSpeed;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(effectiveSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    checkObstacles(time) {
        // Verificar si estamos bloqueados por un obstáculo lateral
        if (this.body.blocked.left || this.body.blocked.right) {
            // Estamos bloqueados, decidir acción inmediata
            if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                // 50% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 50% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido
                this.stuckTimer = 0;
                this.lastDirectionChange = time; // Resetear cooldown
            }
            return; // Ya manejamos el obstáculo
        }
        
        // Verificar si estamos cerca de los bordes del mundo
        if (this.body.onFloor() && this.body.velocity.x !== 0) {
            const worldBounds = this.scene.physics.world.bounds;
            const isNearWorldEdge = (this.direction > 0 && this.x > worldBounds.width - 150) ||
                                   (this.direction < 0 && this.x < 150);
            
            if (isNearWorldEdge && Math.random() < 0.2) {
                // 20% de probabilidad de cambiar dirección cerca de los bordes
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
                this.lastDirectionChange = time;
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.8); // Salto del jefe (80% del salto del jugador)
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
            this.stuckTimer = 0; // Resetear contador al saltar
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Reproducir rugido del jefe
            try {
                if (this.scene.cache.audio.exists('sfx_boss_roar')) {
                    this.scene.sound.play('sfx_boss_roar', { volume: 0.5 });
                }
            } catch (e) {
                // Silenciar errores si el sonido no existe
            }
            
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

```javascript
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria
        this.playSound('sfx_victory', 0.6);
        
        // Calcular bonus de tiempo
        const elapsedTime = Date.now() - this.startTime;
        const timeBonus = Math.max(0, Math.floor((300000 - elapsedTime) / 1000) * SCORES.TIME_BONUS_MULTIPLIER);
        this.score += timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over
        this.playSound('sfx_game_over', 0.6);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
```

Revisando el método completo y agregando sonidos en las otras escenas:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5); // Sonido de muerte de enemigo
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6); // Sonido de muerte de jefe
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5); // Sonido de daño al jugador
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6); // Sonido de daño al jugador (más fuerte para jefe)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     */
    playSound(key, volume = 0.5) {
        try {
            if (this.cache.audio.exists(key)) {
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                this.playSound('sfx_throw', 0.3); // Sonido de lanzar proyectil (volumen bajo para disparo rápido)
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria
        this.playSound('sfx_victory', 0.6);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over
        this.playSound('sfx_game_over', 0.6);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class VictoryScene extends Phaser.Scene {
    constructor() {
        super({ key: 'VictoryScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.timeBonus = data.timeBonus || 0;
    }
    
    create() {
        // Reproducir música de victoria
        try {
            if (this.cache.audio.exists('victory_music')) {
                this.music = this.sound.add('victory_music', { loop: false, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x001a00)
            .setOrigin(0, 0);
        
        // Título Victoria
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, '¡NIVEL COMPLETADO!', {
            fontSize: '56px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información del nivel
        const infoStyle = {
            fontSize: '28px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 280, `Nivel ${this.level}`, {
            ...infoStyle,
            fontSize: '36px',
            fill: '#ffff00'
        }).setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 340, `Puntuación: ${this.score}`, infoStyle)
            .setOrigin(0.5);
        
        if (this.timeBonus > 0) {
            this.add.text(GAME_CONFIG.WIDTH / 2, 380, `Bonus de Tiempo: +${this.timeBonus}`, {
                ...infoStyle,
                fill: '#00ff00'
            }).setOrigin(0.5);
        }
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 420, `Vidas Restantes: ${this.lives}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Siguiente Nivel (solo si no es el nivel 4)
        if (this.level < 4) {
            const nextButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 350, 80, 0x228b22)
                .setInteractive({ useHandCursor: true })
                .on('pointerdown', () => {
                    this.scene.start('GameScene', {
                        level: this.level + 1,
                        score: this.score,
                        lives: this.lives
                    });
                })
                .on('pointerover', () => {
                    nextButton.setFillStyle(0x32cd32);
                })
                .on('pointerout', () => {
                    nextButton.setFillStyle(0x228b22);
                });
            
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'SIGUIENTE NIVEL', {
                fontSize: '36px',
                fontFamily: 'Arial',
                fill: '#ffffff',
                stroke: '#000000',
                strokeThickness: 2
            }).setOrigin(0.5);
        } else {
            // Si es el nivel 4, mostrar mensaje de victoria final
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, '¡JUEGO COMPLETADO!', {
                fontSize: '40px',
                fontFamily: 'Arial',
                fill: '#ffd700',
                stroke: '#000000',
                strokeThickness: 3
            }).setOrigin(0.5);
        }
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 350, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Efecto de celebración
        this.createCelebrationEffect();
    }
    
    createCelebrationEffect() {
        // Partículas de celebración
        for (let i = 0; i < 20; i++) {
            const particle = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                5,
                0xffd700
            );
            
            this.tweens.add({
                targets: particle,
                y: particle.y - 200,
                alpha: 0,
                duration: 2000 + Math.random() * 1000,
                delay: Math.random() * 500,
                repeat: -1
            });
        }
    }
}


```

```javascript
class GameOverScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameOverScene' });
    }
    
    init(data) {
        this.finalScore = data.score || 0;
        this.level = data.level || 1;
    }
    
    create() {
        // Reproducir sonido de game over (si no se reprodujo antes)
        try {
            if (this.cache.audio.exists('sfx_game_over')) {
                this.sound.play('sfx_game_over', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x1a0000)
            .setOrigin(0, 0);
        
        // Título Game Over
        this.add.text(GAME_CONFIG.WIDTH / 2, 200, 'GAME OVER', {
            fontSize: '72px',
            fontFamily: 'Arial',
            fill: '#ff0000',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información
        const infoStyle = {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 350, `Puntuación Final: ${this.finalScore}`, infoStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, `Nivel Alcanzado: ${this.level}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Reintentar
        const retryButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                retryButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                retryButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'REINTENTAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 300, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
    }
}


```

```javascript
class MenuScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MenuScene' });
    }
    
    create() {
        // Reproducir música de menú
        try {
            if (this.cache.audio.exists('menu_music')) {
                this.music = this.sound.add('menu_music', { loop: true, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
        
        // Fondo - usar imagen si existe, sino usar color sólido
        if (this.textures.exists('menu_bg')) {
            this.add.image(0, 0, 'menu_bg').setOrigin(0, 0);
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Título del juego
        const titleStyle = {
            fontSize: '64px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5,
                stroke: true,
                fill: true
            }
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, 'PLÁTANO BORRACHO', titleStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 220, 'VS MONOS', {
            ...titleStyle,
            fontSize: '48px'
        }).setOrigin(0.5);
        
        // Botón Jugar
        const playButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 400, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                playButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                playButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, 'JUGAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Instrucciones
        const instructionsStyle = {
            fontSize: '20px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        const instructions = [
            'CONTROLES:',
            'A/D o ←/→ : Mover',
            'W o ↑ : Saltar',
            'J o Espacio : Lanzar proyectil',
            '',
            'Derrota a los monos y enfrenta',
            'a los poderosos gorilas jefe!'
        ];
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, instructions, instructionsStyle)
            .setOrigin(0.5);
        
        // Efecto de fondo animado (monos moviéndose)
        this.createAnimatedBackground();
    }
    
    createAnimatedBackground() {
        // Crear algunos monos decorativos en el fondo
        for (let i = 0; i < 5; i++) {
            const monkey = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                20,
                COLORS.MONKEY,
                0.3
            );
            
            this.tweens.add({
                targets: monkey,
                x: Math.random() * GAME_CONFIG.WIDTH,
                y: Math.random() * GAME_CONFIG.HEIGHT,
                duration: 3000 + Math.random() * 2000,
                repeat: -1,
                yoyo: true
            });
        }
    }
}


```

Sistema de sonidos implementado. Resumen:

## Sistema de sonidos implementado

### Sonidos agregados

1. Efectos de sonido durante el juego:
   - Salto del jugador (`sfx_jump`) - al saltar
   - Lanzar proyectil (`sfx_throw`) - al disparar
   - Impacto (`sfx_hit`) - cuando un proyectil impacta
   - Muerte de enemigo (`sfx_enemy_death`) - cuando un enemigo muere
   - Daño al jugador (`sfx_player_hurt`) - cuando el jugador recibe daño
   - Rugido del jefe (`sfx_boss_roar`) - cuando el jefe ataca

2. Música de fondo:
   - Música del juego (`game_music`) - durante el juego (loop)
   - Música de menú (`menu_music`) - en el menú principal (loop)
   - Música de victoria (`victory_music`) - en la pantalla de victoria

3. Sonidos de eventos:
   - Victoria (`sfx_victory`) - al completar un nivel
   - Game Over (`sfx_game_over`) - cuando el juego termina

### Características

- Reproducción segura: verifica si el archivo existe antes de reproducirlo
- Sin errores: si los archivos no existen, el juego continúa sin problemas
- Volúmenes ajustados: diferentes volúmenes según el tipo de sonido
- Música con loop: la música de fondo se reproduce en bucle

### Funciones agregadas

- `playSound(key, volume)`: reproduce un efecto de sonido
- `playMusic(key, loop)`: reproduce música de fondo
- `stopMusic()`: detiene la música de fondo

El juego ahora reproduce sonidos cuando están disponibles. Si los archivos de audio no existen, el juego funciona sin errores.

---

**User**

se sobre ponen los sonidos

---

**Cursor**

Evitando que los sonidos se superpongan, especialmente los que se reproducen con frecuencia (disparo, salto). Implementando un sistema de gestión de sonidos:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5); // Sonido de muerte de enemigo
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6); // Sonido de muerte de jefe
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5); // Sonido de daño al jugador
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6); // Sonido de daño al jugador (más fuerte para jefe)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                this.playSound('sfx_throw', 0.3); // Sonido de lanzar proyectil (volumen bajo para disparo rápido)
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria
        this.playSound('sfx_victory', 0.6);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over
        this.playSound('sfx_game_over', 0.6);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5); // Sonido de muerte de enemigo
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4); // Sonido de impacto
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6); // Sonido de muerte de jefe
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5); // Sonido de daño al jugador
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6); // Sonido de daño al jugador (más fuerte para jefe)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria
        this.playSound('sfx_victory', 0.6);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over
        this.playSound('sfx_game_over', 0.6);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5); // Sonido de daño al jugador
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6); // Sonido de daño al jugador (más fuerte para jefe)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria
        this.playSound('sfx_victory', 0.6);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over
        this.playSound('sfx_game_over', 0.6);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6); // Sonido de daño al jugador (más fuerte para jefe)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria
        this.playSound('sfx_victory', 0.6);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over
        this.playSound('sfx_game_over', 0.6);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria
        this.playSound('sfx_victory', 0.6);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over
        this.playSound('sfx_game_over', 0.6);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria (sin sobreposición)
        this.playSound('sfx_victory', 0.6, false);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over
        this.playSound('sfx_game_over', 0.6);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria (sin sobreposición)
        this.playSound('sfx_victory', 0.6, false);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over (sin sobreposición)
        this.playSound('sfx_game_over', 0.6, false);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class Player extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        if (scene.textures.exists('player')) {
            super(scene, x, y, 'player');
        } else {
            // Crear sprite del plátano borracho programáticamente
            const graphics = scene.add.graphics();
            graphics.fillStyle(COLORS.PLAYER);
            graphics.fillRoundedRect(0, 0, 40, 60, 10);
            // Ojos
            graphics.fillStyle(0x000000);
            graphics.fillCircle(10, 20, 4);
            graphics.fillCircle(30, 20, 4);
            // Boca
            graphics.fillStyle(0xff6347);
            graphics.fillEllipse(20, 35, 15, 10);
            const texture = graphics.generateTexture('player', 40, 60);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el jugador no se desactive al salir de los límites
        this.setActive(true);
        
        // Propiedades del jugador
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.currentHealth = 100; // Porcentaje de salud de la vida actual (0-100)
        this.isAlive = true;
        this.facingRight = true;
        this.canJump = true;
        this.isJumping = false;
        this.invulnerable = false;
        this.hasArmor = true; // Piel del plátano
        
        // Configurar física
        this.body.setSize(35, 55);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        
        // Efecto de tambaleo borracho
        this.drunkOffset = 0;
        this.drunkSpeed = 0.05;
        
        // Tiempo de invulnerabilidad después de recibir daño
        this.invulnerableTime = 0;
        this.invulnerableDuration = 2000; // 2 segundos
        
        // Referencia a la escena para crear proyectiles
        this.scene = scene;
    }
    
    update(cursors, time) {
        if (!this.isAlive) return;
        
        // Actualizar invulnerabilidad
        if (this.invulnerable) {
            this.invulnerableTime -= time;
            if (this.invulnerableTime <= 0) {
                this.invulnerable = false;
                this.setAlpha(1);
            } else {
                // Efecto de parpadeo
                this.setAlpha((Math.sin(time * 0.02) + 1) / 2);
            }
        }
        
        // Movimiento horizontal
        let horizontalVelocity = 0;
        
        if (cursors.left.isDown || cursors.a.isDown) {
            horizontalVelocity = -GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = false;
        } else if (cursors.right.isDown || cursors.d.isDown) {
            horizontalVelocity = GAME_CONFIG.PLAYER_SPEED;
            this.facingRight = true;
        }
        
        this.setVelocityX(horizontalVelocity);
        
        // Salto (solo W o flecha arriba, NO espacio)
        if ((cursors.up.isDown || cursors.w.isDown) && this.canJump && !this.isJumping) {
            this.jump();
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Efecto de tambaleo borracho (solo visual - rotación)
        this.drunkOffset += this.drunkSpeed;
        const drunkRotation = Math.sin(this.drunkOffset) * 0.12; // Rotación suave
        this.setRotation(drunkRotation);
    }
    
    jump() {
        if (this.canJump && !this.isJumping) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY);
            this.canJump = false;
            this.isJumping = true;
            // Reproducir sonido de salto (evitar sobreposición)
            try {
                if (this.scene.cache.audio.exists('sfx_jump')) {
                    // Detener el sonido anterior si está reproduciéndose
                    if (this.scene.sound.sounds) {
                        this.scene.sound.sounds.forEach(sound => {
                            if (sound.key === 'sfx_jump' && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                    this.scene.sound.play('sfx_jump', { volume: 0.4 });
                }
            } catch (e) {
                // Silenciar errores si el sonido no existe
            }
        }
    }
    
    throwProjectile() {
        if (!this.isAlive) return null;
        
        const direction = this.facingRight ? 1 : -1;
        const offsetX = this.facingRight ? 30 : -30;
        const projectile = new Projectile(this.scene, this.x + offsetX, this.y, direction);
        
        return projectile;
    }
    
    takeDamage(damagePercent = 10) {
        if (this.invulnerable || !this.isAlive) return false;
        
        // Aplicar daño al porcentaje de salud actual
        this.currentHealth -= damagePercent;
        this.invulnerable = true;
        this.invulnerableTime = this.invulnerableDuration;
        
        // Perder armadura (piel del plátano) cuando la salud baja del 50%
        if (this.hasArmor && this.currentHealth <= 50) {
            this.hasArmor = false;
            // Cambiar color a más claro (plátano sin piel)
            this.setTint(0xffffcc);
        }
        
        // Si la salud llega a 0 o menos, perder una vida
        if (this.currentHealth <= 0) {
            this.lives--;
            this.currentHealth = 100; // Resetear salud para la siguiente vida
            
            if (this.lives <= 0) {
                this.die();
                return true; // Indica que se perdió una vida y no hay más vidas
            }
            return true; // Indica que se perdió una vida pero aún hay más
        }
        
        return false; // No se perdió una vida completa
    }
    
    die() {
        this.isAlive = false;
        this.setVelocity(0, 0);
        this.setTint(0x666666); // Gris cuando muere
    }
    
    reset(x, y) {
        this.setPosition(x, y);
        this.lives = GAME_CONFIG.PLAYER_LIVES;
        this.currentHealth = 100; // Resetear salud al 100%
        this.isAlive = true;
        this.hasArmor = true;
        this.invulnerable = false;
        this.setAlpha(1);
        this.clearTint();
        this.setVelocity(0, 0);
    }
}


```

```javascript
class Gorilla extends Phaser.Physics.Arcade.Sprite {
    constructor(scene, x, y, level = 1) {
        // Intentar usar sprite cargado, si no existe, generar programáticamente
        const spriteKey = `gorilla_${level}`;
        
        if (scene.textures.exists(spriteKey)) {
            super(scene, x, y, spriteKey);
        } else {
            // Crear sprite del gorila programáticamente
            const graphics = scene.add.graphics();
            const size = 80 + (level * 10); // Más grande según el nivel
            
            graphics.fillStyle(COLORS.GORILLA);
            graphics.fillRoundedRect(0, 0, size, size, 15);
            // Ojos rojos (más agresivos)
            graphics.fillStyle(0xff0000);
            graphics.fillCircle(size / 2 - 15, size / 2 - 10, 8);
            graphics.fillCircle(size / 2 + 15, size / 2 - 10, 8);
            // Boca
            graphics.fillStyle(0x000000);
            graphics.fillEllipse(size / 2, size / 2 + 15, 20, 15);
            // Pecho más claro
            graphics.fillStyle(0x3d2817);
            graphics.fillEllipse(size / 2, size / 2 + 5, 30, 25);
            
            const texture = graphics.generateTexture(spriteKey, size, size);
            graphics.destroy();
            
            super(scene, x, y, texture);
        }
        
        scene.add.existing(this);
        scene.physics.add.existing(this);
        
        // Asegurar que el gorila no se desactive al salir de los límites
        this.setActive(true);
        
        this.level = level;
        this.isAlive = true;
        this.direction = 0; // Empieza quieto, se moverá cuando detecte al jugador
        this.resistance = this.getResistance();
        this.maxResistance = this.resistance;
        this.speed = this.getSpeed();
        this.attackCooldown = 0;
        this.attackInterval = 2000 + (level * 500); // Más rápido en niveles altos
        
        // Comportamiento inteligente del jefe
        this.detectionRange = 400; // Rango de detección más amplio que los monos
        this.lastDirectionChange = 0;
        // Cooldown más largo para reducir agresividad, especialmente en nivel 3
        this.directionChangeCooldown = level === 3 ? 1500 : 800; // Nivel 3: 1.5s, otros: 0.8s
        this.pauseMovement = false;
        this.pauseTimer = 0;
        
        // Capacidad de salto y detección de obstáculos (similar a los monos pero más inteligente)
        this.canJump = true;
        this.isJumping = false;
        this.lastObstacleCheck = 0;
        this.obstacleCheckCooldown = 150; // Verificar obstáculos más frecuentemente que los monos
        this.jumpCooldown = 0;
        this.minJumpCooldown = 800; // Mínimo 0.8 segundos entre saltos
        this.stuckTimer = 0; // Tiempo que lleva atascado
        this.stuckThreshold = 500; // Si está atascado más de 500ms, forzar acción
        
        // Configurar física - usar el tamaño real del sprite
        const spriteSize = this.width || size;
        this.body.setSize(spriteSize - 10, spriteSize - 10);
        // Activar colisiones con todos los bordes del mundo
        this.body.setCollideWorldBounds(true);
        this.body.checkCollision.left = true; // Colisión con borde izquierdo
        this.body.checkCollision.right = true; // Colisión con borde derecho
        this.body.checkCollision.up = true;
        this.body.checkCollision.down = true; // Colisión con límite inferior
        this.body.setGravityY(GAME_CONFIG.GRAVITY);
        this.body.setVelocityX(this.speed * this.direction);
        
        // Barra de vida
        this.createHealthBar(scene);
        
        // Cambiar dirección o saltar al tocar bordes
        this.body.onWorldBounds = true;
        scene.physics.world.on('worldbounds', (event) => {
            if (event.gameObject === this && (event.body.blocked.left || event.body.blocked.right)) {
                // Decidir si saltar o cambiar de dirección (50% saltar, 50% cambiar dirección)
                if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                    this.jump();
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction);
                    this.stuckTimer = 0; // Resetear contador de atascado
                }
            }
        });
    }
    
    getResistance() {
        switch (this.level) {
            case 1:
                return RESISTANCE.GORILLA_LEVEL_1;
            case 2:
                return RESISTANCE.GORILLA_LEVEL_2;
            case 3:
                return RESISTANCE.GORILLA_LEVEL_3;
            default:
                return RESISTANCE.GORILLA_LEVEL_1 + (this.level - 1) * 3;
        }
    }
    
    getSpeed() {
        switch (this.level) {
            case 1:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1;
            case 2:
                return ENEMY_SPEEDS.GORILLA_LEVEL_2;
            case 3:
                return ENEMY_SPEEDS.GORILLA_LEVEL_3;
            default:
                return ENEMY_SPEEDS.GORILLA_LEVEL_1 + (this.level - 1) * 50;
        }
    }
    
    createHealthBar(scene) {
        // Fondo de la barra
        this.healthBarBg = scene.add.rectangle(this.x, this.y - 60, 100, 10, 0x000000);
        this.healthBarBg.setDepth(100);
        this.healthBarBg.setScrollFactor(0);
        
        // Barra de vida
        this.healthBar = scene.add.rectangle(this.x, this.y - 60, 100, 8, 0xff0000);
        this.healthBar.setDepth(101);
        this.healthBar.setScrollFactor(0);
    }
    
    update(player, time) {
        if (!this.isAlive) return;
        
        // Actualizar posición de la barra de vida
        this.healthBarBg.setPosition(this.x, this.y - 60);
        this.healthBar.setPosition(this.x - 50 + (this.resistance / this.maxResistance * 50), this.y - 60);
        this.healthBar.width = (this.resistance / this.maxResistance) * 100;
        
        // Actualizar cooldowns
        if (this.jumpCooldown > 0) {
            this.jumpCooldown -= time;
        }
        
        // Verificar si está en el suelo
        if (this.body.onFloor()) {
            this.canJump = true;
            this.isJumping = false;
        } else {
            this.isJumping = true;
        }
        
        // Detectar si está atascado (bloqueado y sin movimiento)
        if (this.body.blocked.left || this.body.blocked.right) {
            this.stuckTimer += time;
            
            // Si está atascado más del umbral, forzar acción inmediata
            if (this.stuckTimer > this.stuckThreshold) {
                // Forzar cambio de dirección o salto sin cooldown
                if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
                    this.jump();
                    this.stuckTimer = 0;
                } else {
                    this.direction *= -1;
                    this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido para escapar
                    this.stuckTimer = 0;
                    this.lastDirectionChange = time; // Resetear cooldown de cambio de dirección
                }
            }
        } else {
            // Si no está bloqueado, resetear contador
            this.stuckTimer = 0;
        }
        
        // Verificar obstáculos delante (más frecuentemente que los monos)
        if (time - this.lastObstacleCheck > this.obstacleCheckCooldown) {
            this.checkObstacles(time);
            this.lastObstacleCheck = time;
        }
        
        // Comportamiento similar a los monos pero más complejo
        let targetDirection = this.direction;
        
        if (player && player.isAlive) {
            const distanceToPlayer = Phaser.Math.Distance.Between(
                this.x, this.y, player.x, player.y
            );
            
            // Si el jugador está dentro del rango de detección
            if (distanceToPlayer < this.detectionRange) {
                // En nivel 3, hacer el movimiento menos directo y agresivo
                const isLevel3 = this.level === 3;
                const aggressionFactor = isLevel3 ? 0.6 : 1.0; // Reducir agresividad en nivel 3
                
                // Moverse hacia el jugador, pero con menos precisión en nivel 3
                if (player.x > this.x + 30) {
                    targetDirection = 1;
                } else if (player.x < this.x - 30) {
                    targetDirection = -1;
                } else {
                    // Si el jugador está muy cerca, mantener dirección actual o elegir una
                    targetDirection = this.direction !== 0 ? this.direction : (player.x > this.x ? 1 : -1);
                }
                
                // En nivel 3, ocasionalmente ignorar el jugador para hacer movimiento más errático
                if (isLevel3 && Math.random() < 0.15) {
                    // 15% de probabilidad de moverse en dirección aleatoria en lugar de hacia el jugador
                    targetDirection = Math.random() < 0.5 ? -1 : 1;
                }
                
                // Establecer dirección si no tiene una o cambiar si es necesario
                // Si está atascado, ignorar el cooldown
                const canChangeDirection = (time - this.lastDirectionChange > this.directionChangeCooldown) || 
                                         (this.stuckTimer > this.stuckThreshold / 2);
                
                if (this.direction === 0) {
                    // Primera vez que detecta al jugador
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                } else if (targetDirection !== this.direction && canChangeDirection) {
                    // Cambiar dirección (movimiento más estratégico, pero permitir cambio si está atascado)
                    this.direction = targetDirection;
                    this.lastDirectionChange = time;
                    this.stuckTimer = 0; // Resetear contador al cambiar dirección
                }
                
                // Ocasionalmente pausar el movimiento para hacer el jefe más impredecible
                // En nivel 3, pausar más frecuentemente
                const pauseChance = isLevel3 ? 0.02 : 0.01;
                if (Math.random() < pauseChance && !this.pauseMovement) {
                    this.pauseMovement = true;
                    const pauseDuration = isLevel3 ? 500 + Math.random() * 300 : 300 + Math.random() * 200;
                    this.pauseTimer = time + pauseDuration; // Pausa más larga en nivel 3
                }
            } else {
                // Si el jugador está lejos, quedarse quieto o patrullar suavemente
                if (this.direction !== 0 && time - this.lastDirectionChange > this.directionChangeCooldown * 2) {
                    // Ocasionalmente cambiar dirección cuando el jugador está lejos
                    if (Math.random() < 0.2) {
                        this.direction *= -1;
                        this.lastDirectionChange = time;
                    } else if (Math.random() < 0.1) {
                        // Ocasionalmente detenerse completamente
                        this.direction = 0;
                        this.lastDirectionChange = time;
                    }
                }
            }
        }
        
        // Manejar pausa de movimiento
        if (this.pauseMovement) {
            if (time > this.pauseTimer) {
                this.pauseMovement = false;
            } else {
                this.body.setVelocityX(0);
                // Voltear sprite según dirección
                if (this.direction > 0) {
                    this.setFlipX(false);
                } else {
                    this.setFlipX(true);
                }
                return; // No hacer nada más durante la pausa
            }
        }
        
        // Aplicar velocidad con variación para hacer el movimiento más complejo
        // Solo moverse si hay una dirección válida
        if (this.direction !== 0) {
            const baseSpeed = this.speed * this.direction;
            // En nivel 3, reducir la velocidad efectiva para hacerlo menos agresivo
            const effectiveSpeed = this.level === 3 ? baseSpeed * 0.85 : baseSpeed;
            // Agregar variación de velocidad ocasional (aceleraciones/desaceleraciones)
            const speedVariation = Math.sin(time * 0.002) * (this.speed * 0.2);
            this.body.setVelocityX(effectiveSpeed + speedVariation);
        } else {
            // Si no hay dirección, quedarse quieto
            this.body.setVelocityX(0);
        }
        
        // Voltear sprite según dirección
        if (this.direction > 0) {
            this.setFlipX(false);
        } else {
            this.setFlipX(true);
        }
        
        // Ataque periódico
        this.attackCooldown -= time;
        if (this.attackCooldown <= 0 && player && player.isAlive) {
            this.attack(player);
            this.attackCooldown = this.attackInterval;
        }
    }
    
    checkObstacles(time) {
        // Verificar si estamos bloqueados por un obstáculo lateral
        if (this.body.blocked.left || this.body.blocked.right) {
            // Estamos bloqueados, decidir acción inmediata
            if (this.canJump && !this.isJumping && Math.random() < 0.5 && this.jumpCooldown <= 0) {
                // 50% de probabilidad de saltar sobre el obstáculo
                this.jump();
            } else {
                // 50% de probabilidad de cambiar de dirección
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction * 1.2); // Movimiento más rápido
                this.stuckTimer = 0;
                this.lastDirectionChange = time; // Resetear cooldown
            }
            return; // Ya manejamos el obstáculo
        }
        
        // Verificar si estamos cerca de los bordes del mundo
        if (this.body.onFloor() && this.body.velocity.x !== 0) {
            const worldBounds = this.scene.physics.world.bounds;
            const isNearWorldEdge = (this.direction > 0 && this.x > worldBounds.width - 150) ||
                                   (this.direction < 0 && this.x < 150);
            
            if (isNearWorldEdge && Math.random() < 0.2) {
                // 20% de probabilidad de cambiar dirección cerca de los bordes
                this.direction *= -1;
                this.body.setVelocityX(this.speed * this.direction);
                this.lastDirectionChange = time;
            }
        }
    }
    
    jump() {
        if (this.canJump && !this.isJumping && this.jumpCooldown <= 0) {
            this.setVelocityY(GAME_CONFIG.JUMP_VELOCITY * 0.8); // Salto del jefe (80% del salto del jugador)
            this.canJump = false;
            this.isJumping = true;
            this.jumpCooldown = this.minJumpCooldown;
            this.stuckTimer = 0; // Resetear contador al saltar
        }
    }
    
    attack(player) {
        // Ataque más complejo: carga hacia el jugador
        if (player && player.isAlive) {
            // Reproducir rugido del jefe (evitar sobreposición)
            try {
                if (this.scene.cache.audio.exists('sfx_boss_roar')) {
                    // Detener el rugido anterior si está reproduciéndose
                    if (this.scene.sound.sounds) {
                        this.scene.sound.sounds.forEach(sound => {
                            if (sound.key === 'sfx_boss_roar' && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                    this.scene.sound.play('sfx_boss_roar', { volume: 0.5 });
                }
            } catch (e) {
                // Silenciar errores si el sonido no existe
            }
            
            // Determinar dirección hacia el jugador
            if (player.x > this.x) {
                this.direction = 1;
            } else {
                this.direction = -1;
            }
            // Carga rápida hacia el jugador
            this.body.setVelocityX(this.speed * this.direction * 1.3);
            // La carga dura un tiempo limitado (se resetea en el siguiente update)
        }
    }
    
    takeDamage() {
        this.resistance--;
        if (this.resistance <= 0) {
            this.die();
            return true; // Gorila derrotado
        }
        return false; // Aún vivo
    }
    
    die() {
        this.isAlive = false;
        // Destruir barra de vida
        if (this.healthBar) this.healthBar.destroy();
        if (this.healthBarBg) this.healthBarBg.destroy();
        
        // Efecto visual de muerte
        this.setTint(0xff0000);
        this.scene.time.delayedCall(200, () => {
            this.destroy();
        });
    }
    
    getScore() {
        return SCORES.GORILLA_BASE * this.level;
    }
}


```

Sistema implementado para evitar que los sonidos se sobrepongan. Cambios:

## Sistema anti-sobreposición de sonidos

### Cambios realizados

1. Función `playSound` mejorada:
   - Nuevo parámetro `allowOverlap` (por defecto `false`)
   - Si `allowOverlap` es `false`, detiene cualquier instancia anterior del mismo sonido antes de reproducir uno nuevo
   - Evita acumulación de sonidos

2. Configuración por tipo de sonido:
   - Disparo (`sfx_throw`): permite sobreposición con volumen bajo (0.2) para disparo rápido
   - Salto (`sfx_jump`): sin sobreposición
   - Impacto (`sfx_hit`): sin sobreposición
   - Muerte de enemigo (`sfx_enemy_death`): sin sobreposición
   - Daño al jugador (`sfx_player_hurt`): sin sobreposición
   - Victoria (`sfx_victory`): sin sobreposición
   - Game Over (`sfx_game_over`): sin sobreposición
   - Rugido del jefe (`sfx_boss_roar`): sin sobreposición

3. Detección y detención:
   - Busca todas las instancias del sonido que están reproduciéndose
   - Las detiene antes de reproducir una nueva instancia
   - Solo aplica cuando `allowOverlap` es `false`

### Resultado

- Sin acumulación: los sonidos no se acumulan cuando se reproducen rápidamente
- Disparo más natural: el sonido de disparo permite sobreposición controlada con volumen bajo
- Mejor experiencia: cada evento de sonido se reproduce claramente sin interferencias

Los sonidos ya no deberían sobreponerse. Cada evento reproduce su sonido sin acumular instancias anteriores.

---

**User**

se superponen las músicas y sonidos aún. Por ejemplo, cuando gano un nivel y suena la musica  sfx_victory y victory_music, cuando empiezo el siguiente nivel, siguen sonando y se superponen con la musica del juego.

Tampoco suena la música del menú.

---

**Cursor**

Deteniendo todas las músicas y sonidos al cambiar de escena y asegurando que la música del menú se reproduzca correctamente:


Implementando un sistema para detener todos los sonidos y músicas al cambiar de escena:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo
            if (this.music && this.music.isPlaying) {
                this.music.stop();
            }
            
            // Detener todos los sonidos activos
            if (this.sound && this.sound.sounds) {
                this.sound.sounds.forEach(sound => {
                    if (sound.isPlaying) {
                        sound.stop();
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de victoria (sin sobreposición)
        this.playSound('sfx_victory', 0.6, false);
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener música del juego
        this.stopMusic();
        
        // Reproducir sonido de game over (sin sobreposición)
        this.playSound('sfx_game_over', 0.6, false);
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo
            if (this.music && this.music.isPlaying) {
                this.music.stop();
            }
            
            // Detener todos los sonidos activos
            if (this.sound && this.sound.sounds) {
                this.sound.sounds.forEach(sound => {
                    if (sound.isPlaying) {
                        sound.stop();
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
        
        // Detener todos los sonidos de escenas anteriores
        // Esto se ejecuta antes de create(), así que necesitamos hacerlo en create()
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo
            if (this.music && this.music.isPlaying) {
                this.music.stop();
            }
            
            // Detener todos los sonidos activos
            if (this.sound && this.sound.sounds) {
                this.sound.sounds.forEach(sound => {
                    if (sound.isPlaying) {
                        sound.stop();
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
        
        // Detener todos los sonidos de escenas anteriores
        // Esto se ejecuta antes de create(), así que necesitamos hacerlo en create()
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo
            if (this.music && this.music.isPlaying) {
                this.music.stop();
            }
            
            // Detener todos los sonidos activos
            if (this.sound && this.sound.sounds) {
                this.sound.sounds.forEach(sound => {
                    if (sound.isPlaying) {
                        sound.stop();
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class VictoryScene extends Phaser.Scene {
    constructor() {
        super({ key: 'VictoryScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.timeBonus = data.timeBonus || 0;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de victoria
        try {
            if (this.cache.audio.exists('sfx_victory')) {
                this.sound.play('sfx_victory', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Reproducir música de victoria
        try {
            if (this.cache.audio.exists('victory_music')) {
                this.music = this.sound.add('victory_music', { loop: false, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x001a00)
            .setOrigin(0, 0);
        
        // Título Victoria
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, '¡NIVEL COMPLETADO!', {
            fontSize: '56px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información del nivel
        const infoStyle = {
            fontSize: '28px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 280, `Nivel ${this.level}`, {
            ...infoStyle,
            fontSize: '36px',
            fill: '#ffff00'
        }).setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 340, `Puntuación: ${this.score}`, infoStyle)
            .setOrigin(0.5);
        
        if (this.timeBonus > 0) {
            this.add.text(GAME_CONFIG.WIDTH / 2, 380, `Bonus de Tiempo: +${this.timeBonus}`, {
                ...infoStyle,
                fill: '#00ff00'
            }).setOrigin(0.5);
        }
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 420, `Vidas Restantes: ${this.lives}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Siguiente Nivel (solo si no es el nivel 4)
        if (this.level < 4) {
            const nextButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 350, 80, 0x228b22)
                .setInteractive({ useHandCursor: true })
                .on('pointerdown', () => {
                    this.scene.start('GameScene', {
                        level: this.level + 1,
                        score: this.score,
                        lives: this.lives
                    });
                })
                .on('pointerover', () => {
                    nextButton.setFillStyle(0x32cd32);
                })
                .on('pointerout', () => {
                    nextButton.setFillStyle(0x228b22);
                });
            
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'SIGUIENTE NIVEL', {
                fontSize: '36px',
                fontFamily: 'Arial',
                fill: '#ffffff',
                stroke: '#000000',
                strokeThickness: 2
            }).setOrigin(0.5);
        } else {
            // Si es el nivel 4, mostrar mensaje de victoria final
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, '¡JUEGO COMPLETADO!', {
                fontSize: '40px',
                fontFamily: 'Arial',
                fill: '#ffd700',
                stroke: '#000000',
                strokeThickness: 3
            }).setOrigin(0.5);
        }
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 350, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Efecto de celebración
        this.createCelebrationEffect();
    }
    
    createCelebrationEffect() {
        // Partículas de celebración
        for (let i = 0; i < 20; i++) {
            const particle = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                5,
                0xffd700
            );
            
            this.tweens.add({
                targets: particle,
                y: particle.y - 200,
                alpha: 0,
                duration: 2000 + Math.random() * 1000,
                delay: Math.random() * 500,
                repeat: -1
            });
        }
    }
}


```

```javascript
class VictoryScene extends Phaser.Scene {
    constructor() {
        super({ key: 'VictoryScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.timeBonus = data.timeBonus || 0;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de victoria
        try {
            if (this.cache.audio.exists('sfx_victory')) {
                this.sound.play('sfx_victory', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Reproducir música de victoria
        try {
            if (this.cache.audio.exists('victory_music')) {
                this.music = this.sound.add('victory_music', { loop: false, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x001a00)
            .setOrigin(0, 0);
        
        // Título Victoria
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, '¡NIVEL COMPLETADO!', {
            fontSize: '56px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información del nivel
        const infoStyle = {
            fontSize: '28px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 280, `Nivel ${this.level}`, {
            ...infoStyle,
            fontSize: '36px',
            fill: '#ffff00'
        }).setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 340, `Puntuación: ${this.score}`, infoStyle)
            .setOrigin(0.5);
        
        if (this.timeBonus > 0) {
            this.add.text(GAME_CONFIG.WIDTH / 2, 380, `Bonus de Tiempo: +${this.timeBonus}`, {
                ...infoStyle,
                fill: '#00ff00'
            }).setOrigin(0.5);
        }
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 420, `Vidas Restantes: ${this.lives}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Siguiente Nivel (solo si no es el nivel 4)
        if (this.level < 4) {
            const nextButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 350, 80, 0x228b22)
                .setInteractive({ useHandCursor: true })
                .on('pointerdown', () => {
                    // Detener todos los sonidos antes de cambiar de escena
                    this.stopAllSounds();
                    this.scene.start('GameScene', {
                        level: this.level + 1,
                        score: this.score,
                        lives: this.lives
                    });
                })
                .on('pointerover', () => {
                    nextButton.setFillStyle(0x32cd32);
                })
                .on('pointerout', () => {
                    nextButton.setFillStyle(0x228b22);
                });
            
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'SIGUIENTE NIVEL', {
                fontSize: '36px',
                fontFamily: 'Arial',
                fill: '#ffffff',
                stroke: '#000000',
                strokeThickness: 2
            }).setOrigin(0.5);
        } else {
            // Si es el nivel 4, mostrar mensaje de victoria final
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, '¡JUEGO COMPLETADO!', {
                fontSize: '40px',
                fontFamily: 'Arial',
                fill: '#ffd700',
                stroke: '#000000',
                strokeThickness: 3
            }).setOrigin(0.5);
        }
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 350, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Efecto de celebración
        this.createCelebrationEffect();
    }
    
    createCelebrationEffect() {
        // Partículas de celebración
        for (let i = 0; i < 20; i++) {
            const particle = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                5,
                0xffd700
            );
            
            this.tweens.add({
                targets: particle,
                y: particle.y - 200,
                alpha: 0,
                duration: 2000 + Math.random() * 1000,
                delay: Math.random() * 500,
                repeat: -1
            });
        }
    }
}


```

```javascript
class VictoryScene extends Phaser.Scene {
    constructor() {
        super({ key: 'VictoryScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.timeBonus = data.timeBonus || 0;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de victoria
        try {
            if (this.cache.audio.exists('sfx_victory')) {
                this.sound.play('sfx_victory', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Reproducir música de victoria
        try {
            if (this.cache.audio.exists('victory_music')) {
                this.music = this.sound.add('victory_music', { loop: false, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x001a00)
            .setOrigin(0, 0);
        
        // Título Victoria
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, '¡NIVEL COMPLETADO!', {
            fontSize: '56px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información del nivel
        const infoStyle = {
            fontSize: '28px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 280, `Nivel ${this.level}`, {
            ...infoStyle,
            fontSize: '36px',
            fill: '#ffff00'
        }).setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 340, `Puntuación: ${this.score}`, infoStyle)
            .setOrigin(0.5);
        
        if (this.timeBonus > 0) {
            this.add.text(GAME_CONFIG.WIDTH / 2, 380, `Bonus de Tiempo: +${this.timeBonus}`, {
                ...infoStyle,
                fill: '#00ff00'
            }).setOrigin(0.5);
        }
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 420, `Vidas Restantes: ${this.lives}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Siguiente Nivel (solo si no es el nivel 4)
        if (this.level < 4) {
            const nextButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 350, 80, 0x228b22)
                .setInteractive({ useHandCursor: true })
                .on('pointerdown', () => {
                    // Detener todos los sonidos antes de cambiar de escena
                    this.stopAllSounds();
                    this.scene.start('GameScene', {
                        level: this.level + 1,
                        score: this.score,
                        lives: this.lives
                    });
                })
                .on('pointerover', () => {
                    nextButton.setFillStyle(0x32cd32);
                })
                .on('pointerout', () => {
                    nextButton.setFillStyle(0x228b22);
                });
            
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'SIGUIENTE NIVEL', {
                fontSize: '36px',
                fontFamily: 'Arial',
                fill: '#ffffff',
                stroke: '#000000',
                strokeThickness: 2
            }).setOrigin(0.5);
        } else {
            // Si es el nivel 4, mostrar mensaje de victoria final
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, '¡JUEGO COMPLETADO!', {
                fontSize: '40px',
                fontFamily: 'Arial',
                fill: '#ffd700',
                stroke: '#000000',
                strokeThickness: 3
            }).setOrigin(0.5);
        }
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 350, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Efecto de celebración
        this.createCelebrationEffect();
    }
    
    createCelebrationEffect() {
        // Partículas de celebración
        for (let i = 0; i < 20; i++) {
            const particle = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                5,
                0xffd700
            );
            
            this.tweens.add({
                targets: particle,
                y: particle.y - 200,
                alpha: 0,
                duration: 2000 + Math.random() * 1000,
                delay: Math.random() * 500,
                repeat: -1
            });
        }
    }
}


```

```javascript
class VictoryScene extends Phaser.Scene {
    constructor() {
        super({ key: 'VictoryScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.timeBonus = data.timeBonus || 0;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de victoria
        try {
            if (this.cache.audio.exists('sfx_victory')) {
                this.sound.play('sfx_victory', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Reproducir música de victoria
        try {
            if (this.cache.audio.exists('victory_music')) {
                this.music = this.sound.add('victory_music', { loop: false, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x001a00)
            .setOrigin(0, 0);
        
        // Título Victoria
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, '¡NIVEL COMPLETADO!', {
            fontSize: '56px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información del nivel
        const infoStyle = {
            fontSize: '28px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 280, `Nivel ${this.level}`, {
            ...infoStyle,
            fontSize: '36px',
            fill: '#ffff00'
        }).setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 340, `Puntuación: ${this.score}`, infoStyle)
            .setOrigin(0.5);
        
        if (this.timeBonus > 0) {
            this.add.text(GAME_CONFIG.WIDTH / 2, 380, `Bonus de Tiempo: +${this.timeBonus}`, {
                ...infoStyle,
                fill: '#00ff00'
            }).setOrigin(0.5);
        }
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 420, `Vidas Restantes: ${this.lives}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Siguiente Nivel (solo si no es el nivel 4)
        if (this.level < 4) {
            const nextButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 350, 80, 0x228b22)
                .setInteractive({ useHandCursor: true })
                .on('pointerdown', () => {
                    // Detener todos los sonidos antes de cambiar de escena
                    this.stopAllSounds();
                    this.scene.start('GameScene', {
                        level: this.level + 1,
                        score: this.score,
                        lives: this.lives
                    });
                })
                .on('pointerover', () => {
                    nextButton.setFillStyle(0x32cd32);
                })
                .on('pointerout', () => {
                    nextButton.setFillStyle(0x228b22);
                });
            
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'SIGUIENTE NIVEL', {
                fontSize: '36px',
                fontFamily: 'Arial',
                fill: '#ffffff',
                stroke: '#000000',
                strokeThickness: 2
            }).setOrigin(0.5);
        } else {
            // Si es el nivel 4, mostrar mensaje de victoria final
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, '¡JUEGO COMPLETADO!', {
                fontSize: '40px',
                fontFamily: 'Arial',
                fill: '#ffd700',
                stroke: '#000000',
                strokeThickness: 3
            }).setOrigin(0.5);
        }
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 350, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Efecto de celebración
        this.createCelebrationEffect();
    }
    
    createCelebrationEffect() {
        // Partículas de celebración
        for (let i = 0; i < 20; i++) {
            const particle = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                5,
                0xffd700
            );
            
            this.tweens.add({
                targets: particle,
                y: particle.y - 200,
                alpha: 0,
                duration: 2000 + Math.random() * 1000,
                delay: Math.random() * 500,
                repeat: -1
            });
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo
            if (this.music && this.music.isPlaying) {
                this.music.stop();
            }
            
            // Detener todos los sonidos activos
            if (this.sound && this.sound.sounds) {
                this.sound.sounds.forEach(sound => {
                    if (sound.isPlaying) {
                        sound.stop();
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
        }
    }
}


```

```javascript
class GameOverScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameOverScene' });
    }
    
    init(data) {
        this.finalScore = data.score || 0;
        this.level = data.level || 1;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de game over
        try {
            if (this.cache.audio.exists('sfx_game_over')) {
                this.sound.play('sfx_game_over', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x1a0000)
            .setOrigin(0, 0);
        
        // Título Game Over
        this.add.text(GAME_CONFIG.WIDTH / 2, 200, 'GAME OVER', {
            fontSize: '72px',
            fontFamily: 'Arial',
            fill: '#ff0000',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información
        const infoStyle = {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 350, `Puntuación Final: ${this.finalScore}`, infoStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, `Nivel Alcanzado: ${this.level}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Reintentar
        const retryButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                retryButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                retryButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'REINTENTAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 300, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
    }
}


```

```javascript
class GameOverScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameOverScene' });
    }
    
    init(data) {
        this.finalScore = data.score || 0;
        this.level = data.level || 1;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de game over
        try {
            if (this.cache.audio.exists('sfx_game_over')) {
                this.sound.play('sfx_game_over', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x1a0000)
            .setOrigin(0, 0);
        
        // Título Game Over
        this.add.text(GAME_CONFIG.WIDTH / 2, 200, 'GAME OVER', {
            fontSize: '72px',
            fontFamily: 'Arial',
            fill: '#ff0000',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información
        const infoStyle = {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 350, `Puntuación Final: ${this.finalScore}`, infoStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, `Nivel Alcanzado: ${this.level}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Reintentar
        const retryButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                retryButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                retryButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'REINTENTAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 300, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
    }
}


```

```javascript
class GameOverScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameOverScene' });
    }
    
    init(data) {
        this.finalScore = data.score || 0;
        this.level = data.level || 1;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de game over
        try {
            if (this.cache.audio.exists('sfx_game_over')) {
                this.sound.play('sfx_game_over', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x1a0000)
            .setOrigin(0, 0);
        
        // Título Game Over
        this.add.text(GAME_CONFIG.WIDTH / 2, 200, 'GAME OVER', {
            fontSize: '72px',
            fontFamily: 'Arial',
            fill: '#ff0000',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información
        const infoStyle = {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 350, `Puntuación Final: ${this.finalScore}`, infoStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, `Nivel Alcanzado: ${this.level}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Reintentar
        const retryButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                retryButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                retryButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'REINTENTAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 300, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
    }
}


```

```javascript
class GameOverScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameOverScene' });
    }
    
    init(data) {
        this.finalScore = data.score || 0;
        this.level = data.level || 1;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de game over
        try {
            if (this.cache.audio.exists('sfx_game_over')) {
                this.sound.play('sfx_game_over', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x1a0000)
            .setOrigin(0, 0);
        
        // Título Game Over
        this.add.text(GAME_CONFIG.WIDTH / 2, 200, 'GAME OVER', {
            fontSize: '72px',
            fontFamily: 'Arial',
            fill: '#ff0000',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información
        const infoStyle = {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 350, `Puntuación Final: ${this.finalScore}`, infoStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, `Nivel Alcanzado: ${this.level}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Reintentar
        const retryButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                retryButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                retryButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'REINTENTAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 300, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo
            if (this.music && this.music.isPlaying) {
                this.music.stop();
            }
            
            // Detener todos los sonidos activos
            if (this.sound && this.sound.sounds) {
                this.sound.sounds.forEach(sound => {
                    if (sound.isPlaying) {
                        sound.stop();
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
        }
    }
}


```

```javascript
class MenuScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MenuScene' });
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir música de menú
        try {
            if (this.cache.audio.exists('menu_music')) {
                this.music = this.sound.add('menu_music', { loop: true, volume: 0.3 });
                this.music.play();
            } else {
                console.log('Música de menú no encontrada');
            }
        } catch (e) {
            console.error('Error al reproducir música de menú:', e);
        }
        
        // Fondo - usar imagen si existe, sino usar color sólido
        if (this.textures.exists('menu_bg')) {
            this.add.image(0, 0, 'menu_bg').setOrigin(0, 0);
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Título del juego
        const titleStyle = {
            fontSize: '64px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5,
                stroke: true,
                fill: true
            }
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, 'PLÁTANO BORRACHO', titleStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 220, 'VS MONOS', {
            ...titleStyle,
            fontSize: '48px'
        }).setOrigin(0.5);
        
        // Botón Jugar
        const playButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 400, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                playButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                playButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, 'JUGAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Instrucciones
        const instructionsStyle = {
            fontSize: '20px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        const instructions = [
            'CONTROLES:',
            'A/D o ←/→ : Mover',
            'W o ↑ : Saltar',
            'J o Espacio : Lanzar proyectil',
            '',
            'Derrota a los monos y enfrenta',
            'a los poderosos gorilas jefe!'
        ];
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, instructions, instructionsStyle)
            .setOrigin(0.5);
        
        // Efecto de fondo animado (monos moviéndose)
        this.createAnimatedBackground();
    }
    
    createAnimatedBackground() {
        // Crear algunos monos decorativos en el fondo
        for (let i = 0; i < 5; i++) {
            const monkey = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                20,
                COLORS.MONKEY,
                0.3
            );
            
            this.tweens.add({
                targets: monkey,
                x: Math.random() * GAME_CONFIG.WIDTH,
                y: Math.random() * GAME_CONFIG.HEIGHT,
                duration: 3000 + Math.random() * 2000,
                repeat: -1,
                yoyo: true
            });
        }
    }
}


```

```javascript
class MenuScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MenuScene' });
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir música de menú
        try {
            if (this.cache.audio.exists('menu_music')) {
                this.music = this.sound.add('menu_music', { loop: true, volume: 0.3 });
                this.music.play();
            } else {
                console.log('Música de menú no encontrada');
            }
        } catch (e) {
            console.error('Error al reproducir música de menú:', e);
        }
        
        // Fondo - usar imagen si existe, sino usar color sólido
        if (this.textures.exists('menu_bg')) {
            this.add.image(0, 0, 'menu_bg').setOrigin(0, 0);
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Título del juego
        const titleStyle = {
            fontSize: '64px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5,
                stroke: true,
                fill: true
            }
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, 'PLÁTANO BORRACHO', titleStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 220, 'VS MONOS', {
            ...titleStyle,
            fontSize: '48px'
        }).setOrigin(0.5);
        
        // Botón Jugar
        const playButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 400, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                playButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                playButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, 'JUGAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Instrucciones
        const instructionsStyle = {
            fontSize: '20px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        const instructions = [
            'CONTROLES:',
            'A/D o ←/→ : Mover',
            'W o ↑ : Saltar',
            'J o Espacio : Lanzar proyectil',
            '',
            'Derrota a los monos y enfrenta',
            'a los poderosos gorilas jefe!'
        ];
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, instructions, instructionsStyle)
            .setOrigin(0.5);
        
        // Efecto de fondo animado (monos moviéndose)
        this.createAnimatedBackground();
    }
    
    createAnimatedBackground() {
        // Crear algunos monos decorativos en el fondo
        for (let i = 0; i < 5; i++) {
            const monkey = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                20,
                COLORS.MONKEY,
                0.3
            );
            
            this.tweens.add({
                targets: monkey,
                x: Math.random() * GAME_CONFIG.WIDTH,
                y: Math.random() * GAME_CONFIG.HEIGHT,
                duration: 3000 + Math.random() * 2000,
                repeat: -1,
                yoyo: true
            });
        }
    }
}


```

```javascript
class MenuScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MenuScene' });
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir música de menú
        try {
            if (this.cache.audio.exists('menu_music')) {
                this.music = this.sound.add('menu_music', { loop: true, volume: 0.3 });
                this.music.play();
            } else {
                console.log('Música de menú no encontrada');
            }
        } catch (e) {
            console.error('Error al reproducir música de menú:', e);
        }
        
        // Fondo - usar imagen si existe, sino usar color sólido
        if (this.textures.exists('menu_bg')) {
            this.add.image(0, 0, 'menu_bg').setOrigin(0, 0);
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Título del juego
        const titleStyle = {
            fontSize: '64px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5,
                stroke: true,
                fill: true
            }
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, 'PLÁTANO BORRACHO', titleStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 220, 'VS MONOS', {
            ...titleStyle,
            fontSize: '48px'
        }).setOrigin(0.5);
        
        // Botón Jugar
        const playButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 400, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                playButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                playButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, 'JUGAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Instrucciones
        const instructionsStyle = {
            fontSize: '20px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        const instructions = [
            'CONTROLES:',
            'A/D o ←/→ : Mover',
            'W o ↑ : Saltar',
            'J o Espacio : Lanzar proyectil',
            '',
            'Derrota a los monos y enfrenta',
            'a los poderosos gorilas jefe!'
        ];
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, instructions, instructionsStyle)
            .setOrigin(0.5);
        
        // Efecto de fondo animado (monos moviéndose)
        this.createAnimatedBackground();
    }
    
    createAnimatedBackground() {
        // Crear algunos monos decorativos en el fondo
        for (let i = 0; i < 5; i++) {
            const monkey = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                20,
                COLORS.MONKEY,
                0.3
            );
            
            this.tweens.add({
                targets: monkey,
                x: Math.random() * GAME_CONFIG.WIDTH,
                y: Math.random() * GAME_CONFIG.HEIGHT,
                duration: 3000 + Math.random() * 2000,
                repeat: -1,
                yoyo: true
            });
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo
            if (this.music && this.music.isPlaying) {
                this.music.stop();
            }
            
            // Detener todos los sonidos activos
            if (this.sound && this.sound.sounds) {
                this.sound.sounds.forEach(sound => {
                    if (sound.isPlaying) {
                        sound.stop();
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
        }
    }
}


```

Mejorando el sistema para usar el sistema de sonido global de Phaser y asegurar que funcione correctamente:


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
        
        // Detener todos los sonidos de escenas anteriores
        // Esto se ejecuta antes de create(), así que necesitamos hacerlo en create()
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo de esta escena
            if (this.music && this.music.isPlaying) {
                this.music.stop();
                this.music.destroy();
                this.music = null;
            }
            
            // Detener todos los sonidos activos del sistema de sonido
            if (this.sound) {
                // Detener todos los sonidos que están reproduciéndose
                this.sound.sounds.forEach(sound => {
                    if (sound && sound.isPlaying) {
                        sound.stop();
                    }
                });
                // También intentar detener todos los sonidos por clave
                const soundKeys = ['sfx_victory', 'sfx_game_over', 'sfx_player_hurt', 'sfx_enemy_death', 
                                 'sfx_hit', 'sfx_throw', 'sfx_jump', 'sfx_boss_roar',
                                 'game_music', 'victory_music', 'menu_music'];
                soundKeys.forEach(key => {
                    if (this.cache.audio.exists(key)) {
                        this.sound.stopByKey(key);
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
            console.log('Error al detener sonidos:', e);
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class VictoryScene extends Phaser.Scene {
    constructor() {
        super({ key: 'VictoryScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.timeBonus = data.timeBonus || 0;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de victoria
        try {
            if (this.cache.audio.exists('sfx_victory')) {
                this.sound.play('sfx_victory', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Reproducir música de victoria
        try {
            if (this.cache.audio.exists('victory_music')) {
                this.music = this.sound.add('victory_music', { loop: false, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x001a00)
            .setOrigin(0, 0);
        
        // Título Victoria
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, '¡NIVEL COMPLETADO!', {
            fontSize: '56px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información del nivel
        const infoStyle = {
            fontSize: '28px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 280, `Nivel ${this.level}`, {
            ...infoStyle,
            fontSize: '36px',
            fill: '#ffff00'
        }).setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 340, `Puntuación: ${this.score}`, infoStyle)
            .setOrigin(0.5);
        
        if (this.timeBonus > 0) {
            this.add.text(GAME_CONFIG.WIDTH / 2, 380, `Bonus de Tiempo: +${this.timeBonus}`, {
                ...infoStyle,
                fill: '#00ff00'
            }).setOrigin(0.5);
        }
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 420, `Vidas Restantes: ${this.lives}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Siguiente Nivel (solo si no es el nivel 4)
        if (this.level < 4) {
            const nextButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 350, 80, 0x228b22)
                .setInteractive({ useHandCursor: true })
                .on('pointerdown', () => {
                    // Detener todos los sonidos antes de cambiar de escena
                    this.stopAllSounds();
                    this.scene.start('GameScene', {
                        level: this.level + 1,
                        score: this.score,
                        lives: this.lives
                    });
                })
                .on('pointerover', () => {
                    nextButton.setFillStyle(0x32cd32);
                })
                .on('pointerout', () => {
                    nextButton.setFillStyle(0x228b22);
                });
            
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'SIGUIENTE NIVEL', {
                fontSize: '36px',
                fontFamily: 'Arial',
                fill: '#ffffff',
                stroke: '#000000',
                strokeThickness: 2
            }).setOrigin(0.5);
        } else {
            // Si es el nivel 4, mostrar mensaje de victoria final
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, '¡JUEGO COMPLETADO!', {
                fontSize: '40px',
                fontFamily: 'Arial',
                fill: '#ffd700',
                stroke: '#000000',
                strokeThickness: 3
            }).setOrigin(0.5);
        }
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 350, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Efecto de celebración
        this.createCelebrationEffect();
    }
    
    createCelebrationEffect() {
        // Partículas de celebración
        for (let i = 0; i < 20; i++) {
            const particle = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                5,
                0xffd700
            );
            
            this.tweens.add({
                targets: particle,
                y: particle.y - 200,
                alpha: 0,
                duration: 2000 + Math.random() * 1000,
                delay: Math.random() * 500,
                repeat: -1
            });
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo de esta escena
            if (this.music && this.music.isPlaying) {
                this.music.stop();
                this.music.destroy();
                this.music = null;
            }
            
            // Detener todos los sonidos activos del sistema de sonido
            if (this.sound) {
                // Detener todos los sonidos que están reproduciéndose
                this.sound.sounds.forEach(sound => {
                    if (sound && sound.isPlaying) {
                        sound.stop();
                    }
                });
                // También intentar detener todos los sonidos por clave
                const soundKeys = ['sfx_victory', 'sfx_game_over', 'sfx_player_hurt', 'sfx_enemy_death', 
                                 'sfx_hit', 'sfx_throw', 'sfx_jump', 'sfx_boss_roar',
                                 'game_music', 'victory_music', 'menu_music'];
                soundKeys.forEach(key => {
                    if (this.cache.audio.exists(key)) {
                        this.sound.stopByKey(key);
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
            console.log('Error al detener sonidos:', e);
        }
    }
}


```

```javascript
class GameOverScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameOverScene' });
    }
    
    init(data) {
        this.finalScore = data.score || 0;
        this.level = data.level || 1;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de game over
        try {
            if (this.cache.audio.exists('sfx_game_over')) {
                this.sound.play('sfx_game_over', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x1a0000)
            .setOrigin(0, 0);
        
        // Título Game Over
        this.add.text(GAME_CONFIG.WIDTH / 2, 200, 'GAME OVER', {
            fontSize: '72px',
            fontFamily: 'Arial',
            fill: '#ff0000',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información
        const infoStyle = {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 350, `Puntuación Final: ${this.finalScore}`, infoStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, `Nivel Alcanzado: ${this.level}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Reintentar
        const retryButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                retryButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                retryButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'REINTENTAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 300, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo de esta escena
            if (this.music && this.music.isPlaying) {
                this.music.stop();
                this.music.destroy();
                this.music = null;
            }
            
            // Detener todos los sonidos activos del sistema de sonido
            if (this.sound) {
                // Detener todos los sonidos que están reproduciéndose
                this.sound.sounds.forEach(sound => {
                    if (sound && sound.isPlaying) {
                        sound.stop();
                    }
                });
                // También intentar detener todos los sonidos por clave
                const soundKeys = ['sfx_victory', 'sfx_game_over', 'sfx_player_hurt', 'sfx_enemy_death', 
                                 'sfx_hit', 'sfx_throw', 'sfx_jump', 'sfx_boss_roar',
                                 'game_music', 'victory_music', 'menu_music'];
                soundKeys.forEach(key => {
                    if (this.cache.audio.exists(key)) {
                        this.sound.stopByKey(key);
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
            console.log('Error al detener sonidos:', e);
        }
    }
}


```

```javascript
class MenuScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MenuScene' });
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir música de menú
        try {
            if (this.cache.audio.exists('menu_music')) {
                this.music = this.sound.add('menu_music', { loop: true, volume: 0.3 });
                this.music.play();
            } else {
                console.log('Música de menú no encontrada');
            }
        } catch (e) {
            console.error('Error al reproducir música de menú:', e);
        }
        
        // Fondo - usar imagen si existe, sino usar color sólido
        if (this.textures.exists('menu_bg')) {
            this.add.image(0, 0, 'menu_bg').setOrigin(0, 0);
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Título del juego
        const titleStyle = {
            fontSize: '64px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5,
                stroke: true,
                fill: true
            }
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, 'PLÁTANO BORRACHO', titleStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 220, 'VS MONOS', {
            ...titleStyle,
            fontSize: '48px'
        }).setOrigin(0.5);
        
        // Botón Jugar
        const playButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 400, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                playButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                playButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, 'JUGAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Instrucciones
        const instructionsStyle = {
            fontSize: '20px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        const instructions = [
            'CONTROLES:',
            'A/D o ←/→ : Mover',
            'W o ↑ : Saltar',
            'J o Espacio : Lanzar proyectil',
            '',
            'Derrota a los monos y enfrenta',
            'a los poderosos gorilas jefe!'
        ];
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, instructions, instructionsStyle)
            .setOrigin(0.5);
        
        // Efecto de fondo animado (monos moviéndose)
        this.createAnimatedBackground();
    }
    
    createAnimatedBackground() {
        // Crear algunos monos decorativos en el fondo
        for (let i = 0; i < 5; i++) {
            const monkey = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                20,
                COLORS.MONKEY,
                0.3
            );
            
            this.tweens.add({
                targets: monkey,
                x: Math.random() * GAME_CONFIG.WIDTH,
                y: Math.random() * GAME_CONFIG.HEIGHT,
                duration: 3000 + Math.random() * 2000,
                repeat: -1,
                yoyo: true
            });
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo de esta escena
            if (this.music && this.music.isPlaying) {
                this.music.stop();
                this.music.destroy();
                this.music = null;
            }
            
            // Detener todos los sonidos activos del sistema de sonido
            if (this.sound) {
                // Detener todos los sonidos que están reproduciéndose
                this.sound.sounds.forEach(sound => {
                    if (sound && sound.isPlaying) {
                        sound.stop();
                    }
                });
                // También intentar detener todos los sonidos por clave
                const soundKeys = ['sfx_victory', 'sfx_game_over', 'sfx_player_hurt', 'sfx_enemy_death', 
                                 'sfx_hit', 'sfx_throw', 'sfx_jump', 'sfx_boss_roar',
                                 'game_music', 'victory_music', 'menu_music'];
                soundKeys.forEach(key => {
                    if (this.cache.audio.exists(key)) {
                        this.sound.stopByKey(key);
                    }
                });
            }
        } catch (e) {
            // Silenciar errores
            console.log('Error al detener sonidos:', e);
        }
    }
}


```

Corrigiendo el uso de `stopByKey` (puede no existir en todas las versiones de Phaser):


```javascript
class GameScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.startTime = Date.now();
        
        // Detener todos los sonidos de escenas anteriores
        // Esto se ejecuta antes de create(), así que necesitamos hacerlo en create()
    }
    
    create() {
        // Fondo - usar imagen si existe, sino usar color sólido
        const bgKey = `jungle_bg_${this.level}`;
        if (this.textures.exists(bgKey)) {
            // Crear fondo repetido para el scroll
            for (let i = 0; i < 3; i++) {
                this.add.image(i * GAME_CONFIG.WIDTH, 0, bgKey).setOrigin(0, 0);
            }
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Grupos de objetos (crear primero para poder usarlos)
        this.monkeys = this.physics.add.group();
        this.gorillas = this.physics.add.group();
        this.projectiles = this.physics.add.group();
        this.enemyProjectiles = this.physics.add.group();
        this.platforms = this.physics.add.staticGroup();
        
        // Crear plataformas
        this.createPlatforms();
        
        // Crear jugador (sobre la primera plataforma)
        this.player = new Player(this, 100, GAME_CONFIG.HEIGHT - 150);
        this.player.lives = this.lives; // Usar las vidas pasadas desde la escena anterior
        this.player.currentHealth = 100; // Inicializar salud al 100% al comenzar/reiniciar nivel
        
        // Crear enemigos según el nivel
        this.createEnemies();
        
        // Colisiones (configurar primero para que el jefe colisione correctamente)
        this.setupCollisions();
        
        // Crear gorila jefe (sobre la última plataforma)
        // La última plataforma está en x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150
        // La plataforma tiene 30px de grosor, así que la parte superior está en y: GAME_CONFIG.HEIGHT - 150
        const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
        const bossPlatformY = GAME_CONFIG.HEIGHT - 150; // Parte superior de la plataforma
        const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
        const bossX = bossPlatformX + bossPlatformWidth / 2; // Centro de la plataforma
        // El gorila tiene 80px de altura, posicionarlo justo tocando la plataforma
        // Si el origen está en el centro, el jefe debe estar en: plataformaY - (altura/2)
        const bossY = bossPlatformY - 40; // Centro del jefe justo en la parte superior de la plataforma
        this.boss = new Gorilla(this, bossX, bossY, this.level);
        this.gorillas.add(this.boss);
        
        // Asegurar que el jefe sea visible y activo
        this.boss.setActive(true);
        this.boss.setVisible(true);
        this.boss.setDepth(10); // Asegurar que esté por encima de otros elementos
        
        // Detener completamente el movimiento del jefe inicialmente
        // El jefe comenzará a moverse cuando detecte al jugador
        this.boss.body.setVelocityX(0);
        this.boss.body.setVelocityY(0);
        // El jefe comenzará quieto y se moverá cuando detecte al jugador
        
        // Forzar que el jefe esté sobre la plataforma después de un frame
        this.time.delayedCall(100, () => {
            if (this.boss && this.boss.active) {
                // Asegurar que esté en la posición correcta
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe reposicionado: x=${this.boss.x}, y=${this.boss.y}`);
            }
        });
        
        // Debug: mostrar información del jefe
        console.log(`Jefe creado en posición: x=${bossX}, y=${bossY}, nivel=${this.level}`);
        console.log(`Jefe activo: ${this.boss.active}, visible: ${this.boss.visible}, alive: ${this.boss.isAlive}`);
        console.log(`Tamaño del jefe: width=${this.boss.width}, height=${this.boss.height}`);
        
        // Controles
        this.cursors = this.input.keyboard.createCursorKeys();
        this.cursors.w = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.W);
        this.cursors.a = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.A);
        this.cursors.d = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.D);
        this.cursors.space = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.SPACE);
        this.cursors.j = this.input.keyboard.addKey(Phaser.Input.Keyboard.KeyCodes.J);
        
        // HUD
        this.createHUD();
        this.updateHUD(); // Inicializar HUD con valores correctos
        
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Iniciar música de fondo del juego
        this.playMusic('game_music', true);
        
        // Configurar límites del mundo físico (más ancho que el fondo para permitir avanzar)
        // Ampliar límite inferior significativamente para que los personajes no desaparezcan al caer
        // setBounds(x, y, width, height) - ampliamos height para incluir espacio extra abajo
        this.physics.world.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT + 2000);
        
        // Cámara - seguir al jugador hasta el final del mundo
        this.cameras.main.setBounds(0, 0, GAME_CONFIG.WIDTH * 3, GAME_CONFIG.HEIGHT);
        this.cameras.main.startFollow(this.player, false, 1, 0.5, 0, 0); // Seguir suavemente, centrado horizontalmente
        this.cameras.main.setDeadzone(0, 0); // Sin deadzone para que siempre siga al jugador
        this.cameras.main.setFollowOffset(0, 0); // Sin offset
        
        // Timer para proyectiles
        this.lastProjectileTime = 0;
        this.projectileCooldown = 100; // 100ms entre proyectiles (disparo rápido continuo)
    }
    
    createPlatforms() {
        // Plataformas del nivel
        const platformData = [
            { x: 0, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 0.8, y: GAME_CONFIG.HEIGHT - 50, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 1.2, y: GAME_CONFIG.HEIGHT - 200, width: GAME_CONFIG.WIDTH * 0.4 },
            { x: GAME_CONFIG.WIDTH * 1.6, y: GAME_CONFIG.HEIGHT - 100, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.0, y: GAME_CONFIG.HEIGHT - 250, width: GAME_CONFIG.WIDTH * 0.3 },
            { x: GAME_CONFIG.WIDTH * 2.4, y: GAME_CONFIG.HEIGHT - 150, width: GAME_CONFIG.WIDTH * 0.4 } // Plataforma del jefe
        ];
        
        platformData.forEach(platform => {
            const plat = this.add.rectangle(
                platform.x + platform.width / 2,
                platform.y,
                platform.width,
                30,
                COLORS.PLATFORM
            );
            this.physics.add.existing(plat, true);
            this.platforms.add(plat);
        });
        
        // Plataforma inferior del mundo (suelo) - cubre todo el ancho para evitar que caigan
        const bottomPlatform = this.add.rectangle(
            (GAME_CONFIG.WIDTH * 3) / 2, // Centro del ancho total del mundo
            GAME_CONFIG.HEIGHT - 15, // Justo en el límite inferior visible
            GAME_CONFIG.WIDTH * 3, // Cubre todo el ancho del mundo
            30, // Grosor suficiente para colisionar
            COLORS.PLATFORM
        );
        this.physics.add.existing(bottomPlatform, true);
        this.platforms.add(bottomPlatform);
    }
    
    createEnemies() {
        // Crear monos según el nivel
        const monkeyCount = 3 + this.level * 2;
        const monkeyPositions = [
            { x: GAME_CONFIG.WIDTH * 0.2, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre primera plataforma
            { x: GAME_CONFIG.WIDTH * 0.6, y: GAME_CONFIG.HEIGHT - 200 }, // Sobre segunda plataforma
            { x: GAME_CONFIG.WIDTH * 1.0, y: GAME_CONFIG.HEIGHT - 100 }, // Sobre tercera plataforma
            { x: GAME_CONFIG.WIDTH * 1.4, y: GAME_CONFIG.HEIGHT - 250 }, // Sobre cuarta plataforma
            { x: GAME_CONFIG.WIDTH * 1.8, y: GAME_CONFIG.HEIGHT - 150 }, // Sobre quinta plataforma
            { x: GAME_CONFIG.WIDTH * 2.2, y: GAME_CONFIG.HEIGHT - 300 } // Sobre sexta plataforma
        ];
        
        for (let i = 0; i < Math.min(monkeyCount, monkeyPositions.length); i++) {
            const pos = monkeyPositions[i];
            let type = ENEMY_TYPES.MONKEY_BASIC;
            
            // Variar tipos según el nivel
            if (this.level >= 2 && i % 3 === 0) {
                type = ENEMY_TYPES.MONKEY_FAST;
            } else if (this.level >= 3 && i % 4 === 0) {
                type = ENEMY_TYPES.MONKEY_STRONG;
            }
            
            const monkey = new Monkey(this, pos.x, pos.y, type);
            this.monkeys.add(monkey);
        }
    }
    
    setupCollisions() {
        // Jugador con plataformas
        this.physics.add.collider(this.player, this.platforms);
        
        // Enemigos con plataformas
        this.physics.add.collider(this.monkeys, this.platforms);
        this.physics.add.collider(this.gorillas, this.platforms);
        
        // Proyectiles con plataformas
        this.physics.add.collider(this.projectiles, this.platforms, (projectile, platform) => {
            projectile.destroy();
        });
        
        // Proyectiles con enemigos
        this.physics.add.overlap(this.projectiles, this.monkeys, (projectile, monkey) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (monkey.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.5, false); // Sonido de muerte de enemigo (sin sobreposición)
                this.score += monkey.getScore();
                this.updateHUD();
            }
        });
        
        this.physics.add.overlap(this.projectiles, this.gorillas, (projectile, gorilla) => {
            projectile.destroy();
            this.playSound('sfx_hit', 0.4, false); // Sonido de impacto (sin sobreposición)
            if (gorilla.takeDamage()) {
                this.playSound('sfx_enemy_death', 0.6, false); // Sonido de muerte de jefe (sin sobreposición)
                this.score += gorilla.getScore();
                this.updateHUD();
                // Verificar si el jefe fue derrotado
                if (gorilla === this.boss) {
                    this.completeLevel();
                }
            }
        });
        
        // Flag para prevenir múltiples reinicios simultáneos
        this.isRestarting = false;
        
        // Jugador con enemigos (monos - 10% de daño)
        this.physics.add.overlap(this.player, this.monkeys, (player, monkey) => {
            if (monkey.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 10% de daño
                this.playSound('sfx_player_hurt', 0.5, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(10);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
        
        // Jugador con jefe (gorila - 25% de daño)
        this.physics.add.overlap(this.player, this.gorillas, (player, gorilla) => {
            if (gorilla.isAlive && !player.invulnerable && !this.isRestarting) {
                // Aplicar 25% de daño
                this.playSound('sfx_player_hurt', 0.6, false); // Sonido de daño al jugador (sin sobreposición)
                const lostLife = player.takeDamage(25);
                this.lives = player.lives;
                this.updateHUD();
                
                // Solo reiniciar si se perdió una vida completa
                if (lostLife) {
                    // Prevenir múltiples colisiones
                    this.isRestarting = true;
                    
                    this.time.delayedCall(500, () => {
                        const remainingLives = player.lives;
                        
                        if (remainingLives > 0) {
                            // Reiniciar nivel con las vidas restantes
                            this.scene.start('GameScene', {
                                level: this.level,
                                score: this.score,
                                lives: remainingLives
                            });
                        } else {
                            // No hay vidas, ir a Game Over
                            this.scene.start('GameOverScene', {
                                score: this.score,
                                level: this.level
                            });
                        }
                    });
                }
            }
        });
    }
    
    createHUD() {
        // HUD fijo en la cámara
        this.hudContainer = this.add.container(0, 0);
        this.hudContainer.setScrollFactor(0);
        this.hudContainer.setDepth(1000);
        
        // Fondo del HUD (más ancho para incluir barra de salud)
        const hudBg = this.add.rectangle(10, 10, 350, 120, 0x000000, 0.7);
        hudBg.setOrigin(0, 0);
        this.hudContainer.add(hudBg);
        
        // Textos del HUD
        this.scoreText = this.add.text(20, 20, `Puntuación: ${this.score}`, {
            fontSize: '20px',
            fill: '#ffffff'
        });
        this.hudContainer.add(this.scoreText);
        
        this.livesText = this.add.text(20, 50, `Vidas: ${this.lives}`, {
            fontSize: '20px',
            fill: '#ff0000'
        });
        this.hudContainer.add(this.livesText);
        
        // Barra de salud
        this.healthBarBg = this.add.rectangle(20, 85, 200, 20, 0x333333, 1);
        this.healthBarBg.setOrigin(0, 0);
        this.hudContainer.add(this.healthBarBg);
        
        this.healthBar = this.add.rectangle(20, 85, 200, 20, 0x00ff00, 1);
        this.healthBar.setOrigin(0, 0);
        this.hudContainer.add(this.healthBar);
        
        this.healthText = this.add.text(230, 85, '100%', {
            fontSize: '16px',
            fill: '#ffffff'
        });
        this.healthText.setOrigin(0, 0.5);
        this.hudContainer.add(this.healthText);
        
        this.levelText = this.add.text(20, 110, `Nivel: ${this.level}`, {
            fontSize: '20px',
            fill: '#ffff00'
        });
        this.hudContainer.add(this.levelText);
    }
    
    updateHUD() {
        this.scoreText.setText(`Puntuación: ${this.score}`);
        this.livesText.setText(`Vidas: ${this.lives}`);
        
        // Actualizar barra de salud
        const healthPercent = Math.max(0, Math.min(100, this.player.currentHealth));
        const healthWidth = (healthPercent / 100) * 200;
        this.healthBar.width = healthWidth;
        
        // Cambiar color según la salud
        if (healthPercent > 60) {
            this.healthBar.fillColor = 0x00ff00; // Verde
        } else if (healthPercent > 30) {
            this.healthBar.fillColor = 0xffff00; // Amarillo
        } else {
            this.healthBar.fillColor = 0xff0000; // Rojo
        }
        
        this.healthText.setText(`${Math.round(healthPercent)}%`);
    }
    
    /**
     * Reproduce un sonido de forma segura (verifica si existe)
     * Evita sobreposiciones deteniendo el sonido anterior si está reproduciéndose
     */
    playSound(key, volume = 0.5, allowOverlap = false) {
        try {
            if (this.cache.audio.exists(key)) {
                // Si no se permite sobreposición, detener el sonido anterior
                if (!allowOverlap) {
                    // Buscar y detener todas las instancias de este sonido
                    if (this.sound.sounds) {
                        this.sound.sounds.forEach(sound => {
                            if (sound.key === key && sound.isPlaying) {
                                sound.stop();
                            }
                        });
                    }
                }
                this.sound.play(key, { volume: volume });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
    }
    
    /**
     * Reproduce música de fondo
     */
    playMusic(key, loop = false) {
        try {
            if (this.cache.audio.exists(key)) {
                if (this.music && this.music.isPlaying) {
                    this.music.stop();
                }
                this.music = this.sound.add(key, { loop: loop, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
    }
    
    /**
     * Detiene la música de fondo
     */
    stopMusic() {
        if (this.music && this.music.isPlaying) {
            this.music.stop();
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo de esta escena
            if (this.music && this.music.isPlaying) {
                this.music.stop();
                this.music.destroy();
                this.music = null;
            }
            
            // Detener todos los sonidos activos del sistema de sonido
            if (this.sound) {
                // Detener todos los sonidos que están reproduciéndose
                this.sound.sounds.forEach(sound => {
                    if (sound && sound.isPlaying) {
                        sound.stop();
                    }
                });
                // También intentar detener todos los sonidos por clave (si el método existe)
                if (this.sound.stopByKey) {
                    const soundKeys = ['sfx_victory', 'sfx_game_over', 'sfx_player_hurt', 'sfx_enemy_death', 
                                     'sfx_hit', 'sfx_throw', 'sfx_jump', 'sfx_boss_roar',
                                     'game_music', 'victory_music', 'menu_music'];
                    soundKeys.forEach(key => {
                        if (this.cache.audio.exists(key)) {
                            try {
                                this.sound.stopByKey(key);
                            } catch (e) {
                                // Ignorar si no existe el método
                            }
                        }
                    });
                }
            }
        } catch (e) {
            // Silenciar errores
            console.log('Error al detener sonidos:', e);
        }
    }
    
    update(time, delta) {
        if (!this.player.isAlive) return;
        
        // Asegurar que el jugador permanezca activo y visible
        if (!this.player.active) {
            this.player.setActive(true);
        }
        if (!this.player.visible) {
            this.player.setVisible(true);
        }
        
        // Asegurar que el jefe permanezca activo y visible
        if (this.boss && this.boss.isAlive) {
            if (!this.boss.active) {
                this.boss.setActive(true);
                console.log('Jefe reactivado');
            }
            if (!this.boss.visible) {
                this.boss.setVisible(true);
                console.log('Jefe hecho visible');
            }
            
            // Si el jefe está cayendo (y > 1000), reposicionarlo sobre la plataforma
            if (this.boss.y > GAME_CONFIG.HEIGHT + 100) {
                const bossPlatformX = GAME_CONFIG.WIDTH * 2.4;
                const bossPlatformY = GAME_CONFIG.HEIGHT - 150;
                const bossPlatformWidth = GAME_CONFIG.WIDTH * 0.4;
                const bossX = bossPlatformX + bossPlatformWidth / 2;
                const bossY = bossPlatformY - 40;
                this.boss.setPosition(bossX, bossY);
                this.boss.body.setVelocityX(0);
                this.boss.body.setVelocityY(0);
                console.log(`Jefe estaba cayendo, reposicionado a: x=${bossX}, y=${bossY}`);
            }
            
            // Debug periódico (cada 5 segundos aproximadamente)
            if (Math.floor(time / 5000) !== Math.floor((time - delta) / 5000)) {
                console.log(`Jefe - Posición: x=${Math.round(this.boss.x)}, y=${Math.round(this.boss.y)}, activo: ${this.boss.active}, visible: ${this.boss.visible}, en suelo: ${this.boss.body.onFloor()}`);
            }
        }
        
        // Actualizar jugador
        this.player.update(this.cursors, time);
        
        // Lanzar proyectiles (disparo continuo rápido si se mantiene pulsado)
        const isShooting = this.cursors.j.isDown || this.cursors.space.isDown;
        if (isShooting && time - this.lastProjectileTime >= this.projectileCooldown) {
            const projectile = this.player.throwProjectile();
            if (projectile) {
                this.projectiles.add(projectile);
                this.lastProjectileTime = time;
                // Permitir sobreposición para el sonido de disparo (suena más natural con disparo rápido)
                this.playSound('sfx_throw', 0.2, true); // Volumen más bajo y permitir sobreposición controlada
            }
        }
        
        // Actualizar proyectiles
        this.projectiles.children.entries.forEach(projectile => {
            if (projectile.active) {
                projectile.update();
            }
        });
        
        // Actualizar enemigos
        this.monkeys.children.entries.forEach(monkey => {
            if (monkey.isAlive) {
                monkey.update(this.player, time);
            }
        });
        
        this.gorillas.children.entries.forEach(gorilla => {
            if (gorilla.isAlive) {
                gorilla.update(this.player, time);
            }
        });
    }
    
    completeLevel() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Calcular tiempo y bonus
        const elapsedTime = (Date.now() - this.startTime) / 1000;
        const timeBonus = Math.max(0, Math.floor((300 - elapsedTime) * SCORES.TIME_BONUS_MULTIPLIER));
        this.score += SCORES.LEVEL_COMPLETE + timeBonus;
        
        // Ir a la escena de victoria
        this.scene.start('VictoryScene', {
            level: this.level,
            score: this.score,
            lives: this.player.lives,
            timeBonus: timeBonus
        });
    }
    
    gameOver() {
        // Detener TODOS los sonidos y músicas antes de cambiar de escena
        this.stopAllSounds();
        
        // Esta función ahora solo se usa cuando realmente no hay vidas
        // La lógica de reiniciar con vidas se maneja directamente en las colisiones
        this.scene.start('GameOverScene', {
            score: this.score,
            level: this.level
        });
    }
}


```

```javascript
class VictoryScene extends Phaser.Scene {
    constructor() {
        super({ key: 'VictoryScene' });
    }
    
    init(data) {
        this.level = data.level || 1;
        this.score = data.score || 0;
        this.lives = data.lives || GAME_CONFIG.PLAYER_LIVES;
        this.timeBonus = data.timeBonus || 0;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de victoria
        try {
            if (this.cache.audio.exists('sfx_victory')) {
                this.sound.play('sfx_victory', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Reproducir música de victoria
        try {
            if (this.cache.audio.exists('victory_music')) {
                this.music = this.sound.add('victory_music', { loop: false, volume: 0.3 });
                this.music.play();
            }
        } catch (e) {
            // Silenciar errores si la música no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x001a00)
            .setOrigin(0, 0);
        
        // Título Victoria
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, '¡NIVEL COMPLETADO!', {
            fontSize: '56px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información del nivel
        const infoStyle = {
            fontSize: '28px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 280, `Nivel ${this.level}`, {
            ...infoStyle,
            fontSize: '36px',
            fill: '#ffff00'
        }).setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 340, `Puntuación: ${this.score}`, infoStyle)
            .setOrigin(0.5);
        
        if (this.timeBonus > 0) {
            this.add.text(GAME_CONFIG.WIDTH / 2, 380, `Bonus de Tiempo: +${this.timeBonus}`, {
                ...infoStyle,
                fill: '#00ff00'
            }).setOrigin(0.5);
        }
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 420, `Vidas Restantes: ${this.lives}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Siguiente Nivel (solo si no es el nivel 4)
        if (this.level < 4) {
            const nextButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 350, 80, 0x228b22)
                .setInteractive({ useHandCursor: true })
                .on('pointerdown', () => {
                    // Detener todos los sonidos antes de cambiar de escena
                    this.stopAllSounds();
                    this.scene.start('GameScene', {
                        level: this.level + 1,
                        score: this.score,
                        lives: this.lives
                    });
                })
                .on('pointerover', () => {
                    nextButton.setFillStyle(0x32cd32);
                })
                .on('pointerout', () => {
                    nextButton.setFillStyle(0x228b22);
                });
            
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'SIGUIENTE NIVEL', {
                fontSize: '36px',
                fontFamily: 'Arial',
                fill: '#ffffff',
                stroke: '#000000',
                strokeThickness: 2
            }).setOrigin(0.5);
        } else {
            // Si es el nivel 4, mostrar mensaje de victoria final
            this.add.text(GAME_CONFIG.WIDTH / 2, 550, '¡JUEGO COMPLETADO!', {
                fontSize: '40px',
                fontFamily: 'Arial',
                fill: '#ffd700',
                stroke: '#000000',
                strokeThickness: 3
            }).setOrigin(0.5);
        }
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 350, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Efecto de celebración
        this.createCelebrationEffect();
    }
    
    createCelebrationEffect() {
        // Partículas de celebración
        for (let i = 0; i < 20; i++) {
            const particle = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                5,
                0xffd700
            );
            
            this.tweens.add({
                targets: particle,
                y: particle.y - 200,
                alpha: 0,
                duration: 2000 + Math.random() * 1000,
                delay: Math.random() * 500,
                repeat: -1
            });
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo de esta escena
            if (this.music && this.music.isPlaying) {
                this.music.stop();
                this.music.destroy();
                this.music = null;
            }
            
            // Detener todos los sonidos activos del sistema de sonido
            if (this.sound) {
                // Detener todos los sonidos que están reproduciéndose
                this.sound.sounds.forEach(sound => {
                    if (sound && sound.isPlaying) {
                        sound.stop();
                    }
                });
                // También intentar detener todos los sonidos por clave (si el método existe)
                if (this.sound.stopByKey) {
                    const soundKeys = ['sfx_victory', 'sfx_game_over', 'sfx_player_hurt', 'sfx_enemy_death', 
                                     'sfx_hit', 'sfx_throw', 'sfx_jump', 'sfx_boss_roar',
                                     'game_music', 'victory_music', 'menu_music'];
                    soundKeys.forEach(key => {
                        if (this.cache.audio.exists(key)) {
                            try {
                                this.sound.stopByKey(key);
                            } catch (e) {
                                // Ignorar si no existe el método
                            }
                        }
                    });
                }
            }
        } catch (e) {
            // Silenciar errores
            console.log('Error al detener sonidos:', e);
        }
    }
}


```

```javascript
class GameOverScene extends Phaser.Scene {
    constructor() {
        super({ key: 'GameOverScene' });
    }
    
    init(data) {
        this.finalScore = data.score || 0;
        this.level = data.level || 1;
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir sonido de game over
        try {
            if (this.cache.audio.exists('sfx_game_over')) {
                this.sound.play('sfx_game_over', { volume: 0.6 });
            }
        } catch (e) {
            // Silenciar errores si el sonido no existe
        }
        
        // Fondo
        this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, 0x1a0000)
            .setOrigin(0, 0);
        
        // Título Game Over
        this.add.text(GAME_CONFIG.WIDTH / 2, 200, 'GAME OVER', {
            fontSize: '72px',
            fontFamily: 'Arial',
            fill: '#ff0000',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5
            }
        }).setOrigin(0.5);
        
        // Información
        const infoStyle = {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 350, `Puntuación Final: ${this.finalScore}`, infoStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, `Nivel Alcanzado: ${this.level}`, infoStyle)
            .setOrigin(0.5);
        
        // Botón Reintentar
        const retryButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 550, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                retryButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                retryButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, 'REINTENTAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Botón Menú Principal
        const menuButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 650, 300, 80, 0x444444)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('MenuScene');
            })
            .on('pointerover', () => {
                menuButton.setFillStyle(0x666666);
            })
            .on('pointerout', () => {
                menuButton.setFillStyle(0x444444);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 650, 'MENÚ PRINCIPAL', {
            fontSize: '32px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo de esta escena
            if (this.music && this.music.isPlaying) {
                this.music.stop();
                this.music.destroy();
                this.music = null;
            }
            
            // Detener todos los sonidos activos del sistema de sonido
            if (this.sound) {
                // Detener todos los sonidos que están reproduciéndose
                this.sound.sounds.forEach(sound => {
                    if (sound && sound.isPlaying) {
                        sound.stop();
                    }
                });
                // También intentar detener todos los sonidos por clave (si el método existe)
                if (this.sound.stopByKey) {
                    const soundKeys = ['sfx_victory', 'sfx_game_over', 'sfx_player_hurt', 'sfx_enemy_death', 
                                     'sfx_hit', 'sfx_throw', 'sfx_jump', 'sfx_boss_roar',
                                     'game_music', 'victory_music', 'menu_music'];
                    soundKeys.forEach(key => {
                        if (this.cache.audio.exists(key)) {
                            try {
                                this.sound.stopByKey(key);
                            } catch (e) {
                                // Ignorar si no existe el método
                            }
                        }
                    });
                }
            }
        } catch (e) {
            // Silenciar errores
            console.log('Error al detener sonidos:', e);
        }
    }
}


```

```javascript
class MenuScene extends Phaser.Scene {
    constructor() {
        super({ key: 'MenuScene' });
    }
    
    create() {
        // Detener todos los sonidos de escenas anteriores
        this.stopAllSounds();
        
        // Reproducir música de menú
        try {
            if (this.cache.audio.exists('menu_music')) {
                this.music = this.sound.add('menu_music', { loop: true, volume: 0.3 });
                this.music.play();
            } else {
                console.log('Música de menú no encontrada');
            }
        } catch (e) {
            console.error('Error al reproducir música de menú:', e);
        }
        
        // Fondo - usar imagen si existe, sino usar color sólido
        if (this.textures.exists('menu_bg')) {
            this.add.image(0, 0, 'menu_bg').setOrigin(0, 0);
        } else {
            this.add.rectangle(0, 0, GAME_CONFIG.WIDTH, GAME_CONFIG.HEIGHT, COLORS.BACKGROUND)
                .setOrigin(0, 0);
        }
        
        // Título del juego
        const titleStyle = {
            fontSize: '64px',
            fontFamily: 'Arial',
            fill: '#ffd700',
            stroke: '#000000',
            strokeThickness: 4,
            shadow: {
                offsetX: 4,
                offsetY: 4,
                color: '#000000',
                blur: 5,
                stroke: true,
                fill: true
            }
        };
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 150, 'PLÁTANO BORRACHO', titleStyle)
            .setOrigin(0.5);
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 220, 'VS MONOS', {
            ...titleStyle,
            fontSize: '48px'
        }).setOrigin(0.5);
        
        // Botón Jugar
        const playButton = this.add.rectangle(GAME_CONFIG.WIDTH / 2, 400, 300, 80, 0x228b22)
            .setInteractive({ useHandCursor: true })
            .on('pointerdown', () => {
                // Detener todos los sonidos antes de cambiar de escena
                this.stopAllSounds();
                this.scene.start('GameScene', { level: 1, score: 0, lives: GAME_CONFIG.PLAYER_LIVES });
            })
            .on('pointerover', () => {
                playButton.setFillStyle(0x32cd32);
            })
            .on('pointerout', () => {
                playButton.setFillStyle(0x228b22);
            });
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 400, 'JUGAR', {
            fontSize: '36px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            stroke: '#000000',
            strokeThickness: 2
        }).setOrigin(0.5);
        
        // Instrucciones
        const instructionsStyle = {
            fontSize: '20px',
            fontFamily: 'Arial',
            fill: '#ffffff',
            align: 'center'
        };
        
        const instructions = [
            'CONTROLES:',
            'A/D o ←/→ : Mover',
            'W o ↑ : Saltar',
            'J o Espacio : Lanzar proyectil',
            '',
            'Derrota a los monos y enfrenta',
            'a los poderosos gorilas jefe!'
        ];
        
        this.add.text(GAME_CONFIG.WIDTH / 2, 550, instructions, instructionsStyle)
            .setOrigin(0.5);
        
        // Efecto de fondo animado (monos moviéndose)
        this.createAnimatedBackground();
    }
    
    createAnimatedBackground() {
        // Crear algunos monos decorativos en el fondo
        for (let i = 0; i < 5; i++) {
            const monkey = this.add.circle(
                Math.random() * GAME_CONFIG.WIDTH,
                Math.random() * GAME_CONFIG.HEIGHT,
                20,
                COLORS.MONKEY,
                0.3
            );
            
            this.tweens.add({
                targets: monkey,
                x: Math.random() * GAME_CONFIG.WIDTH,
                y: Math.random() * GAME_CONFIG.HEIGHT,
                duration: 3000 + Math.random() * 2000,
                repeat: -1,
                yoyo: true
            });
        }
    }
    
    /**
     * Detiene TODOS los sonidos y músicas activos
     */
    stopAllSounds() {
        try {
            // Detener música de fondo de esta escena
            if (this.music && this.music.isPlaying) {
                this.music.stop();
                this.music.destroy();
                this.music = null;
            }
            
            // Detener todos los sonidos activos del sistema de sonido
            if (this.sound) {
                // Detener todos los sonidos que están reproduciéndose
                this.sound.sounds.forEach(sound => {
                    if (sound && sound.isPlaying) {
                        sound.stop();
                    }
                });
                // También intentar detener todos los sonidos por clave (si el método existe)
                if (this.sound.stopByKey) {
                    const soundKeys = ['sfx_victory', 'sfx_game_over', 'sfx_player_hurt', 'sfx_enemy_death', 
                                     'sfx_hit', 'sfx_throw', 'sfx_jump', 'sfx_boss_roar',
                                     'game_music', 'victory_music', 'menu_music'];
                    soundKeys.forEach(key => {
                        if (this.cache.audio.exists(key)) {
                            try {
                                this.sound.stopByKey(key);
                            } catch (e) {
                                // Ignorar si no existe el método
                            }
                        }
                    });
                }
            }
        } catch (e) {
            // Silenciar errores
            console.log('Error al detener sonidos:', e);
        }
    }
}


```

Sistema de gestión de sonidos implementado. Resumen:

## Sistema de gestión de sonidos implementado

### Cambios realizados

1. Función `stopAllSounds()` en todas las escenas:
   - Detiene la música de fondo de la escena
   - Detiene todos los sonidos activos del sistema
   - Intenta detener sonidos por clave (si el método existe)
   - Destruye y limpia referencias de música

2. Llamadas a `stopAllSounds()`:
   - Al iniciar cada escena (en `create()`)
   - Antes de cambiar de escena (en botones)
   - Al completar nivel (en `completeLevel()`)
   - Al terminar el juego (en `gameOver()`)

3. Música del menú:
   - Se detienen todos los sonidos antes de reproducirla
   - Se agregaron logs para depuración
   - Se verifica que el archivo exista antes de reproducirlo

### Flujo de sonidos

- Menú → Juego: se detiene la música del menú, se inicia la del juego
- Juego → Victoria: se detienen todos los sonidos, se reproduce victoria
- Victoria → Siguiente nivel: se detienen todos los sonidos, se inicia la música del juego
- Victoria → Menú: se detienen todos los sonidos, se inicia la música del menú
- Juego → Game Over: se detienen todos los sonidos, se reproduce game over

### Resultado

- Sin superposiciones: los sonidos se detienen antes de iniciar nuevos
- Transiciones limpias: cada escena inicia con su música correspondiente
- Música del menú: debería reproducirse correctamente al entrar al menú

Si la música del menú no suena, revisa la consola del navegador para ver si aparece el mensaje "Música de menú no encontrada" o algún error. Esto indicará si el archivo no se está cargando correctamente.

